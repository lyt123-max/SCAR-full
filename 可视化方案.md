> From: https://gemini.google.com/u/1/app/5e5412422836f551?is_sa=1&is_sa=1&android-min-version=301356232&ios-min-version=322.0&campaign_id=bkws&utm_source=sem&utm_medium=paid-media&utm_campaign=bkws&pt=9008&mt=8&ct=p-growth-sem-bkws&gclsrc=aw.ds&gad_source=1&gad_campaignid=20108148196&gbraid=0AAAAApk5Bhmo4ZyZDH8_G8pqBMUeWxcae&gclid=Cj0KCQjw7cLOBhDmARIsAGsuA0kmMXA6HbyBsZovgzMRUhjW7B9ZTt3OUpq1WwvjorSsIT0bGyVeVrwaAgijEALw_wcB&pageId=none

# you asked

请你原封不动的输出：# 可视化方案：分优先级

---

## 结论：加，但分三个优先级

```
现在就加（调试必须）：   3 个
论文写作时加（投稿必须）：4 个
可以不加（锦上添花）：   跳过
```

**先把模型跑通、F1 调到合理水平，再画图。不要在模型还没跑通时花时间做可视化。**

---

## 第一优先级：调试用（现在就加）

这三个可视化不是给论文的，是**帮你判断模型是否正常工作**的。

### 1. 训练 Loss 曲线

```python
# 在 Stage A 训练结束后自动生成

import matplotlib.pyplot as plt

def plot_training_curves(history, save_path):
    """Stage A 的 train/val loss 曲线。"""
    fig, ax = plt.subplots(figsize=(10, 5))
    
    ax.plot(history['train_loss'], label='Train Loss', alpha=0.8)
    if 'val_loss' in history and history['val_loss']:
        ax.plot(history['val_loss'], label='Val Loss', alpha=0.8)
    
    # 标记 early stopping 点
    if 'val_loss' in history and history['val_loss']:
        best_epoch = np.argmin(history['val_loss'])
        ax.axvline(x=best_epoch, color='red', linestyle='--', 
                    alpha=0.5, label=f'Best (epoch {best_epoch})')
    
    ax.set_xlabel('Epoch')
    ax.set_ylabel('Loss')
    ax.set_title('Stage A Training Curve')
    ax.legend()
    ax.grid(True, alpha=0.3)
    
    fig.savefig(save_path, dpi=150, bbox_inches='tight')
    plt.close(fig)
    print(f"[Saved] {save_path}")
```

**看什么：**
```
✅ 正常：train 和 val 都下降，val 略高于 train，最终趋于平稳
⚠️ 异常：val 一直不降 → encoder 没学到东西
⚠️ 异常：train 降到极低但 val 上升 → 过拟合
⚠️ 异常：loss 突然跳升 → 梯度爆炸或数据问题
```

### 2. 分数分布直方图

```python
def plot_score_distribution(scores, labels, save_path):
    """测试集的异常分数分布：正常 vs 异常。"""
    fig, ax = plt.subplots(figsize=(10, 5))
    
    normal_scores = scores[labels == 0]
    anomaly_scores = scores[labels == 1]
    
    ax.hist(normal_scores, bins=100, alpha=0.5, density=True,
            color='blue', label=f'Normal (n={len(normal_scores)})')
    if len(anomaly_scores) > 0:
        ax.hist(anomaly_scores, bins=100, alpha=0.5, density=True,
                color='red', label=f'Anomaly (n={len(anomaly_scores)})')
    
    ax.set_xlabel('Anomaly Score')
    ax.set_ylabel('Density')
    ax.set_title('Score Distribution: Normal vs Anomaly')
    ax.legend()
    ax.grid(True, alpha=0.3)
    
    fig.savefig(save_path, dpi=150, bbox_inches='tight')
    plt.close(fig)
```

**看什么：**
```
✅ 正常：两个分布有明显分离 → 模型能区分正常和异常
⚠️ 异常：两个分布完全重叠 → 模型没学到有用的东西
⚠️ 异常：正常分数的分布有很长的右尾 → 会有大量误报
⚠️ 异常：异常分数分布偏低 → 会有大量漏检
```

### 3. 异常分数时间曲线

```python
def plot_score_timeline(scores, labels, save_path, 
                        time_range=None):
    """异常分数随时间变化 + 真实标签叠加。"""
    if time_range:
        s, e = time_range
        scores = scores[s:e]
        labels = labels[s:e]
    
    T = len(scores)
    t = np.arange(T)
    
    fig, axes = plt.subplots(2, 1, figsize=(16, 6),
                              gridspec_kw={'height_ratios': [4, 1]},
                              sharex=True)
    
    # 上：分数曲线
    axes[0].plot(t, scores, color='purple', linewidth=0.5, alpha=0.8)
    axes[0].fill_between(t, 0, scores, alpha=0.2, color='purple')
    
    # 叠加异常区域
    anomaly_mask = labels.astype(bool)
    if anomaly_mask.any():
        ymax = np.percentile(scores, 99.5)
        axes[0].fill_between(t, 0, ymax, where=anomaly_mask,
                              alpha=0.1, color='red')
    
    axes[0].set_ylabel('Anomaly Score')
    axes[0].set_title('Detection Result')
    axes[0].grid(True, alpha=0.2)
    
    # 下：真实标签条
    axes[1].imshow(labels.reshape(1, -1), aspect='auto',
                    cmap='Reds', interpolation='nearest',
                    extent=[0, T, 0, 1])
    axes[1].set_ylabel('GT')
    axes[1].set_yticks([])
    axes[1].set_xlabel('Time Step')
    
    plt.tight_layout()
    fig.savefig(save_path, dpi=150, bbox_inches='tight')
    plt.close(fig)
```

**看什么：**
```
✅ 正常：红色区域（真异常）处分数高，白色区域分数低
⚠️ 异常：异常区域分数不高 → 漏检
⚠️ 异常：正常区域大量高分 → 误报
⚠️ 异常：分数曲线完全平坦 → 模型输出退化为常数
```

---

## 第二优先级：论文用（写论文时加）

这四个是**论文必须有的图**，但可以等模型跑通、F1 合理之后再画。

### 4. STSD 分解效果图

```python
def plot_decomposition(X, S, R, labels, channels=[0, 1, 2],
                       save_path=None):
    """
    论文 Method 节或 Case Study 节的图。
    三行：原始信号 X、慢变分量 S、残差 R。
    展示 STSD 的分解效果。
    """
    n_ch = len(channels)
    fig, axes = plt.subplots(3, n_ch, figsize=(5*n_ch, 7), sharex=True)
    if n_ch == 1:
        axes = axes.reshape(3, 1)
    
    data = [X, S, R]
    titles = ['Raw Signal $X$', 'Slow Component $S$', 'Residual $R$']
    colors = ['#607D8B', '#4CAF50', '#FF9800']
    
    T = X.shape[0]
    t = np.arange(T)
    
    for col, ch in enumerate(channels):
        for row in range(3):
            ax = axes[row, col]
            sig = data[row][:, ch]
            ax.plot(t, sig, color=colors[row], linewidth=0.7)
            
            if labels is not None:
                ymin, ymax = sig.min(), sig.max()
                m = (ymax - ymin) * 0.1 + 1e-6
                ax.fill_between(t, ymin-m, ymax+m,
                                where=labels.astype(bool),
                                alpha=0.15, color='red')
            
            if row == 0:
                ax.set_title(f'Channel {ch}')
            if col == 0:
                ax.set_ylabel(titles[row])
            ax.grid(True, alpha=0.2)
    
    axes[-1, n_ch//2].set_xlabel('Time Step')
    plt.tight_layout()
    if save_path:
        fig.savefig(save_path, dpi=300, bbox_inches='tight')
    plt.close(fig)
```

**论文中放在哪里：**
```
Method 节 4.1（STSD 介绍）旁边放一个 Figure
或 Experiments 的 Case Study 节
```

### 5. 状态空间 t-SNE 图

```python
def plot_state_tsne(train_states, test_states, test_window_labels,
                    save_path=None):
    """
    论文 Case Study 或 Experiments 节的图。
    展示不同工况在 embedding 空间中的分布。
    """
    from sklearn.manifold import TSNE
    
    # 子采样
    n_train = min(3000, len(train_states))
    n_test = min(2000, len(test_states))
    idx_tr = np.random.choice(len(train_states), n_train, replace=False)
    idx_te = np.random.choice(len(test_states), n_test, replace=False)
    
    all_states = np.concatenate([
        train_states[idx_tr], test_states[idx_te]
    ])
    
    coords = TSNE(n_components=2, perplexity=30, random_state=42,
                   init='pca').fit_transform(all_states)
    
    tr_coords = coords[:n_train]
    te_coords = coords[n_train:]
    te_labels = test_window_labels[idx_te]
    
    fig, ax = plt.subplots(figsize=(8, 7))
    
    # Train: gray
    ax.scatter(tr_coords[:, 0], tr_coords[:, 1],
               c='gray', s=5, alpha=0.15, label='Train (Normal)')
    
    # Test normal: blue
    normal = te_labels < 0.1
    if normal.any():
        ax.scatter(te_coords[normal, 0], te_coords[normal, 1],
                   c='#2196F3', s=10, alpha=0.4, label='Test (Normal)')
    
    # Test anomaly: red
    anomaly = te_labels >= 0.1
    if anomaly.any():
        ax.scatter(te_coords[anomaly, 0], te_coords[anomaly, 1],
                   c='#F44336', s=15, alpha=0.6, label='Test (Anomaly)')
    
    ax.legend()
    ax.set_title('State Embedding Space (t-SNE)')
    ax.grid(True, alpha=0.2)
    
    plt.tight_layout()
    if save_path:
        fig.savefig(save_path, dpi=300, bbox_inches='tight')
    plt.close(fig)
```

**论文中放在哪里：**
```
Case Study 节
用来说明"不同工况在状态空间中自然分开，
异常窗口偏离正常区域或散布在边缘"
```

### 6. Ablation 柱状图

```python
def plot_ablation_bar(ablation_results, save_path=None):
    """
    论文 Ablation 节的图。
    横轴：各 variant，纵轴：F1。
    """
    names = list(ablation_results.keys())
    f1s = [ablation_results[n]['best_f1'] for n in names]
    
    # 排序
    order = np.argsort(f1s)[::-1]
    names = [names[i] for i in order]
    f1s = [f1s[i] for i in order]
    
    fig, ax = plt.subplots(figsize=(10, 5))
    
    colors = ['#4CAF50' if n == 'Full CoReM-AD' else '#90CAF9' for n in names]
    bars = ax.barh(range(len(names)), f1s, color=colors, edgecolor='white')
    
    ax.set_yticks(range(len(names)))
    ax.set_yticklabels(names)
    ax.set_xlabel('F1 Score')
    ax.set_title('Ablation Study')
    ax.invert_yaxis()
    
    # 标注数值
    for bar, f1 in zip(bars, f1s):
        ax.text(bar.get_width() + 0.005, bar.get_y() + bar.get_height()/2,
                f'{f1:.4f}', va='center', fontsize=9)
    
    ax.grid(True, alpha=0.2, axis='x')
    plt.tight_layout()
    if save_path:
        fig.savefig(save_path, dpi=300, bbox_inches='tight')
    plt.close(fig)
```

### 7. 检索可解释性图（Case Study 核心图）

```python
def plot_retrieval_case(
    query_patch,           # (P, C) 当前 patch 的原始信号
    retrieved_patches,     # (K, P, C) 检索到的 K 个正常 patch
    query_score,           # float 异常分数
    is_anomaly,            # bool 是否是异常
    channel=0,
    save_path=None,
):
    """
    论文 Case Study 的核心图。
    展示"当前 patch vs 检索到的正常近邻"。
    
    正常 patch：和近邻形状相似 → 分数低
    异常 patch：和近邻形状差异大 → 分数高
    """
    K = len(retrieved_patches)
    fig, axes = plt.subplots(1, K+1, figsize=(3*(K+1), 3), sharey=True)
    
    # 当前 patch
    color = '#F44336' if is_anomaly else '#2196F3'
    label = 'Anomaly' if is_anomaly else 'Normal'
    axes[0].plot(query_patch[:, channel], color=color, linewidth=1.5)
    axes[0].set_title(f'Query ({label})\nscore={query_score:.3f}',
                       fontweight='bold')
    axes[0].set_xlabel('Time')
    
    # 检索到的近邻
    for k in range(K):
        axes[k+1].plot(retrieved_patches[k, :, channel],
                        color='gray', linewidth=1)
        axes[k+1].plot(query_patch[:, channel],
                        color=color, linewidth=0.8, alpha=0.5, linestyle='--')
        axes[k+1].set_title(f'Neighbor {k+1}')
    
    fig.suptitle('Dual-Key Retrieval: Query vs Retrieved Normal Patches',
                  fontweight='bold', y=1.05)
    plt.tight_layout()
    if save_path:
        fig.savefig(save_path, dpi=300, bbox_inches='tight')
    plt.close(fig)
```

**论文中放在哪里：**
```
这是最有论文价值的图。

用两行展示：
  第一行：一个正常 patch → 检索到形状相似的近邻 → 分数低 → 正确
  第二行：一个异常 patch → 检索到的近邻和它不像 → 分数高 → 正确

这张图直接证明了你的核心 claim：
"异常判别是相对条件化局部正常流形来做的"
```

---

## 整合：何时画什么

```
现在（调试阶段）：
  ├── plot_training_curves()      ← Stage A 结束后自动生成
  ├── plot_score_distribution()   ← Test 结束后自动生成
  └── plot_score_timeline()       ← Test 结束后自动生成

写论文时：
  ├── plot_decomposition()        ← 选一个好的测试窗口手动画
  ├── plot_state_tsne()           ← 全部数据画一次
  ├── plot_ablation_bar()         ← Ablation 跑完后画
  └── plot_retrieval_case()       ← 精选 2-3 个 case 手动画
```

---

## 代码集成方式

把三个调试用可视化嵌入到你的训练和测试流程中，**自动生成**：

```python
# trainer.py

def run_stage_a(self, ...):
    # ... 训练 ...
    
    # ---- 训练结束自动画图 ----
    plot_training_curves(
        self.history,
        save_path=self.artifact_root / "stage_a_loss_curve.png"
    )


def run_test(self, ...):
    # ... 推理 + 评估 ...
    
    # ---- 测试结束自动画图 ----
    plot_score_distribution(
        scores, labels,
        save_path=self.artifact_root / "score_distribution.png"
    )
    
    plot_score_timeline(
        scores, labels,
        save_path=self.artifact_root / "score_timeline_full.png"
    )
    
    # 再画几个局部放大图（取异常段附近）
    segments = find_anomaly_segments(labels)
    for i, (seg_start, seg_end) in enumerate(segments[:3]):
        margin = 500
        plot_score_timeline(
            scores, labels,
            time_range=(max(0, seg_start-margin), min(len(scores), seg_end+margin)),
            save_path=self.artifact_root / f"score_timeline_segment_{i}.png"
        )
```

**这样每次跑完实验，artifacts 目录下自动有这些图，你不需要手动画。**

---

## 一句话总结

> **现在加 3 个调试用可视化（loss 曲线、分数分布、分数时间线），嵌入训练和测试流程自动生成。论文用的 4 个图（分解效果、t-SNE、ablation 柱状图、检索 case study）等模型跑通后再画。不要在模型还没跑通时花时间做可视化。**

---

# gemini response

# 可视化方案：分优先级

---

## 结论：加，但分三个优先级

```
现在就加（调试必须）：   3 个
论文写作时加（投稿必须）：4 个
可以不加（锦上添花）：   跳过
```

**先把模型跑通、F1 调到合理水平，再画图。不要在模型还没跑通时花时间做可视化。**

---

## 第一优先级：调试用（现在就加）

这三个可视化不是给论文的，是**帮你判断模型是否正常工作**的。

### 1. 训练 Loss 曲线

```python
# 在 Stage A 训练结束后自动生成

import matplotlib.pyplot as plt

def plot_training_curves(history, save_path):
    """Stage A 的 train/val loss 曲线。"""
    fig, ax = plt.subplots(figsize=(10, 5))
    
    ax.plot(history['train_loss'], label='Train Loss', alpha=0.8)
    if 'val_loss' in history and history['val_loss']:
        ax.plot(history['val_loss'], label='Val Loss', alpha=0.8)
    
    # 标记 early stopping 点
    if 'val_loss' in history and history['val_loss']:
        best_epoch = np.argmin(history['val_loss'])
        ax.axvline(x=best_epoch, color='red', linestyle='--', 
                    alpha=0.5, label=f'Best (epoch {best_epoch})')
    
    ax.set_xlabel('Epoch')
    ax.set_ylabel('Loss')
    ax.set_title('Stage A Training Curve')
    ax.legend()
    ax.grid(True, alpha=0.3)
    
    fig.savefig(save_path, dpi=150, bbox_inches='tight')
    plt.close(fig)
    print(f"[Saved] {save_path}")
```

**看什么：**
```
✅ 正常：train 和 val 都下降，val 略高于 train，最终趋于平稳
⚠️ 异常：val 一直不降 → encoder 没学到东西
⚠️ 异常：train 降到极低但 val 上升 → 过拟合
⚠️ 异常：loss 突然跳升 → 梯度爆炸或数据问题
```

### 2. 分数分布直方图

```python
def plot_score_distribution(scores, labels, save_path):
    """测试集的异常分数分布：正常 vs 异常。"""
    fig, ax = plt.subplots(figsize=(10, 5))
    
    normal_scores = scores[labels == 0]
    anomaly_scores = scores[labels == 1]
    
    ax.hist(normal_scores, bins=100, alpha=0.5, density=True,
            color='blue', label=f'Normal (n={len(normal_scores)})')
    if len(anomaly_scores) > 0:
        ax.hist(anomaly_scores, bins=100, alpha=0.5, density=True,
                color='red', label=f'Anomaly (n={len(anomaly_scores)})')
    
    ax.set_xlabel('Anomaly Score')
    ax.set_ylabel('Density')
    ax.set_title('Score Distribution: Normal vs Anomaly')
    ax.legend()
    ax.grid(True, alpha=0.3)
    
    fig.savefig(save_path, dpi=150, bbox_inches='tight')
    plt.close(fig)
```

**看什么：**
```
✅ 正常：两个分布有明显分离 → 模型能区分正常和异常
⚠️ 异常：两个分布完全重叠 → 模型没学到有用的东西
⚠️ 异常：正常分数的分布有很长的右尾 → 会有大量误报
⚠️ 异常：异常分数分布偏低 → 会有大量漏检
```

### 3. 异常分数时间曲线

```python
def plot_score_timeline(scores, labels, save_path, 
                        time_range=None):
    """异常分数随时间变化 + 真实标签叠加。"""
    if time_range:
        s, e = time_range
        scores = scores[s:e]
        labels = labels[s:e]
    
    T = len(scores)
    t = np.arange(T)
    
    fig, axes = plt.subplots(2, 1, figsize=(16, 6),
                              gridspec_kw={'height_ratios': [4, 1]},
                              sharex=True)
    
    # 上：分数曲线
    axes[0].plot(t, scores, color='purple', linewidth=0.5, alpha=0.8)
    axes[0].fill_between(t, 0, scores, alpha=0.2, color='purple')
    
    # 叠加异常区域
    anomaly_mask = labels.astype(bool)
    if anomaly_mask.any():
        ymax = np.percentile(scores, 99.5)
        axes[0].fill_between(t, 0, ymax, where=anomaly_mask,
                              alpha=0.1, color='red')
    
    axes[0].set_ylabel('Anomaly Score')
    axes[0].set_title('Detection Result')
    axes[0].grid(True, alpha=0.2)
    
    # 下：真实标签条
    axes[1].imshow(labels.reshape(1, -1), aspect='auto',
                    cmap='Reds', interpolation='nearest',
                    extent=[0, T, 0, 1])
    axes[1].set_ylabel('GT')
    axes[1].set_yticks([])
    axes[1].set_xlabel('Time Step')
    
    plt.tight_layout()
    fig.savefig(save_path, dpi=150, bbox_inches='tight')
    plt.close(fig)
```

**看什么：**
```
✅ 正常：红色区域（真异常）处分数高，白色区域分数低
⚠️ 异常：异常区域分数不高 → 漏检
⚠️ 异常：正常区域大量高分 → 误报
⚠️ 异常：分数曲线完全平坦 → 模型输出退化为常数
```

---

## 第二优先级：论文用（写论文时加）

这四个是**论文必须有的图**，但可以等模型跑通、F1 合理之后再画。

### 4. STSD 分解效果图

```python
def plot_decomposition(X, S, R, labels, channels=[0, 1, 2],
                        save_path=None):
    """
    论文 Method 节或 Case Study 节的图。
    三行：原始信号 X、慢变分量 S、残差 R。
    展示 STSD 的分解效果。
    """
    n_ch = len(channels)
    fig, axes = plt.subplots(3, n_ch, figsize=(5*n_ch, 7), sharex=True)
    if n_ch == 1:
        axes = axes.reshape(3, 1)
    
    data = [X, S, R]
    titles = ['Raw Signal $X$', 'Slow Component $S$', 'Residual $R$']
    colors = ['#607D8B', '#4CAF50', '#FF9800']
    
    T = X.shape[0]
    t = np.arange(T)
    
    for col, ch in enumerate(channels):
        for row in range(3):
            ax = axes[row, col]
            sig = data[row][:, ch]
            ax.plot(t, sig, color=colors[row], linewidth=0.7)
            
            if labels is not None:
                ymin, ymax = sig.min(), sig.max()
                m = (ymax - ymin) * 0.1 + 1e-6
                ax.fill_between(t, ymin-m, ymax+m,
                                where=labels.astype(bool),
                                alpha=0.15, color='red')
            
            if row == 0:
                ax.set_title(f'Channel {ch}')
            if col == 0:
                ax.set_ylabel(titles[row])
            ax.grid(True, alpha=0.2)
    
    axes[-1, n_ch//2].set_xlabel('Time Step')
    plt.tight_layout()
    if save_path:
        fig.savefig(save_path, dpi=300, bbox_inches='tight')
    plt.close(fig)
```

**论文中放在哪里：**
```
Method 节 4.1（STSD 介绍）旁边放一个 Figure
或 Experiments 的 Case Study 节
```

### 5. 状态空间 t-SNE 图

```python
def plot_state_tsne(train_states, test_states, test_window_labels,
                    save_path=None):
    """
    论文 Case Study 或 Experiments 节的图。
    展示不同工况在 embedding 空间中的分布。
    """
    from sklearn.manifold import TSNE
    
    # 子采样
    n_train = min(3000, len(train_states))
    n_test = min(2000, len(test_states))
    idx_tr = np.random.choice(len(train_states), n_train, replace=False)
    idx_te = np.random.choice(len(test_states), n_test, replace=False)
    
    all_states = np.concatenate([
        train_states[idx_tr], test_states[idx_te]
    ])
    
    coords = TSNE(n_components=2, perplexity=30, random_state=42,
                    init='pca').fit_transform(all_states)
    
    tr_coords = coords[:n_train]
    te_coords = coords[n_train:]
    te_labels = test_window_labels[idx_te]
    
    fig, ax = plt.subplots(figsize=(8, 7))
    
    # Train: gray
    ax.scatter(tr_coords[:, 0], tr_coords[:, 1],
                c='gray', s=5, alpha=0.15, label='Train (Normal)')
    
    # Test normal: blue
    normal = te_labels < 0.1
    if normal.any():
        ax.scatter(te_coords[normal, 0], te_coords[normal, 1],
                    c='#2196F3', s=10, alpha=0.4, label='Test (Normal)')
    
    # Test anomaly: red
    anomaly = te_labels >= 0.1
    if anomaly.any():
        ax.scatter(te_coords[anomaly, 0], te_coords[anomaly, 1],
                    c='#F44336', s=15, alpha=0.6, label='Test (Anomaly)')
    
    ax.legend()
    ax.set_title('State Embedding Space (t-SNE)')
    ax.grid(True, alpha=0.2)
    
    plt.tight_layout()
    if save_path:
        fig.savefig(save_path, dpi=300, bbox_inches='tight')
    plt.close(fig)
```

**论文中放在哪里：**
```
Case Study 节
用来说明"不同工况在状态空间中自然分开，
异常窗口偏离正常区域或散布在边缘"
```

### 6. Ablation 柱状图

```python
def plot_ablation_bar(ablation_results, save_path=None):
    """
    论文 Ablation 节的图。
    横轴：各 variant，纵轴：F1。
    """
    names = list(ablation_results.keys())
    f1s = [ablation_results[n]['best_f1'] for n in names]
    
    # 排序
    order = np.argsort(f1s)[::-1]
    names = [names[i] for i in order]
    f1s = [f1s[i] for i in order]
    
    fig, ax = plt.subplots(figsize=(10, 5))
    
    colors = ['#4CAF50' if n == 'Full CoReM-AD' else '#90CAF9' for n in names]
    bars = ax.barh(range(len(names)), f1s, color=colors, edgecolor='white')
    
    ax.set_yticks(range(len(names)))
    ax.set_yticklabels(names)
    ax.set_xlabel('F1 Score')
    ax.set_title('Ablation Study')
    ax.invert_yaxis()
    
    # 标注数值
    for bar, f1 in zip(bars, f1s):
        ax.text(bar.get_width() + 0.005, bar.get_y() + bar.get_height()/2,
                f'{f1:.4f}', va='center', fontsize=9)
    
    ax.grid(True, alpha=0.2, axis='x')
    plt.tight_layout()
    if save_path:
        fig.savefig(save_path, dpi=300, bbox_inches='tight')
    plt.close(fig)
```

### 7. 检索可解释性图（Case Study 核心图）

```python
def plot_retrieval_case(
    query_patch,           # (P, C) 当前 patch 的原始信号
    retrieved_patches,     # (K, P, C) 检索到的 K 个正常 patch
    query_score,           # float 异常分数
    is_anomaly,            # bool 是否是异常
    channel=0,
    save_path=None,
):
    """
    论文 Case Study 的核心图。
    展示"当前 patch vs 检索到的正常近邻"。
    
    正常 patch：和近邻形状相似 → 分数低
    异常 patch：和近邻形状差异大 → 分数高
    """
    K = len(retrieved_patches)
    fig, axes = plt.subplots(1, K+1, figsize=(3*(K+1), 3), sharey=True)
    
    # 当前 patch
    color = '#F44336' if is_anomaly else '#2196F3'
    label = 'Anomaly' if is_anomaly else 'Normal'
    axes[0].plot(query_patch[:, channel], color=color, linewidth=1.5)
    axes[0].set_title(f'Query ({label})\nscore={query_score:.3f}',
                        fontweight='bold')
    axes[0].set_xlabel('Time')
    
    # 检索到的近邻
    for k in range(K):
        axes[k+1].plot(retrieved_patches[k, :, channel],
                        color='gray', linewidth=1)
        axes[k+1].plot(query_patch[:, channel],
                        color=color, linewidth=0.8, alpha=0.5, linestyle='--')
        axes[k+1].set_title(f'Neighbor {k+1}')
    
    fig.suptitle('Dual-Key Retrieval: Query vs Retrieved Normal Patches',
                  fontweight='bold', y=1.05)
    plt.tight_layout()
    if save_path:
        fig.savefig(save_path, dpi=300, bbox_inches='tight')
    plt.close(fig)
```

**论文中放在哪里：**
```
这是最有论文价值的图。

用两行展示：
  第一行：一个正常 patch → 检索到形状相似的近邻 → 分数低 → 正确
  第二行：一个异常 patch → 检索到的近邻和它不像 → 分数高 → 正确

这张图直接证明了你的核心 claim：
"异常判别是相对条件化局部正常流形来做的"
```

---

## 整合：何时画什么

```
现在（调试阶段）：
  ├── plot_training_curves()      ← Stage A 结束后自动生成
  ├── plot_score_distribution()   ← Test 结束后自动生成
  └── plot_score_timeline()       ← Test 结束后自动生成

写论文时：
  ├── plot_decomposition()        ← 选一个好的测试窗口手动画
  ├── plot_state_tsne()           ← 全部数据画一次
  ├── plot_ablation_bar()         ← Ablation 跑完后画
  └── plot_retrieval_case()       ← 精选 2-3 个 case 手动画
```

---

## 代码集成方式

把三个调试用可视化嵌入到你的训练和测试流程中，**自动生成**：

```python
# trainer.py

def run_stage_a(self, ...):
    # ... 训练 ...
    
    # ---- 训练结束自动画图 ----
    plot_training_curves(
        self.history,
        save_path=self.artifact_root / "stage_a_loss_curve.png"
    )


def run_test(self, ...):
    # ... 推理 + 评估 ...
    
    # ---- 测试结束自动画图 ----
    plot_score_distribution(
        scores, labels,
        save_path=self.artifact_root / "score_distribution.png"
    )
    
    plot_score_timeline(
        scores, labels,
        save_path=self.artifact_root / "score_timeline_full.png"
    )
    
    # 再画几个局部放大图（取异常段附近）
    segments = find_anomaly_segments(labels)
    for i, (seg_start, seg_end) in enumerate(segments[:3]):
        margin = 500
        plot_score_timeline(
            scores, labels,
            time_range=(max(0, seg_start-margin), min(len(scores), seg_end+margin)),
            save_path=self.artifact_root / f"score_timeline_segment_{i}.png"
        )
```

**这样每次跑完实验，artifacts 目录下自动有这些图，你不需要手动画。**

---

## 一句话总结

> **现在加 3 个调试用可视化（loss 曲线、分数分布、分数时间线），嵌入训练和测试流程自动生成。论文用的 4 个图（分解效果、t-SNE、ablation 柱状图、检索 case study）等模型跑通后再画。不要在模型还没跑通时花时间做可视化。**