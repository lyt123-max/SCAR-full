# 消融实验方案

## 本轮落地范围

本轮先只落地 A1，两组消融都已经拆成独立脚本，不再把多个消融混在同一个入口文件里。

已落地文件：

- `scripts/ablation/run_full_ablation.sh`
- `scripts/ablation/run_a1_wo_decomposition.sh`
- `scripts/ablation/run_a1_wo_state_aware_representation.sh`
- `scripts/ablation/run_a1_matrix.sh`

兼容入口：

- `scripts/ablation/run_dataset_ablation.sh`

它现在只负责分发，不再承担 A1 消融本身的配置逻辑。

---

## A0. Full 对照定义

所有 A1 消融都相对同一个 Full 版本比较，默认保持以下设置不变：

- STSD 分解
- 状态编码器 `S -> g_w`
- 状态条件化通道调制
- 双尺度 patch 编码
- 两级检索
- context key 检索
- completion 分支
- state novelty
- CDF-PIT 融合

除非某个消融本身就在研究这些模块，否则下面这些训练和检索超参不单独改：

- `seq_len`
- `batch_size`
- `lr`
- `epoch`
- `top_M`
- `top_K`
- `knn_k`
- `coreset_keep_ratio`
- 随机种子

---

## A1. 表征主干消融

A1 只回答一个问题：

> 收益是否主要来自“状态感知的表征主干”，而不是来自后面的检索或打分技巧？

### A1.1 w/o decomposition

文件标题：`A1: w/o decomposition`

脚本文件：`scripts/ablation/run_a1_wo_decomposition.sh`

实现方式：

- 关闭 `use_stsd_decomposition`
- 将 `lambda_smooth` 置为 `0.0`
- 其余训练、检索和融合设置保持与 Full 一致

实验含义：

- 不再显式把输入分成 `S` 和 `R`
- 状态编码器和 patch 编码器都直接吃原始输入 `X`
- 用来验证 STSD 分解是否真的在帮模型把慢变工况和快变局部扰动拆开

### A1.2 w/o state-aware representation

文件标题：`A1: w/o state-aware representation`

脚本文件：`scripts/ablation/run_a1_wo_state_aware_representation.sh`

实现方式：

- 关闭 `use_channel_modulation`
- 保留状态编码器
- 保留 state-based coarse retrieval
- 保留 state novelty
- 保留 context key 检索、completion 分支和后续融合

实验含义：

- 状态向量 `g_w` 仍然存在
- 但它不再进入局部 patch 表征链路
- 这样可以把“状态是否参与表征学习”和“状态是否参与检索条件”两个问题拆开

---

## A1 推荐汇报表

| Variant | SWAT | SMD | PSM | MSL | SMAP | Avg |
|---|---:|---:|---:|---:|---:|---:|
| Full |  |  |  |  |  |  |
| w/o decomposition |  |  |  |  |  |  |
| w/o state-aware representation |  |  |  |  |  |  |

建议主指标：

- `best_f1`
- `pr_auc`
- `range_f1`

建议补充观察：

- 工况切换段误报是否上升
- state space 聚类是否变差
- memory distance 的 normal / anomaly 分离度是否下降

---

## 运行方式

直接跑某一个 A1 消融：

```bash
bash scripts/ablation/run_a1_wo_decomposition.sh PSM
bash scripts/ablation/run_a1_wo_state_aware_representation.sh PSM
```

跑 A1 整组：

```bash
bash scripts/ablation/run_a1_matrix.sh
```

如果仍想沿用旧入口，也可以：

```bash
bash scripts/ablation/run_psm.sh wo_decomposition
bash scripts/ablation/run_psm.sh wo_state_aware_representation
```

---

## 待拍板事项

下面两点我先按当前代码现状保留说明，没有擅自替你扩展：

1. 文档里原来写过 `WADI`，但当前代码只支持 `MSL / SMAP / PSM / SWAT / SMD`，还没有 `WADI` 数据入口。
2. 文档里提到“原始旁路门控”，但当前模型实现里没有独立的 raw bypass gate。现在的 A1.2 是用“关闭 channel modulation、保留 state retrieval 和 novelty”来近似对应你的定义。

---

## 下一步

A2 和 A3 先不在本轮继续落地，等你先拍板下面两件事后我再继续拆：

- A2 最终到底是按 `global / state-only / context-only / dual-condition` 四组来做，还是沿用现在代码里的旧命名；
- `WADI` 是否要正式接入，如果要接，我会一起把表格和脚本范围补齐。
