> From: https://gemini.google.com/u/1/app/34e480718aa4114e?is_sa=1&is_sa=1&android-min-version=301356232&ios-min-version=322.0&campaign_id=bkws&utm_source=sem&utm_medium=paid-media&utm_campaign=bkws&pt=9008&mt=8&ct=p-growth-sem-bkws&gclsrc=aw.ds&gad_source=1&gad_campaignid=20108148196&gbraid=0AAAAApk5Bhmo4ZyZDH8_G8pqBMUeWxcae&gclid=Cj0KCQjw7cLOBhDmARIsAGsuA0kmMXA6HbyBsZovgzMRUhjW7B9ZTt3OUpq1WwvjorSsIT0bGyVeVrwaAgijEALw_wcB&pageId=none

# you asked

请你原封不动的输出：# 三阶段自动运行：评估

---

## 结论：方向对，但需要确认几个关键点

三阶段自动串联本身没有问题，但**自动化程度越高，出错时越难定位**。需要确认以下几个方面。

---

## 必须满足的硬性条件

### 1. 阶段间的依赖关系必须严格

```
正确的依赖链：

  Stage A 成功完成
       │
       ├── stage_a_best.pt 存在且可加载
       │
       ▼
  Stage B 成功完成
       │
       ├── memory_bank.npz 存在
       ├── faiss index 存在
       │
       ▼
  Stage C 成功完成（或跳过）
       │
       ├── stage_c.pt 存在（如果没跳过）
       │
       ▼
  Test

如果任何一个阶段失败，后续阶段必须停止，不能继续跑。
```

**需要确认：你的脚本在 Stage A 失败时是否会自动终止？**

```bash
# 你的脚本开头有 set -euo pipefail ← ✅ 好
# 这意味着任何命令返回非零就会终止整个脚本
# 但要确认 python run.py 在出错时确实返回非零退出码

# 在 run.py 中应该有：
import sys

try:
    main()
except Exception as e:
    print(f"[FATAL] {e}")
    sys.exit(1)  # ← 确保 bash 能捕获到失败
```

### 2. Checkpoint 存在性检查

```python
# run.py 中每个阶段开头应该检查前置产出

def run_stage_b(config):
    ckpt_path = config.artifact_root / "stage_a_best.pt"
    if not ckpt_path.exists():
        raise FileNotFoundError(
            f"Stage A checkpoint not found at {ckpt_path}. "
            f"Run Stage A first."
        )
    # ...

def run_stage_c(config):
    memory_path = config.artifact_root / "memory_bank.npz"
    if not memory_path.exists():
        raise FileNotFoundError(
            f"Memory bank not found at {memory_path}. "
            f"Run Stage B first."
        )
    # ...

def run_test(config):
    # 检查 stage_a + memory 必须存在
    # stage_c 可选（如果不存在就不用 scorer）
    pass
```

### 3. model.eval() 和 requires_grad 的切换

```
最容易出的 bug：

  Stage A 结束时 model 处于 train() 模式
  → Stage B 忘了切 eval()
  → BatchNorm 用 batch statistics 而非 running statistics
  → memory 里的表征和测试时的表征不一致
  → 检索距离全部偏移
  → F1 莫名其妙地低

  Stage B 结束时 model 处于 eval() 模式
  → Stage C 忘了 freeze encoder
  → scorer 训练时 encoder 也在更新
  → memory 和 encoder 不在同一表征空间
  → 同样的灾难
```

**在每个阶段入口加防御性检查：**

```python
def run_stage_b(model, ...):
    model.eval()
    for p in model.parameters():
        p.requires_grad = False
    # 确认
    assert not model.training, "Model must be in eval mode for Stage B"


def run_stage_c(model, scorer, ...):
    model.eval()
    for p in model.parameters():
        p.requires_grad = False
    scorer.train()
    for p in scorer.parameters():
        p.requires_grad = True
    # 确认
    assert not model.training, "Encoder must be frozen in Stage C"
    assert scorer.training, "Scorer must be in train mode for Stage C"
```

### 4. 同一个进程 vs 三次独立调用

```
两种运行方式都可以：

方式 A：一个进程跑完三个阶段（你的 --stage full）
  优点：简单，一条命令搞定
  缺点：Stage A 跑了 2 小时后 Stage B 挂了，要从头来
  
方式 B：三次独立调用
  python run.py --stage stage_a
  python run.py --stage stage_b  
  python run.py --stage stage_c
  python run.py --stage test
  优点：每个阶段可以独立重跑
  缺点：需要手动管理

推荐：两种都支持

  --stage full    → A + B + C + Test 一气呵成
  --stage stage_a → 只跑 A
  --stage stage_b → 只跑 B（要求 A 的 checkpoint 存在）
  --stage stage_c → 只跑 C（要求 A 和 B 的产出存在）
  --stage test    → 只跑测试（要求 A 和 B 存在，C 可选）
```

**你的脚本已经支持 `STAGE="${1:-full}"`，这是对的。但需要确认 `--stage stage_b` 单独跑时能正确加载 Stage A 的 checkpoint。**

---

## 建议加的东西

### 1. 阶段间的日志分隔

```python
# 在 run.py 中每个阶段前后打印清晰的分隔线

def run_full(config):
    
    # Stage A
    print("\n" + "=" * 70)
    print("  STAGE A: Self-Supervised Representation Learning")
    print("=" * 70)
    t0 = time.time()
    run_stage_a(config)
    print(f"  Stage A completed in {time.time()-t0:.1f}s")
    print(f"  Checkpoint: {config.artifact_root}/stage_a_best.pt")
    
    # Stage B
    print("\n" + "=" * 70)
    print("  STAGE B: Building Memory Bank")
    print("=" * 70)
    t0 = time.time()
    run_stage_b(config)
    print(f"  Stage B completed in {time.time()-t0:.1f}s")
    print(f"  Memory: {config.artifact_root}/memory_bank.npz")
    
    # Stage C
    print("\n" + "=" * 70)
    print("  STAGE C: Training Scorer (Optional)")
    print("=" * 70)
    t0 = time.time()
    run_stage_c(config)
    print(f"  Stage C completed in {time.time()-t0:.1f}s")
    
    # Test
    print("\n" + "=" * 70)
    print("  TESTING")
    print("=" * 70)
    t0 = time.time()
    metrics = run_test(config)
    print(f"  Testing completed in {time.time()-t0:.1f}s")
    
    return metrics
```

### 2. 每个阶段结束后的 Sanity Check

```python
def run_stage_a(config):
    # ... 训练 ...
    
    # ---- 阶段结束后自动检查 ----
    ckpt = torch.load(config.artifact_root / "stage_a_best.pt")
    assert 'model_state' in ckpt, "Checkpoint missing model_state"
    assert 'normalizer_mean' in ckpt, "Checkpoint missing normalizer"
    print(f"  [Check] Stage A checkpoint valid")
    print(f"  [Check] Best val_loss: {ckpt.get('best_val_loss', 'N/A')}")
    print(f"  [Check] Stopped at epoch: {ckpt.get('epoch', 'N/A')}")


def run_stage_b(config):
    # ... 建库 ...
    
    # ---- Faiss 自检索 ----
    print(f"  [Check] Memory patches: scale_0={len(z0)}, scale_1={len(z1)}")
    print(f"  [Check] Faiss self-retrieval: PASSED")


def run_test(config):
    # ... 推理 ...
    
    # ---- 分数合理性检查 ----
    print(f"  [Check] Score range: [{scores.min():.4f}, {scores.max():.4f}]")
    print(f"  [Check] Score NaN count: {np.isnan(scores).sum()}")
    assert not np.isnan(scores).any(), "Scores contain NaN!"
```

### 3. 结果自动保存

```python
def run_test(config):
    # ... 计算 metrics ...
    
    # 保存结果到 JSON
    result_path = config.artifact_root / "results.json"
    
    results_to_save = {
        'dataset': config.dataset,
        'experiment': config.experiment_name,
        'timestamp': time.strftime('%Y-%m-%d %H:%M:%S'),
        'config': vars(config),
        'metrics': metrics,  # best_f1, auc_roc, pa_f1, ...
    }
    
    with open(result_path, 'w') as f:
        json.dump(results_to_save, f, indent=2, default=str)
    
    print(f"  Results saved to {result_path}")
```

### 4. 断点续跑能力

```python
def run_full(config):
    """如果某个阶段的产出已经存在，跳过它。"""
    
    stage_a_done = (config.artifact_root / "stage_a_best.pt").exists()
    stage_b_done = (config.artifact_root / "memory_bank.npz").exists()
    stage_c_done = (config.artifact_root / "stage_c.pt").exists()
    
    if not stage_a_done:
        run_stage_a(config)
    else:
        print("  [Skip] Stage A checkpoint found, skipping.")
    
    if not stage_b_done:
        run_stage_b(config)
    else:
        print("  [Skip] Memory bank found, skipping.")
    
    if not stage_c_done:
        run_stage_c(config)
    else:
        print("  [Skip] Scorer checkpoint found, skipping.")
    
    metrics = run_test(config)
    return metrics
```

**这个功能非常实用：Stage A 跑了 2 小时，Stage B 挂了 → 修完 bug 后重跑 `--stage full`，自动跳过 Stage A，直接从 Stage B 开始。**

但需要加一个 `--force` 参数来强制重跑所有阶段：

```bash
# 自动跳过已完成阶段
bash scripts/train_msl.sh full msl_exp1

# 强制从头跑（忽略已有 checkpoint）
FORCE=1 bash scripts/train_msl.sh full msl_exp1
```

---

## 自检清单

在正式提交到服务器跑之前，在本地用小数据验证这些：

```bash
# 1. full 模式能跑通
python run.py --stage full --dataset MSL --stage_a_epochs 3 --stage_c_epochs 2

# 2. 分步模式能跑通
python run.py --stage stage_a --dataset MSL --stage_a_epochs 3
python run.py --stage stage_b --dataset MSL
python run.py --stage stage_c --dataset MSL --stage_c_epochs 2
python run.py --stage test --dataset MSL

# 3. Stage A 中断后能从 B 续跑
python run.py --stage stage_a --dataset MSL --stage_a_epochs 3
# 假装 Stage B 挂了，修完后：
python run.py --stage full --dataset MSL  # 应该跳过 A，直接跑 B

# 4. Stage A 失败时后续不继续
python run.py --stage full --dataset NONEXIST  # 应该在 A 就报错停止

# 5. 结果文件正确生成
ls artifacts/msl_exp1/
# 应该看到：stage_a_best.pt, memory_bank.npz, stage_c.pt, results.json
```

---

## 一句话总结

> **三阶段自动运行的方案没问题。需要确认的核心是：阶段间 checkpoint 存在性检查、model.eval()/requires_grad 的正确切换、失败时及时终止。建议加上断点续跑能力和每阶段结束后的 sanity check。这些都是小改动，但能在长时间训练中帮你避免"跑了 3 小时发现中间某步出错，结果全废"的情况。**

---

# gemini response

# 三阶段自动运行：评估

---

## 结论：方向对，但需要确认几个关键点

三阶段自动串联本身没有问题，但**自动化程度越高，出错时越难定位**。需要确认以下几个方面。

---

## 必须满足的硬性条件

### 1. 阶段间的依赖关系必须严格

```
正确的依赖链：

  Stage A 成功完成
       │
       ├── stage_a_best.pt 存在且可加载
       │
       ▼
  Stage B 成功完成
       │
       ├── memory_bank.npz 存在
       ├── faiss index 存在
       │
       ▼
  Stage C 成功完成（或跳过）
       │
       ├── stage_c.pt 存在（如果没跳过）
       │
       ▼
  Test

如果任何一个阶段失败，后续阶段必须停止，不能继续跑。
```

**需要确认：你的脚本在 Stage A 失败时是否会自动终止？**

```bash
# 你的脚本开头有 set -euo pipefail ← ✅ 好
# 这意味着任何命令返回非零就会终止整个脚本
# 但要确认 python run.py 在出错时确实返回非零退出码

# 在 run.py 中应该有：
import sys

try:
    main()
except Exception as e:
    print(f"[FATAL] {e}")
    sys.exit(1)  # ← 确保 bash 能捕获到失败
```

### 2. Checkpoint 存在性检查

```python
# run.py 中每个阶段开头应该检查前置产出

def run_stage_b(config):
    ckpt_path = config.artifact_root / "stage_a_best.pt"
    if not ckpt_path.exists():
        raise FileNotFoundError(
            f"Stage A checkpoint not found at {ckpt_path}. "
            f"Run Stage A first."
        )
    # ...

def run_stage_c(config):
    memory_path = config.artifact_root / "memory_bank.npz"
    if not memory_path.exists():
        raise FileNotFoundError(
            f"Memory bank not found at {memory_path}. "
            f"Run Stage B first."
        )
    # ...

def run_test(config):
    # 检查 stage_a + memory 必须存在
    # stage_c 可选（如果不存在就不用 scorer）
    pass
```

### 3. model.eval() 和 requires_grad 的切换

```
最容易出的 bug：

  Stage A 结束时 model 处于 train() 模式
  → Stage B 忘了切 eval()
  → BatchNorm 用 batch statistics 而非 running statistics
  → memory 里的表征和测试时的表征不一致
  → 检索距离全部偏移
  → F1 莫名其妙地低

  Stage B 结束时 model 处于 eval() 模式
  → Stage C 忘了 freeze encoder
  → scorer 训练时 encoder 也在更新
  → memory 和 encoder 不在同一表征空间
  → 同样的灾难
```

**在每个阶段入口加防御性检查：**

```python
def run_stage_b(model, ...):
    model.eval()
    for p in model.parameters():
        p.requires_grad = False
    # 确认
    assert not model.training, "Model must be in eval mode for Stage B"


def run_stage_c(model, scorer, ...):
    model.eval()
    for p in model.parameters():
        p.requires_grad = False
    scorer.train()
    for p in scorer.parameters():
        p.requires_grad = True
    # 确认
    assert not model.training, "Encoder must be frozen in Stage C"
    assert scorer.training, "Scorer must be in train mode for Stage C"
```

### 4. 同一个进程 vs 三次独立调用

```
两种运行方式都可以：

方式 A：一个进程跑完三个阶段（你的 --stage full）
  优点：简单，一条命令搞定
  缺点：Stage A 跑了 2 小时后 Stage B 挂了，要从头来
  
方式 B：三次独立调用
  python run.py --stage stage_a
  python run.py --stage stage_b  
  python run.py --stage stage_c
  python run.py --stage test
  优点：每个阶段可以独立重跑
  缺点：需要手动管理

推荐：两种都支持

  --stage full    → A + B + C + Test 一气呵成
  --stage stage_a → 只跑 A
  --stage stage_b → 只跑 B（要求 A 的 checkpoint 存在）
  --stage stage_c → 只跑 C（要求 A 和 B 的产出存在）
  --stage test    → 只跑测试（要求 A 和 B 存在，C 可选）
```

**你的脚本已经支持 `STAGE="${1:-full}"`，这是对的。但需要确认 `--stage stage_b` 单独跑时能正确加载 Stage A 的 checkpoint。**

---

## 建议加的东西

### 1. 阶段间的日志分隔

```python
# 在 run.py 中每个阶段前后打印清晰的分隔线

def run_full(config):
    
    # Stage A
    print("\n" + "=" * 70)
    print("  STAGE A: Self-Supervised Representation Learning")
    print("=" * 70)
    t0 = time.time()
    run_stage_a(config)
    print(f"  Stage A completed in {time.time()-t0:.1f}s")
    print(f"  Checkpoint: {config.artifact_root}/stage_a_best.pt")
    
    # Stage B
    print("\n" + "=" * 70)
    print("  STAGE B: Building Memory Bank")
    print("=" * 70)
    t0 = time.time()
    run_stage_b(config)
    print(f"  Stage B completed in {time.time()-t0:.1f}s")
    print(f"  Memory: {config.artifact_root}/memory_bank.npz")
    
    # Stage C
    print("\n" + "=" * 70)
    print("  STAGE C: Training Scorer (Optional)")
    print("=" * 70)
    t0 = time.time()
    run_stage_c(config)
    print(f"  Stage C completed in {time.time()-t0:.1f}s")
    
    # Test
    print("\n" + "=" * 70)
    print("  TESTING")
    print("=" * 70)
    t0 = time.time()
    metrics = run_test(config)
    print(f"  Testing completed in {time.time()-t0:.1f}s")
    
    return metrics
```

### 2. 每个阶段结束后的 Sanity Check

```python
def run_stage_a(config):
    # ... 训练 ...
    
    # ---- 阶段结束后自动检查 ----
    ckpt = torch.load(config.artifact_root / "stage_a_best.pt")
    assert 'model_state' in ckpt, "Checkpoint missing model_state"
    assert 'normalizer_mean' in ckpt, "Checkpoint missing normalizer"
    print(f"  [Check] Stage A checkpoint valid")
    print(f"  [Check] Best val_loss: {ckpt.get('best_val_loss', 'N/A')}")
    print(f"  [Check] Stopped at epoch: {ckpt.get('epoch', 'N/A')}")


def run_stage_b(config):
    # ... 建库 ...
    
    # ---- Faiss 自检索 ----
    print(f"  [Check] Memory patches: scale_0={len(z0)}, scale_1={len(z1)}")
    print(f"  [Check] Faiss self-retrieval: PASSED")


def run_test(config):
    # ... 推理 ...
    
    # ---- 分数合理性检查 ----
    print(f"  [Check] Score range: [{scores.min():.4f}, {scores.max():.4f}]")
    print(f"  [Check] Score NaN count: {np.isnan(scores).sum()}")
    assert not np.isnan(scores).any(), "Scores contain NaN!"
```

### 3. 结果自动保存

```python
def run_test(config):
    # ... 计算 metrics ...
    
    # 保存结果到 JSON
    result_path = config.artifact_root / "results.json"
    
    results_to_save = {
        'dataset': config.dataset,
        'experiment': config.experiment_name,
        'timestamp': time.strftime('%Y-%m-%d %H:%M:%S'),
        'config': vars(config),
        'metrics': metrics,  # best_f1, auc_roc, pa_f1, ...
    }
    
    with open(result_path, 'w') as f:
        json.dump(results_to_save, f, indent=2, default=str)
    
    print(f"  Results saved to {result_path}")
```

### 4. 断点续跑能力

```python
def run_full(config):
    """如果某个阶段的产出已经存在，跳过它。"""
    
    stage_a_done = (config.artifact_root / "stage_a_best.pt").exists()
    stage_b_done = (config.artifact_root / "memory_bank.npz").exists()
    stage_c_done = (config.artifact_root / "stage_c.pt").exists()
    
    if not stage_a_done:
        run_stage_a(config)
    else:
        print("  [Skip] Stage A checkpoint found, skipping.")
    
    if not stage_b_done:
        run_stage_b(config)
    else:
        print("  [Skip] Memory bank found, skipping.")
    
    if not stage_c_done:
        run_stage_c(config)
    else:
        print("  [Skip] Scorer checkpoint found, skipping.")
    
    metrics = run_test(config)
    return metrics
```

**这个功能非常实用：Stage A 跑了 2 小时，Stage B 挂了 → 修完 bug 后重跑 `--stage full`，自动跳过 Stage A，直接从 Stage B 开始。**

但需要加一个 `--force` 参数来强制重跑所有阶段：

```bash
# 自动跳过已完成阶段
bash scripts/train_msl.sh full msl_exp1

# 强制从头跑（忽略已有 checkpoint）
FORCE=1 bash scripts/train_msl.sh full msl_exp1
```

---

## 自检清单

在正式提交到服务器跑之前，在本地用小数据验证这些：

```bash
# 1. full 模式能跑通
python run.py --stage full --dataset MSL --stage_a_epochs 3 --stage_c_epochs 2

# 2. 分步模式能跑通
python run.py --stage stage_a --dataset MSL --stage_a_epochs 3
python run.py --stage stage_b --dataset MSL
python run.py --stage stage_c --dataset MSL --stage_c_epochs 2
python run.py --stage test --dataset MSL

# 3. Stage A 中断后能从 B 续跑
python run.py --stage stage_a --dataset MSL --stage_a_epochs 3
# 假装 Stage B 挂了，修完后：
python run.py --stage full --dataset MSL  # 应该跳过 A，直接跑 B

# 4. Stage A 失败时后续不继续
python run.py --stage full --dataset NONEXIST  # 应该在 A 就报错停止

# 5. 结果文件正确生成
ls artifacts/msl_exp1/
# 应该看到：stage_a_best.pt, memory_bank.npz, stage_c.pt, results.json
```

---

## 一句话总结

> **三阶段自动运行的方案没问题。需要确认的核心是：阶段间 checkpoint 存在性检查、model.eval()/requires_grad 的正确切换、失败时及时终止。建议加上断点续跑能力和每阶段结束后的 sanity check。这些都是小改动，但能在长时间训练中帮你避免"跑了 3 小时发现中间某步出错，结果全废"的情况。**