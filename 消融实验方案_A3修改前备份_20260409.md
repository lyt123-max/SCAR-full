# 消融实验方案

## 本轮落地范围

这次先落实 A2 四组检索条件设计消融，并保持“每个消融单独一个文件，不把所有消融混在一个代码文件里”。

本轮已经对应到以下独立脚本：

- `scripts/ablation/run_a2_global_retrieval.sh`
- `scripts/ablation/run_a2_state_only_retrieval.sh`
- `scripts/ablation/run_a2_context_only_retrieval.sh`
- `scripts/ablation/run_a2_dual_condition_retrieval.sh`
- `scripts/ablation/run_a2_matrix.sh`

兼容入口仍保留：

- `scripts/ablation/run_dataset_ablation.sh`

它现在只做分发，不再承担 A2 四组本身的配置逻辑。

---

## A. 核心消融实验总目标

这一组实验不是为了证明“模块越多越好”，而是为了回答 3 个核心问题：

### Q1. 状态 + 局部解耦表征是否必要？

对应 A1。

### Q2. 检索参考集到底该如何定义？

对应 A2。

### Q3. 多源证据校准和多尺度建模是否真的有贡献？

对应 A3。

---

## A0. Full 模型定义

所有消融都相对同一个 Full 版本进行。

### Full 包含

- STSD 分解：`X -> S, R`
- 状态编码器：`S -> g_w`
- 状态条件化通道门控
- 多尺度 patch 编码
- 原始旁路补偿
- 局部上下文键 `c`
- 双条件检索
- Level 1：state-based coarse retrieval
- Level 2：context-based reranking
- completion score
- memory distance
- state novelty
- CDF-PIT calibration
- 默认融合：`cdf_max`
- 默认尺度：双尺度

### Full 作为对照基线时固定的内容

除非某个消融本身就在研究对应因素，否则以下设置尽量不变：

- encoder 宽度
- window 长度
- batch size
- optimizer / lr
- epoch
- memory bank 压缩率
- top-K
- candidate M
- 阈值规则
- 随机种子组

---

## A1. 表征主干消融

A1 只回答一个问题：

> 收益是否来自状态感知的表征主干，而不是单纯来自检索或后处理？

### A1.1 w/o decomposition

- 独立脚本：`scripts/ablation/run_a1_wo_decomposition.sh`
- 标题：`A1: w/o decomposition`

### A1.2 w/o state-aware representation

- 独立脚本：`scripts/ablation/run_a1_wo_state_aware_representation.sh`
- 标题：`A1: w/o state-aware representation`

---

## A2. 检索条件设计消融

A2 回答的问题是：

> 你的“局部正常参考”到底该怎么构造？  
> 全局、只按状态、只按上下文，还是双条件？

这一组保留 4 个变体，不再使用会造成歧义的旧命名。

### A2.1 global retrieval

独立脚本：`scripts/ablation/run_a2_global_retrieval.sh`  
文件标题：`A2: global retrieval`

#### 目的

给出最弱但必要的下界。

#### 定义

- 不做 state coarse filtering
- 不做 context reranking
- 直接在整个 memory bank 中基于 patch 表征做近邻检索

#### 当前代码实现

- `use_two_level_retrieval=0`
- `use_context_key_retrieval=0`

#### 实验含义

- query 仍然使用 Full 的编码器产生
- 但检索阶段不再使用 `g_w`
- 也不再使用 context key `c`

---

### A2.2 state-only retrieval

独立脚本：`scripts/ablation/run_a2_state_only_retrieval.sh`  
文件标题：`A2: state-only retrieval`

#### 目的

验证只按工况筛选是否仍然不够。

#### 定义

- Level 1：用 `g_w` 做 state-based coarse retrieval
- 不做 context reranking
- 在状态候选池内直接基于 patch 表征 `z` 做检索

#### 当前代码实现

- `use_two_level_retrieval=1`
- `use_context_key_retrieval=0`

#### 实验含义

- 仍保留 state coarse retrieval
- 但 Level 2 不再用 context key 排序
- 对应“同工况，但不要求局部语境也相近”

---

### A2.3 context-only retrieval

独立脚本：`scripts/ablation/run_a2_context_only_retrieval.sh`  
文件标题：`A2: context-only retrieval`

#### 目的

验证只按局部上下文匹配是否会跨工况错配。

#### 定义

- 不做 state coarse retrieval
- 直接在整个 memory bank 中用 context key `c` 检索

#### 当前代码实现

- `use_two_level_retrieval=0`
- `use_context_key_retrieval=1`

#### 实验含义

- 检索 key 是 `c`
- 候选集是全局 memory bank
- 对应“局部语境像，但不要求工况一致”

---

### A2.4 dual-condition retrieval

独立脚本：`scripts/ablation/run_a2_dual_condition_retrieval.sh`  
文件标题：`A2: dual-condition retrieval`

#### 目的

给出 A2 组的最终目标方法行。

#### 定义

- Level 1：state-based coarse retrieval
- Level 2：context-based reranking
- 最终在局部正常候选邻域中计算 memory distance

#### 当前代码实现

- `use_two_level_retrieval=1`
- `use_context_key_retrieval=1`

#### 实验含义

- 这是 A2 组里的 Full 检索设计
- 单独给它独立脚本，是为了让 A2 四组在文件组织和实验命名上完全对齐

---

## A2 组目标结论

希望通过 A2 整组实验说明：

$$
\text{global} < \text{state-only},\ \text{context-only} < \text{dual-condition}
$$

即：

- 状态条件和上下文条件都重要
- 但单条件都不够
- 双条件最好

---

## A2 建议汇报指标

除了主任务指标，建议增加 3 个检索质量指标。

### 1. Retrieval purity

定义：query 的 top-K 邻居中，与 query 属于同状态 / 同阶段的比例。

如果没有人工工况标签，可以用 proxy：

- pseudo-state cluster 一致率
- 同运行段一致率
- 同子系统一致率

### 2. Neighbor compactness

定义：top-K 邻居之间的平均 pairwise distance。

### 3. Query-neighbor comparability

定义：query 和 top-K 邻居之间的平均距离，可分别从两种空间计算：

- state space
- context space

### 主任务指标

- F1
- AUPRC
- event/range-F1

---

## A2 推荐表格

| Retrieval variant | SWaT | WADI | SMD | MSL | SMAP | Avg |
|---|---:|---:|---:|---:|---:|---:|
| Global |  |  |  |  |  |  |
| State-only |  |  |  |  |  |  |
| Context-only |  |  |  |  |  |  |
| Dual-condition |  |  |  |  |  |  |

这张表回答：局部正常邻域该怎么定义，双条件是否必要。

---

## A2 推荐定性图

选 1 个 query patch，展示 4 种检索下的 top-3 邻居波形：

- global
- state-only
- context-only
- dual-condition

这张图应该能直观展示：

- global 的邻居不够可比
- state-only 只保证工况近，不保证语境近
- context-only 只保证语境近，可能跨工况
- dual-condition 最合理

---

## A3. 其他关键结构消融

A3 仍保留两个问题：

1. 统计校准是否必要  
2. 多尺度是否必要

这一部分本轮暂不继续拆文件，等 A2 定型后再往下走。

---

## A 组统一实验设置建议

### 1. 数据集

A 部分建议至少跑：

- SWaT
- WADI
- SMD

如果资源允许再扩到：

- MSL
- SMAP

### 2. 随机种子

建议每个变体跑 3 个 seed，汇报 `mean ± std`。

### 3. 阈值协议

必须全变体一致。

- 如果有验证集：验证集选阈值
- 如果没有：训练正常分数采用同一 quantile rule

### 4. 参数一致性

除非是实验变量本身，以下参数全部固定：

- window size
- patch size 设置
- optimizer / lr
- batch size
- epoch
- memory top-K
- candidate M
- coreset ratio

---

## 运行方式

直接跑某一个 A2 消融：

```bash
bash scripts/ablation/run_a2_global_retrieval.sh PSM
bash scripts/ablation/run_a2_state_only_retrieval.sh PSM
bash scripts/ablation/run_a2_context_only_retrieval.sh PSM
bash scripts/ablation/run_a2_dual_condition_retrieval.sh PSM
```

跑整组 A2：

```bash
bash scripts/ablation/run_a2_matrix.sh
```

如果仍想沿用旧的统一入口，也可以：

```bash
bash scripts/ablation/run_psm.sh global_retrieval
bash scripts/ablation/run_psm.sh state_only_retrieval
bash scripts/ablation/run_psm.sh context_only_retrieval
bash scripts/ablation/run_psm.sh dual_condition_retrieval
```

---

## 待拍板事项

当前我先按代码现状保留了一个待拍板点，没有擅自扩展：

1. 文档里推荐数据集含 `WADI`，但当前脚本仍只支持 `MSL / SMAP / PSM / SWAT / SMD`，还没有 `WADI` 数据入口。
