# CoReM-AD 规范化消融实验与机制验证方案

## 一、最终采用的论文实验结构

本文最终采用如下实验组织方式：

- 点级主消融只使用 `SMD` 与 `SWaT`
- `TEP` 单独作为序列级机制验证节
- A1 中 `wo_state_aware_representation` 的论文展示名统一解释为 `w/o state-guided modulation`
- 主报告分数固定使用 `cdf_mean`
- A2 主表保留四个版本：
  - `global_retrieval`
  - `state_only_retrieval`
  - `context_only_retrieval`
  - `dual_condition_retrieval`
- 不做证据源消融
- 保留稳定性分析表
- A3 融合策略表只比较固定融合家族

## 二、点级主消融

### 1. 数据集

点级主消融正文仅使用：

- `SMD`
- `SWaT`

其余点级数据集如 `MSL`、`SMAP`、`PSM` 作为附录或补充结果保留，不进入正文主表。

### 2. 主报告分数

点级主消融统一使用固定分数：

- `cdf_mean`

不再按数据集切换最优子分数，也不使用事后 oracle 选择作为正式主结果。

### 3. A1-A4 主表结构

正文点级部分建议按四张表组织：

1. A1 表征主干消融
   - `full`
   - `wo_decomposition`
   - `wo_state_aware_representation`

2. A2 检索机制消融
   - `global_retrieval`
   - `state_only_retrieval`
   - `context_only_retrieval`
   - `dual_condition_retrieval`

3. A3 融合策略表
   - `raw_max`
   - `zscore_mean`
   - `cdf_max`
   - `cdf_mean`
   - `cdf_softmax`

4. A4 时间尺度消融
   - `single_scale_short`
   - `single_scale_long`
   - `multi_scale`

### 4. A1 命名说明

虽然脚本内部仍使用键名 `wo_state_aware_representation`，但论文中必须明确说明：

> 该消融关闭的是状态引导调制，而不是完全移除状态分支，因此更准确的展示名应为 `w/o state-guided modulation`。

## 三、稳定性分析表

由于不同数据集上最优子分数并不一致，因此论文不再试图证明“融合一定优于所有子分数”。
稳定性分析表的目标改为验证：

- 固定融合是否提供更稳健的默认决策
- 固定融合是否具有更低 regret
- 在不依赖测试标签事后选择的前提下，是否更适合跨数据集部署

建议比较对象：

- `Oracle Best Subscore`
- `Best Fixed Subscore`
- `Fixed Fusion (cdf_mean)`

建议报告指标：

- `Average Rank`
- `Mean Regret`
- `Cross-dataset Std`
- 平均 `PR-AUC`
- 平均 `ROC-AUC`

## 四、TEP 独立序列级机制验证

`TEP` 不进入点级主消融大表，而是单独作为序列级机制验证节。

### 1. TEP 主指标

`TEP` 正文主指标收敛为：

- sequence-level `AUROC`
- sequence-level `AUPRC`
- sequence-level `F1`

### 2. 保留的机制指标

保留以下机制指标用于解释模型行为：

- `SMC@K`
- `SFR`
- `Mode-FPR-Std`
- `SMR@K`
- `delta_mem_mode`
- `EE95`
- `Tail@0.99_error`
- `CG_mean`
- `%_multi_best`

### 3. 保留的机制图

保留以下关键图：

- `state_embedding.png`
- `normal_final_violin_by_mode.png`
- `retrieval_mode_confusion_heatmap.png`
- `exceedance_plot.png`
- `fault_file_gain_heatmap.png`

## 五、当前代码实现口径

当前仓库已经统一为以下代码口径：

- `cdf_mean` 为默认主报告分数
- 点级论文主表默认整理为 `SMD` 与 `SWaT`
- `TEP` 由独立脚本后处理生成机制指标和机制图
- 点级主表与 A3 融合策略表默认指标为 `PR-AUC`、`ROC-AUC`、`F1`
- 稳定性分析表默认基于 `PR-AUC`
- 论文专用汇总脚本会统一导出：
  - 点级主消融表
  - A3 融合策略表
  - 稳定性分析表
  - TEP 序列级结果与机制图索引

## 六、执行方式

### 1. 跑消融

按原有 ablation 脚本执行实验，例如：

```bash
bash scripts/ablation/run_ablation_matrix.sh
```

或者按组执行：

```bash
bash scripts/ablation/run_a1_matrix.sh
bash scripts/ablation/run_a2_matrix.sh
bash scripts/ablation/run_a3_matrix.sh
```

### 2. 生成论文汇总表

实验完成后，执行：

```bash
python scripts/ablation/generate_paper_tables.py
```

默认会生成：

- `./artifacts/paper_tables/pointwise_main_ablation.{csv,json,md}`
- `./artifacts/paper_tables/a3_fusion_family.{csv,json,md}`
- `./artifacts/paper_tables/stability_analysis.{csv,json,md}`
- `./artifacts/paper_tables/tep_sequence_results.{csv,json,md}`

## 七、当前明确不做的内容

为避免主线发散，当前版本明确不做：

- 证据源消融
- A5 memory cleaning / coreset 独立实验
- 新融合器结构升级

这些内容如后续需要，可作为附录或后续扩展工作再补。
