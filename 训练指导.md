## 我的提问：

You said

请你原封不动的输出：# CoReM-AD 三阶段训练详解

  

\---

  

\## 一、为什么不是端到端

  

先说清楚为什么\*\*不能\*\*端到端：

  

\`\`\`

假设你想端到端训练，一个 batch 的前向传播需要：

  

  x → STSD → S, R

  → StateEncoder → g\_w

  → PatchEncoder → z, c

  → MemoryBank.retrieve(g\_w, c) → s\_mem    ← 问题在这里

  → MaskedCompletion → s\_comp

  → Scorer(s\_comp, s\_mem, a\_w, rho) → score

  → Loss(score, label)

  

问题：

  MemoryBank 里存的是什么？

  → 训练集所有窗口的 (g\_w, z, c) 的快照

  但训练过程中 encoder 参数每步都在变

  → 上一个 epoch 存进 memory 的 embedding 和当前 encoder 输出的 embedding 不在同一空间

  → 检索距离失去意义

  → 梯度信号是错的

  

这就是为什么 PatchCore、SPADE 等所有基于 memory bank 的方法

都不是端到端训练，而是"先学表征，再建库，再校准"。

\`\`\`

  

\*\*核心矛盾：encoder 在变，但 memory 需要稳定。两者不能同时训练。\*\*

  

\---

  

\## 二、三阶段全景

  

\`\`\`

阶段 A                    阶段 B                     阶段 C

(学表征)                  (建记忆库)                  (学融合)

┌─────────────┐          ┌─────────────┐           ┌─────────────┐

│ 训练:        │          │ 不训练:      │           │ 训练:        │

│  STSD        │          │  遍历训练集   │           │  只有 Scorer │

│  StateEncoder│    ──→   │  提取所有     │    ──→   │  (5个参数)   │

│  PatchEncoder│          │  g\_w, z, c   │           │             │

│  Completion  │          │  建 Faiss 索引│           │ 冻结:       │

│  Prediction  │          │  Self-clean   │           │  其他全部    │

└─────────────┘          └─────────────┘           └─────────────┘

  ~100 epochs               跑一遍                    ~30 epochs

  ~1-3 小时                 ~5-10 分钟                 ~10-20 分钟

  目标:                    目标:                     目标:

  学到好的表征空间          在冻结的表征空间中          学习如何加权

  正常模式结构清晰          建立正常参考库             不同来源的证据

\`\`\`

  

\---

  

\## 三、每个阶段的详细说明

  

\### 阶段 A：自监督表征学习

  

\*\*做什么\*\*：

  

\`\`\`python

\# 训练这些模块的参数：

trainable = \[

    STSD\_Decomposer,      # 学会分解 S 和 R

    StateEncoder,          # 学会把 S 编码为状态向量 g\_w

    SharedConvTrunk,       # 学会提取 patch 基础特征

    ScaleHead\_1, ScaleHead\_2,  # 学会投影到表征空间

    BypassEncoder\_1, BypassEncoder\_2,  # 旁路编码器

    ChannelModulation,     # 学会通道调制

    MaskedCompletionHead\_1, MaskedCompletionHead\_2,  # 学会补全

    NextPatchPredHead\_1, NextPatchPredHead\_2,        # 学会预测

\]

  

\# 损失函数：

L\_A = L\_mask + 0.5 \* L\_pred + 0.01 \* L\_smooth

\`\`\`

  

\*\*用什么数据\*\*：训练集（切掉验证部分后的前 85%），全部正常，无标签。

  

\*\*怎么判断"做到什么程度"\*\*：

  

\`\`\`

主要看验证集上的 L\_mask（masked completion loss）：

  

Epoch   Train\_Loss   Val\_Loss    判断

  1      0.850        0.830      还在学

  10     0.420        0.415      还在学

  30     0.210        0.205      还在学

  50     0.145        0.142      接近收敛

  60     0.132        0.131      基本收敛

  65     0.128        0.130      val 开始不降了

  70     0.125        0.132      val 开始上升 ← early stopping 触发

  75     0.121        0.138      过拟合

  

Early stopping 规则：

  val\_loss 连续 15 个 epoch 没有新低 → 停止

  回滚到 val\_loss 最低的那个 epoch 的 checkpoint

\`\`\`

  

\*\*不应该看什么\*\*：

  

\`\`\`

❌ 不看 F1（这个阶段没有异常分数，也没有标签）

❌ 不看 train loss 是否降到 0（降到 0 说明过拟合了）

❌ 不看 L\_pred 是否很低（它是辅助任务，低不低不重要）

❌ 不看 L\_smooth（它是正则项，只要 S 看起来平滑就行）

\`\`\`

  

\*\*阶段 A 结束的标志\*\*：

  

\`\`\`

✅ val\_loss 收敛（early stopping 触发，或跑满 100 epochs）

✅ 保存了最优 checkpoint（含 model\_state + normalizer）

  

此时你应该手动检查一下：

  1. 用几个训练窗口可视化 STSD 分解：S 是否平滑？R 是否捕获了快变部分？

  2. L\_mask 的绝对值是否合理（通常在 0.05-0.3 之间，取决于数据）

  3. 没有 NaN / Inf

\`\`\`

  

\---

  

\### 阶段 B：离线建索引

  

\*\*做什么\*\*：

  

\`\`\`python

\# 不训练任何参数

\# 用阶段 A 学好的 encoder 提取所有训练集的表征

  

model.eval()

torch.no\_grad()

  

for every training window x:

    S, R = STSD(x)

    g\_w = StateEncoder(S)

    z\_list, c\_list = PatchEncoder(R, x, g\_w)

    comp\_scores = deterministic\_mask\_completion(x)  # 用于 self-cleaning

    store(g\_w, z\_list, c\_list, comp\_scores, window\_id)

  

\# 然后：

\# 1. Self-cleaning: 去掉 completion score 最高的 2% patch

\# 2. Coreset 压缩（如果 memory 太大）

\# 3. 建 Faiss 索引

\# 4. 保存 memory bank 到磁盘

\`\`\`

  

\*\*用什么数据\*\*：训练集全部（包含验证部分也可以——因为这里不训练任何参数，不存在泄漏问题）。

  

\*\*关于是否把验证部分也放进 memory\*\*：

  

\`\`\`

两种做法都可以：

  

做法 A：只用 train\_split（不含 val\_split）建 memory

  优点：最严格，验证集完全独立

  缺点：少了 15% 的正常参考数据

  

做法 B：用全部原始训练数据建 memory（含 val\_split）

  优点：memory 覆盖更全

  理由：阶段 B 不训练参数，只是存储表征，不存在"信息泄漏"

        val\_split 已经完成了它的使命（阶段 A 的 early stopping）

推荐做法 B。理由是 memory 越丰富越好，而且 val 的使命已经结束了。

\`\`\`

  

\*\*怎么判断"做到什么程度"\*\*：

  

\`\`\`

阶段 B 没有"程度"，它是一次性操作：

  

  ✅ 所有训练窗口的 g\_w 提取完毕

  ✅ 所有训练 patch 的 z, c 提取完毕

  ✅ Self-cleaning 完成（去掉 top-2%）

  ✅ Faiss 索引构建完成

  ✅ Sanity check 通过（自检索 top-1 距离 ≈ 0）

  ✅ Memory bank 保存到磁盘

  

以上全部完成 → 阶段 B 结束

\`\`\`

  

\*\*阶段 B 结束后应该手动检查\*\*：

  

\`\`\`

1\. Memory 大小是否合理：

   print(f"Scale 1: {len(memory.patch\_z\[0\])} patches")

   print(f"Scale 2: {len(memory.patch\_z\[1\])} patches")

   例如 MSL 训练集 ~50K 窗口，每窗口 16+4=20 个 patch

   去掉 2% 后约 980K patch → 两个尺度合计

   如果显著偏离这个数量级，说明有 bug

  

2\. Faiss 自检索：

   随机取 10 个训练窗口的 g\_w

   检索 top-1 → 应该返回自己

   距离 → 应该 ≈ 0

  

3\. 随机取一个训练窗口，做完整检索：

   Level-1 取 top-50 窗口

   Level-2 取 top-20 patch

   kNN 距离 → 应该很小（因为是训练集自查训练集）

\`\`\`

  

\---

  

\### 阶段 C：训练融合器（可选）

  

\*\*做什么\*\*：

  

\`\`\`python

\# 冻结所有模块，只训练 Scorer

for param in model.parameters():

    param.requires\_grad = False  # 全部冻结

  

\# Scorer 只有 5 个参数

scorer = ReliabilityScorer(input\_dim=4)  # 4个输入特征

scorer.requires\_grad\_(True)

  

\# 对每个训练窗口：

for x in train\_loader:

    with torch.no\_grad():

        # 1. 正常 patch 的完整特征

        features\_normal = extract\_features(x)  # \[s\_comp, s\_mem, a\_w, rho\]

        # 2. 注入伪异常的 patch 的特征

        x\_corrupted = inject\_pseudo\_anomaly(x, ratio=0.15)

        features\_corrupt = extract\_features(x\_corrupted)

    # 3. 拼接，构造标签

    labels = \[0, 0, ..., 0, 1, 1, ..., 1\]

    # 4. 训练 scorer

    scores = scorer(features)

    loss = BCE(scores, labels)

    loss.backward()  # 只有 scorer 的 5 个参数有梯度

\`\`\`

  

\*\*用什么数据\*\*：训练集 + 伪异常。

  

\*\*怎么判断"做到什么程度"\*\*：

  

\`\`\`

阶段 C 非常轻量（5 个参数），收敛很快：

  

Epoch   BCE\_Loss    判断

  1      0.680      初始（接近随机猜测的 0.693）

  5      0.420      在学

  10     0.250      接近收敛

  15     0.180      基本收敛

  20     0.165      可以停了

  30     0.160      跑满也行

  

通常 20-30 个 epoch 就够了，不需要 early stopping。

5 个参数几乎不可能过拟合。

\`\`\`

  

\*\*阶段 C 是可选的\*\*：

  

\`\`\`

如果你发现：

  "直接用 s\_mem 作为最终分数" 的 F1

  vs

  "用 scorer 融合后的分数" 的 F1

  差距 < 1%

  

那就直接跳过阶段 C，省掉伪异常生成和 scorer 训练。

在 ablation 表格里加一行对比即可。

\`\`\`

  

\---

  

\## 四、三阶段之间的衔接

  

\`\`\`

                    阶段 A 结束

                        │

                        ▼

               保存 stage\_a\_best.pt

               (model\_state + normalizer)

                        │

                        ▼

                    阶段 B 开始

                        │

               加载 stage\_a\_best.pt

               model.eval(), no\_grad()

               遍历全部训练数据，提取表征

               建索引，self-cleaning

                        │

                        ▼

               保存 memory\_bank.pt

               (state\_vectors, patch\_z, patch\_c, 

                window\_maps, faiss\_index)

                        │

                        ▼

                    阶段 C 开始（可选）

                        │

               加载 stage\_a\_best.pt（冻结）

               加载 memory\_bank.pt

               对每个训练窗口提取 \[s\_comp, s\_mem, a\_w, rho\]

               注入伪异常，训练 scorer

                        │

                        ▼

               保存 stage\_c.pt

               (scorer\_state + standardizer)

                        │

                        ▼

                    测试 / 推理

                        │

               加载 stage\_a\_best.pt

               加载 memory\_bank.pt  

               加载 stage\_c.pt（如果有）

               对测试集逐窗口推理

\`\`\`

  

\---

  

\## 五、每个阶段保存什么、加载什么

  

\`\`\`python

\# ============================================================

\# 阶段 A 保存

\# ============================================================

torch.save({

    # 必须保存

    'model\_state': model.state\_dict(),

    'normalizer\_mean': normalizer.scaler.mean\_,

    'normalizer\_std': normalizer.scaler.scale\_,

    'normalizer\_var': normalizer.scaler.var\_,

    'config': config,

    # 建议保存（用于断点续训）

    'optimizer\_state': optimizer.state\_dict(),

    'scheduler\_state': scheduler.state\_dict(),

    'epoch': epoch,

    'best\_val\_loss': best\_val\_loss,

    'history': history,

}, 'checkpoints/stage\_a\_best.pt')

  

  

\# ============================================================

\# 阶段 B 保存

\# ============================================================

np.savez\_compressed('checkpoints/memory\_bank.npz',

    # 必须保存

    state\_vectors=state\_vectors,          # (N\_windows, d\_state)

    patch\_z\_scale0=patch\_z\_list\[0\],       # (N\_patches\_0, d\_z)

    patch\_z\_scale1=patch\_z\_list\[1\],       # (N\_patches\_1, d\_z)

    patch\_c\_scale0=patch\_c\_list\[0\],       # (N\_patches\_0, d\_z)

    patch\_c\_scale1=patch\_c\_list\[1\],       # (N\_patches\_1, d\_z)

    window\_map\_scale0=window\_maps\[0\],     # (N\_patches\_0,)

    window\_map\_scale1=window\_maps\[1\],     # (N\_patches\_1,)

    # 建议保存（用于调试）

    completion\_scores\_scale0=comp\_scores\[0\],

    completion\_scores\_scale1=comp\_scores\[1\],

)

\# Faiss 索引单独保存

faiss.write\_index(state\_index, 'checkpoints/faiss\_state.index')

  

  

\# ============================================================

\# 阶段 C 保存

\# ============================================================

torch.save({

    'scorer\_state': scorer.state\_dict(),

    # feat\_mean/feat\_std 已经是 buffer，包含在 state\_dict 里了

}, 'checkpoints/stage\_c.pt')

\`\`\`

  

\---

  

\## 六、完整训练脚本的调用顺序

  

\`\`\`python

def full\_pipeline(config, raw\_data):

    """三阶段完整流程。"""

    # ========================

    # 数据准备

    # ========================

    prepared = prepare\_data(raw\_data, config)

    loaders = build\_dataloaders(prepared, config)

    # ========================

    # 阶段 A：学表征

    # ========================

    print("=" \* 60)

    print("Stage A: Self-supervised representation learning")

    print("=" \* 60)

    model = CoReMAD(config).to(config.device)

    trainer = StageATrainer(model, loaders, config)

    trainer.train()  # 内部有 early stopping

    # → 保存 stage\_a\_best.pt

    # ========================

    # 阶段 B：建记忆库

    # ========================

    print("=" \* 60)

    print("Stage B: Building memory bank")

    print("=" \* 60)

    model.load\_state\_dict(torch.load('stage\_a\_best.pt')\['model\_state'\])

    model.eval()

    memory\_builder = MemoryBuilder(model, config)

    # 注意：这里用全部原始训练数据（含 val 部分）建 memory

    full\_train\_norm = prepared\['normalizer'\].transform(

        np.nan\_to\_num(raw\_data\['train'\])

    )

    memory\_bank = memory\_builder.build(full\_train\_norm)

    # → 保存 memory\_bank.npz + faiss\_state.index

    # ========================

    # 阶段 C：训练融合器（可选）

    # ========================

    print("=" \* 60)

    print("Stage C: Training scorer (optional)")

    print("=" \* 60)

    scorer = ReliabilityScorer(input\_dim=4).to(config.device)

    stage\_c\_trainer = StageCTrainer(model, memory\_bank, scorer, loaders, config)

    stage\_c\_trainer.train()

    # → 保存 stage\_c.pt

    # ========================

    # 测试

    # ========================

    print("=" \* 60)

    print("Testing")

    print("=" \* 60)

    engine = InferenceEngine(model, memory\_bank, scorer, config)

    scores = engine.run(loaders\['test'\])

    # 评估

    metrics = evaluate(scores, prepared\['test\_labels'\])

    print(f"Best F1: {metrics\['best\_f1'\]:.4f}")

    print(f"AUC-ROC: {metrics\['auc\_roc'\]:.4f}")

    return metrics

\`\`\`

  

\---

  

\## 七、常见疑问

  

\### "三阶段是不是太麻烦了？"

  

\`\`\`

和你对比的 baseline 们：

  

AnomalyTransformer:  1 阶段（端到端）   ← 但它是纯重构，没有 memory

PatchCore (CV):      2 阶段（预训练 + 建库）← 和你一样不能端到端

DCdetector:          1 阶段             ← 纯 attention，没有 memory

THOC:                1 阶段             ← 但它的 memory 是在线更新的（容易不稳定）

H-PAD:               1 阶段             ← 纯重构 + 原型

  

你的方法有 memory bank，所以至少需要 2 阶段（A + B）。

阶段 C 是可选的，如果不需要可以跳过 → 实际上也可以是 2 阶段。

\`\`\`

  

\### "能不能把 A 和 B 合并？"

  

\`\`\`

不能。原因：

  

阶段 A 中 encoder 参数每步都在变

→ 如果边训边建库，memory 里的旧 embedding 和新 encoder 不匹配

→ 检索距离没有意义

  

必须等 encoder 完全定型（阶段 A 结束），

然后用定型的 encoder 一次性扫全库（阶段 B）。

\`\`\`

  

\### "能不能把 B 和 C 合并？"

  

\`\`\`

技术上可以，实际上不建议：

  

B 是纯推理（no\_grad），C 是训练（有梯度）

合并后代码逻辑会变混乱

而且 B 只跑一次（5分钟），分开更清晰

  

分开的好处：

  - B 跑完可以单独检查 memory 质量

  - C 可以跳过（如果 scorer 没有增益）

  - 调试时可以只重跑 C 而不重跑 B

\`\`\`

  

\### "阶段 A 和阶段 C 的学习率一样吗？"

  

\`\`\`

不一样：

  

阶段 A：lr = 1e-3，用 CosineAnnealing 衰减

  → 大模型（几百K参数），需要较大学习率 + 充分训练

  

阶段 C：lr = 1e-3 甚至更大都行

  → 只有 5 个参数，怎么设都能收敛

  → 不需要 scheduler

\`\`\`

  

\---

  

\## 八、一句话总结

  

\> \*\*三阶段不是设计缺陷，而是 memory-based 方法的必然结构。阶段 A 学表征（看 val\_loss 收敛 → early stopping），阶段 B 建记忆库（一次性操作，跑完检查自检索），阶段 C 学融合（可选，20 epoch 收敛）。阶段之间通过 checkpoint 文件衔接，每个阶段保存什么、加载什么、训练什么、冻结什么都必须严格分清。\*\*

---

## Gemini 回答：

Gemini said
-----------

CoReM-AD 三阶段训练详解
================

* * *

一、为什么不是端到端
----------

先说清楚为什么**不能**端到端：

假设你想端到端训练，一个 batch 的前向传播需要：
    
      x → STSD → S, R
      → StateEncoder → g_w
      → PatchEncoder → z, c
      → MemoryBank.retrieve(g_w, c) → s_mem    ← 问题在这里
      → MaskedCompletion → s_comp
      → Scorer(s_comp, s_mem, a_w, rho) → score
      → Loss(score, label)
    
    问题：
      MemoryBank 里存的是什么？
      → 训练集所有窗口的 (g_w, z, c) 的快照
      
      但训练过程中 encoder 参数每步都在变
      → 上一个 epoch 存进 memory 的 embedding 和当前 encoder 输出的 embedding 不在同一空间
      → 检索距离失去意义
      → 梯度信号是错时
    
    这就是为什么 PatchCore、SPADE 等所有基于 memory bank 的方法
    都不是端到端训练，而是"先学表征，再建库，再校准"。

**核心矛盾：encoder 在变，但 memory 需要稳定。两者不能同时训练。**

* * *

二、三阶段全景
-------

阶段 A                     阶段 B                      阶段 C
    (学表征)                   (建记忆库)                   (学融合)
                                                           
    ┌─────────────┐          ┌─────────────┐           ┌─────────────┐
    │ 训练:        │          │ 不训练:      │           │ 训练:        │
    │  STSD        │          │  遍历训练集   │           │  只有 Scorer │
    │  StateEncoder│   ──→    │  提取所有     │    ──→    │  (5个参数)   │
    │  PatchEncoder│          │  g_w, z, c   │           │             │
    │  Completion  │          │  建 Faiss 索引│           │ 冻结:        │
    │  Prediction  │          │  Self-clean  │           │  其他全部    │
    └─────────────┘          └─────────────┘           └─────────────┘
                                                           
      ~100 epochs                跑一遍                    ~30 epochs
      ~1-3 小时                  ~5-10 分钟                 ~10-20 分钟
      
      目标:                     目标:                      目标:
      学到好的表征空间           在冻结的表征空间中          学习如何加权
      正常模式结构清晰           建立正常参考库              不同来源的证据

* * *

三、每个阶段的详细说明
-----------

### 阶段 A：自监督表征学习

**做什么**：

Python

    # 训练这些模块的参数：
    trainable = [
        STSD_Decomposer,      # 学会分解 S 和 R
        StateEncoder,          # 学会把 S 编码为状态向量 g_w
        SharedConvTrunk,       # 学会提取 patch 基础特征
        ScaleHead_1, ScaleHead_2,  # 学会投影到表征空间
        BypassEncoder_1, BypassEncoder_2,  # 旁路编码器
        ChannelModulation,     # 学会通道调制
        MaskedCompletionHead_1, MaskedCompletionHead_2,  # 学会补全
        NextPatchPredHead_1, NextPatchPredHead_2,        # 学会预测
    ]
    
    # 损失函数：
    L_A = L_mask + 0.5 * L_pred + 0.01 * L_smooth

**用什么数据**：训练集（切掉验证部分后的前 85%），全部正常，无标签。

**怎么判断"做到什么程度"**：

主要看验证集上的 L_mask（masked completion loss）：
    
    Epoch   Train_Loss   Val_Loss    判断
      1      0.850        0.830      还在学
      10     0.420        0.415      还在学
      30     0.210        0.205      还在学
      50     0.145        0.142      接近收敛
      60     0.132        0.131      基本收敛
      65     0.128        0.130      val 开始不降了
      70     0.125        0.132      val 开始上升 ← early stopping 触发
      75     0.121        0.138      过拟合
    
    Early stopping 规则：
      val_loss 连续 15 个 epoch 没有新低 → 停止
      回滚到 val_loss 最低的那个 epoch 的 checkpoint

**不应该看什么**：

❌ 不看 F1（这个阶段没有异常分数，也没有标签）
    ❌ 不看 train loss 是否降到 0（降到 0 说明过拟合了）
    ❌ 不看 L_pred 是否很低（它是辅助任务，低不低不重要）
    ❌ 不看 L_smooth（它是正则项，只要 S 看起来平滑就行）

**阶段 A 结束的标志**：

✅ val_loss 收敛（early stopping 触发，或跑满 100 epochs）
    ✅ 保存了最优 checkpoint（含 model_state + normalizer）
    
    此时你应该手动检查一下：
      1. 用几个训练窗口可视化 STSD 分解：S 是否平滑？R 是否捕获了快变部分？
      2. L_mask 的绝对值是否合理（通常在 0.05-0.3 之间，取决于数据）
      3. 没有 NaN / Inf

* * *

### 阶段 B：离线建索引

**做什么**：

Python

    # 不训练任何参数
    # 用阶段 A 学好的 encoder 提取所有训练集的表征
    
    model.eval()
    torch.no_grad()
    
    for every training window x:
        S, R = STSD(x)
        g_w = StateEncoder(S)
        z_list, c_list = PatchEncoder(R, x, g_w)
        comp_scores = deterministic_mask_completion(x)  # 用于 self-cleaning
        
        store(g_w, z_list, c_list, comp_scores, window_id)
    
    # 然后：
    # 1. Self-cleaning: 去掉 completion score 最高的 2% patch
    # 2. Coreset 压缩（如果 memory 太大）
    # 3. 建 Faiss 索引
    # 4. 保存 memory bank 到磁盘

**用什么数据**：训练集全部（包含验证部分也可以——因为这里不训练任何参数，不存在泄漏问题）。

**关于是否把验证部分也放进 memory**：

两种做法都可以：
    
    做法 A：只用 train_split（不含 val_split）建 memory
      优点：最严格，验证集完全独立
      缺点：少了 15% 的正常参考数据
    
    做法 B：用全部原始训练数据建 memory（含 val_split）
      优点：memory 覆盖更全
      理由：阶段 B 不训练参数，只是存储表征，不存在"信息泄漏"
            val_split 已经完成了它的使命（阶段 A 的 early stopping）
      
    推荐做法 B。理由是 memory 越丰富越好，而且 val 的使命已经结束了。

**怎么判断"做到什么程度"**：

阶段 B 没有"程度"，它是一次性操作：
    
      ✅ 所有训练窗口的 g_w 提取完毕
      ✅ 所有训练 patch 的 z, c 提取完毕
      ✅ Self-cleaning 完成（去掉 top-2%）
      ✅ Faiss 索引构建完成
      ✅ Sanity check 通过（自检索 top-1 距离 ≈ 0）
      ✅ Memory bank 保存到磁盘
    
    以上全部完成 → 阶段 B 结束

**阶段 B 结束后应该手动检查**：

1. Memory 大小是否合理：
       print(f"Scale 1: {len(memory.patch_z[0])} patches")
       print(f"Scale 2: {len(memory.patch_z[1])} patches")
       
       例如 MSL 训练集 ~50K 窗口，每窗口 16+4=20 个 patch
       去掉 2% 后约 980K patch → 两个尺度合计
       如果显著偏离这个数量级，说明有 bug
    
    2. Faiss 自检索：
       随机取 10 个训练窗口的 g_w
       检索 top-1 → 应该返回自己
       距离 → 应该 ≈ 0
    
    3. 随机取一个训练窗口，做完整检索：
       Level-1 取 top-50 窗口
       Level-2 取 top-20 patch
       kNN 距离 → 应该很小（因为是训练集自查训练集）

* * *

### 阶段 C：训练融合器（可选）

**做什么**：

Python

    # 冻结所有模块，只训练 Scorer
    for param in model.parameters():
        param.requires_grad = False  # 全部冻结
    
    # Scorer 只有 5 个参数
    scorer = ReliabilityScorer(input_dim=4)  # 4个输入特征
    scorer.requires_grad_(True)
    
    # 对每个训练窗口：
    for x in train_loader:
        with torch.no_grad():
            # 1. 正常 patch 的完整特征
            features_normal = extract_features(x)  # [s_comp, s_mem, a_w, rho]
            
            # 2. 注入伪异常的 patch 的特征
            x_corrupted = inject_pseudo_anomaly(x, ratio=0.15)
            features_corrupt = extract_features(x_corrupted)
        
        # 3. 拼接，构造标签
        labels = [0, 0, ..., 0, 1, 1, ..., 1]
        
        # 4. 训练 scorer
        scores = scorer(features)
        loss = BCE(scores, labels)
        loss.backward()  # 只有 scorer 的 5 个参数有梯度

**用什么数据**：训练集 + 伪异常。

**怎么判断"做到什么程度"**：

阶段 C 非常轻量（5 个参数），收敛很快：
    
    Epoch   BCE_Loss    判断
      1      0.680      初始（接近随机猜测的 0.693）
      5      0.420      在学
      10     0.250      接近收敛
      15     0.180      基本收敛
      20     0.165      可以停了
      30     0.160      跑满也行
    
    通常 20-30 个 epoch 就够了，不需要 early stopping。
    5 个参数几乎不可能过拟合。

**阶段 C 是可选的**：

如果你发现：
      "直接用 s_mem 作为最终分数" 的 F1
      vs
      "用 scorer 融合后的分数" 的 F1
      差距 < 1%
    
    那就直接跳过阶段 C，省掉伪异常生成和 scorer 训练。
    在 ablation 表格里加一行对比即可。

* * *

四、三阶段之间的衔接
----------

                    阶段 A 结束
                            │
                            ▼
                   保存 stage_a_best.pt
                   (model_state + normalizer)
                            │
                            ▼
                        阶段 B 开始
                            │
                            ▼
                   加载 stage_a_best.pt
                   model.eval(), no_grad()
                   遍历全部训练数据，提取表征
                   建索引，self-cleaning
                            │
                            ▼
                   保存 memory_bank.pt
                   (state_vectors, patch_z, patch_c, 
                    window_maps, faiss_index)
                            │
                            ▼
                        阶段 C 开始（可选）
                            │
                            ▼
                   加载 stage_a_best.pt（冻结）
                   加载 memory_bank.pt
                   对每个训练窗口提取 [s_comp, s_mem, a_w, rho]
                   注入伪异常，训练 scorer
                            │
                            ▼
                   保存 stage_c.pt
                   (scorer_state + standardizer)
                            │
                            ▼
                        测试 / 推理
                            │
                            ▼
                   加载 stage_a_best.pt
                   加载 memory_bank.pt  
                   加载 stage_c.pt（如果有）
                   对测试集逐窗口推理

* * *

五、每个阶段保存什么、加载什么
---------------

Python

    # ============================================================
    # 阶段 A 保存
    # ============================================================
    torch.save({
        # 必须保存
        'model_state': model.state_dict(),
        'normalizer_mean': normalizer.scaler.mean_,
        'normalizer_std': normalizer.scaler.scale_,
        'normalizer_var': normalizer.scaler.var_,
        'config': config,
        
        # 建议保存（用于断点续训）
        'optimizer_state': optimizer.state_dict(),
        'scheduler_state': scheduler.state_dict(),
        'epoch': epoch,
        'best_val_loss': best_val_loss,
        'history': history,
    }, 'checkpoints/stage_a_best.pt')
    
    
    # ============================================================
    # 阶段 B 保存
    # ============================================================
    np.savez_compressed('checkpoints/memory_bank.npz',
        # 必须保存
        state_vectors=state_vectors,          # (N_windows, d_state)
        patch_z_scale0=patch_z_list[0],       # (N_patches_0, d_z)
        patch_z_scale1=patch_z_list[1],       # (N_patches_1, d_z)
        patch_c_scale0=patch_c_list[0],       # (N_patches_0, d_z)
        patch_c_scale1=patch_c_list[1],       # (N_patches_1, d_z)
        window_map_scale0=window_maps[0],     # (N_patches_0,)
        window_map_scale1=window_maps[1],     # (N_patches_1,)
        
        # 建议保存（用于调试）
        completion_scores_scale0=comp_scores[0],
        completion_scores_scale1=comp_scores[1],
    )
    # Faiss 索引单独保存
    faiss.write_index(state_index, 'checkpoints/faiss_state.index')
    
    
    # ============================================================
    # 阶段 C 保存
    # ============================================================
    torch.save({
        'scorer_state': scorer.state_dict(),
        # feat_mean/feat_std 已经是 buffer，包含在 state_dict 里了
    }, 'checkpoints/stage_c.pt')

* * *

六、完整训练脚本的调用顺序
-------------

Python

    def full_pipeline(config, raw_data):
        """三阶段完整流程。"""
        
        # ========================
        # 数据准备
        # ========================
        prepared = prepare_data(raw_data, config)
        loaders = build_dataloaders(prepared, config)
        
        # ========================
        # 阶段 A：学表征
        # ========================
        print("=" * 60)
        print("Stage A: Self-supervised representation learning")
        print("=" * 60)
        
        model = CoReMAD(config).to(config.device)
        trainer = StageATrainer(model, loaders, config)
        trainer.train()  # 内部有 early stopping
        # → 保存 stage_a_best.pt
        
        # ========================
        # 阶段 B：建记忆库
        # ========================
        print("=" * 60)
        print("Stage B: Building memory bank")
        print("=" * 60)
        
        model.load_state_dict(torch.load('stage_a_best.pt')['model_state'])
        model.eval()
        
        memory_builder = MemoryBuilder(model, config)
        # 注意：这里用全部原始训练数据（含 val 部分）建 memory
        full_train_norm = prepared['normalizer'].transform(
            np.nan_to_num(raw_data['train'])
        )
        memory_bank = memory_builder.build(full_train_norm)
        # → 保存 memory_bank.npz + faiss_state.index
        
        # ========================
        # 阶段 C：训练融合器（可选）
        # ========================
        print("=" * 60)
        print("Stage C: Training scorer (optional)")
        print("=" * 60)
        
        scorer = ReliabilityScorer(input_dim=4).to(config.device)
        stage_c_trainer = StageCTrainer(model, memory_bank, scorer, loaders, config)
        stage_c_trainer.train()
        # → 保存 stage_c.pt
        
        # ========================
        # 测试
        # ========================
        print("=" * 60)
        print("Testing")
        print("=" * 60)
        
        engine = InferenceEngine(model, memory_bank, scorer, config)
        scores = engine.run(loaders['test'])
        
        # 评估
        metrics = evaluate(scores, prepared['test_labels'])
        print(f"Best F1: {metrics['best_f1']:.4f}")
        print(f"AUC-ROC: {metrics['auc_roc']:.4f}")
        
        return metrics

* * *

七、常见疑问
------

### "三阶段是不是太麻烦了？"

和你对比的 baseline 们：
    
    AnomalyTransformer:  1 阶段（端到端）   ← 但它是纯重构，没有 memory
    PatchCore (CV):      2 阶段（预训练 + 建库）← 和你一样不能端到端
    DCdetector:          1 阶段              ← 纯 attention，没有 memory
    THOC:                1 阶段              ← 但它的 memory 是在线更新的（容易不稳定）
    H-PAD:               1 阶段              ← 纯重构 + 原型
    
    你的方法有 memory bank，所以至少需要 2 阶段（A + B）。
    阶段 C 是可选的，如果不需要可以跳过 → 实际上也可以是 2 阶段。

### "能不能把 A 和 B 合并？"

不能。原因：
    
    阶段 A 中 encoder 参数每步都在变
    → 如果边训边建库，memory 里的旧 embedding 和新 encoder 不匹配
    → 检索距离没有意义
    
    必须等 encoder 完全定型（阶段 A 结束），
    然后用定型的 encoder 一次性扫全库（阶段 B）。

### "能不能把 B 和 C 合并？"

技术上可以，实际上不建议：
    
    B 是纯推理（no_grad），C 是训练（有梯度）
    合并后代码逻辑会变混乱
    而且 B 只跑一次（5分钟），分开更清晰
    
    分开的好处：
      - B 跑完可以单独检查 memory 质量
      - C 可以跳过（如果 scorer 没有增益）
      - 调试时可以只重跑 C 而不重跑 B

### "阶段 A 和阶段 C 的学习率一样吗？"

不一样：
    
    阶段 A：lr = 1e-3，用 CosineAnnealing 衰减
      → 大模型（几百K参数），需要较大学习率 + 充分训练
    
    阶段 C：lr = 1e-3 甚至更大都行
      → 只有 5 个参数，怎么设都能收敛
      → 不需要 scheduler

* * *

八、一句话总结
-------

> **三阶段不是设计缺陷，而是 memory-based 方法的必然结构。阶段 A 学表征（看 val\_loss 收敛 → early stopping），阶段 B 建记忆库（一次性操作，跑完检查自检索），阶段 C 学融合（可选，20 epoch 收敛）。阶段之间通过 checkpoint 文件衔接，每个阶段保存什么、加载什么、训练什么、冻结什么都必须严格分清。**

---

