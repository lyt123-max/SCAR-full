> From: https://gemini.google.com/u/1/app/1824eb9d7ca397ca?is_sa=1&is_sa=1&android-min-version=301356232&ios-min-version=322.0&campaign_id=bkws&utm_source=sem&utm_medium=paid-media&utm_campaign=bkws&pt=9008&mt=8&ct=p-growth-sem-bkws&gclsrc=aw.ds&gad_source=1&gad_campaignid=20108148196&gbraid=0AAAAApk5Bhmo4ZyZDH8_G8pqBMUeWxcae&gclid=Cj0KCQjw7cLOBhDmARIsAGsuA0mznv4fIOUpLoYmXaBTN2_ZG3n_bD3AzySepEe4GEncAQcz0Tb9G9YaAiRsEALw_wcB&pageId=none

# you asked

请你原封不动的输出：Let me re-read the original proposal carefully to make sure the rewrite is comprehensive and faithful to the best parts.





Now I have the complete picture. Let me also re-read the evaluation reply to make sure I incorporate all the valid points.

Now I have complete understanding of all three documents. Let me write the comprehensive refined proposal.

---

# CoReM-AD v2.0 完整框架方案

## Condition-aware Residual Memory for Anomaly Detection

---

# 一、核心思想

整篇论文只围绕一个命题展开：

> **异常不应相对全局正常模式来判断，而应相对"状态相似且局部上下文一致"的正常局部流形来判断。**

对应到工业场景：

- 同样的局部波动，在不同工况下意义不同
- 正常模式不是全局共享的几个中心，而是一个**随状态变化的局部流形族**
- 正常边界是**条件化的、局部的、可检索的**

因此，CoReM-AD 的任务不是学习"唯一正常模板"，也不是"把正常序列重构出来"，而是：

1. 判断当前窗口处于什么运行状态
2. 找到与该状态相近、且局部上下文也相似的正常片段
3. 在这些局部正常邻域里判断当前 patch 是否偏离正常流形

---

# 二、问题定义

给定多变量时间序列：

$$X = \{x_t\}_{t=1}^{T}, \quad x_t \in \mathbb{R}^{C}$$

其中 $T$ 为时间长度，$C$ 为变量数。训练阶段仅使用正常数据。测试阶段输出逐点异常分数 $s_t \in \mathbb{R}$。

---

# 三、贡献点

**贡献 1**：提出一种**状态-上下文双条件局部正常边界框架**，将异常判别从相对全局正常模式改写为相对"状态相似且上下文一致"的局部正常流形，从根本上解决全局 prototype 在多工况场景下边界模糊的问题。

**贡献 2**：提出一种**多尺度条件化 patch memory 机制**，在多个时间粒度上刻画局部正常模式，并通过动态尺度融合同时适应短时突变与长子序列异常。

**贡献 3**：提出一种**可信度感知的轻量证据融合策略**，基于状态新颖度与检索覆盖度自适应调整 memory 与 completion 证据的权重，从而减少"新但正常工况"带来的误判。

---

# 四、总体框架

整体数据流：

$$X \rightarrow (S, R) \rightarrow g_w,\ \{z_{i,\ell}\},\ \{c_{i,\ell}\} \rightarrow \text{dual-key retrieval} \rightarrow \text{multi-scale memory matching} + \text{masked completion} \rightarrow \text{reliability-aware fusion} \rightarrow s_t$$

其中：
- $S$：慢变状态分量
- $R = X - S$：快变残差分量
- $g_w$：窗口级状态向量
- $z_{i,\ell}$：第 $i$ 个 patch 在尺度 $\ell$ 下的表征
- $c_{i,\ell}$：第 $i$ 个 patch 在尺度 $\ell$ 下的局部上下文键

**整个方法由五个核心模块构成：**

| 模块 | 职责 |
|------|------|
| **模块 1：状态-残差分解** | 将原始信号拆分为慢变工况背景与快变局部残差 |
| **模块 2：多尺度条件化 patch 表征** | 在多个时间粒度上编码残差 patch，并利用状态信息做通道调制，同时构造局部上下文键 |
| **模块 3：自监督预训练任务** | Masked residual completion（主任务）+ Next-patch prediction（辅助任务），驱动表征学习 |
| **模块 4：状态-上下文双条件检索与多尺度匹配** | 先按状态粗筛、再按上下文精排，构造条件化局部正常邻域，计算多尺度 memory 距离 |
| **模块 5：可信度感知融合** | 基于状态新颖度和检索覆盖度，自适应加权 memory 证据和 completion 证据，输出最终异常分数 |

---

# 五、模块设计

---

## 5.1 模块 1：状态-残差分解

### 目标

将输入窗口拆分为慢变状态分量 $S$（反映工况背景）和快变残差分量 $R$（反映局部动态扰动）。

### 定义

$$X^{(w)} = S^{(w)} + R^{(w)}$$

### 实现：频域低通初始化 + 轻量时域修正

**步骤 1**：对输入做 FFT，用可学习的软低通掩码 $M(f)$ 提取慢变初值：

$$\hat{S}_0(f) = M(f) \cdot \hat{X}(f), \quad S_0 = \mathcal{F}^{-1}(\hat{S}_0(f))$$

**步骤 2**：用小型时域修正网络（1-2 层 1D-Conv + ReLU）精修：

$$S = S_0 + \Delta_\theta(X, S_0)$$

**步骤 3**：

$$R = X - S$$

### 设计原则

这一步**不追求完美分解**，而是为后续的状态编码和残差建模提供一个比原始信号更有用的视图。即使 $S$ 和 $R$ 的分离不完美，后续模块通过原始旁路机制（见 5.2 节）也能兜底。

### 正则

只保留一个轻量平滑约束：

$$\mathcal{L}_{smooth} = |\nabla_t^2 S|_1$$

不堆叠正交约束、频域泄漏约束或其他复杂正则。理由：STSD 已经被降格为"软增强模块"而非"硬前置过滤器"，过多约束反而会限制其自适应能力。

---

## 5.2 模块 2：多尺度条件化 Patch 表征

这个模块整合了三个子功能：多尺度 patch 编码、状态条件化通道调制、局部上下文键构造。

### 5.2.1 状态编码

慢变分量 $S$ 进入状态编码器：

$$g_w = E_s(S^{(w)}), \quad g_w \in \mathbb{R}^{d_s}$$

$E_s$ 采用轻量 1D-Conv + GlobalAvgPool + MLP 结构。

**关键设计**：状态编码器不做离散工况聚类，不引入工况数 $Q$，不引入 state anchor pull loss。它只负责一件事：

> **为当前窗口生成一个连续的状态表征，用于后续条件化检索。**

### 5.2.2 多尺度残差 Patch 切分与编码

对残差序列 $R^{(w)}$ 在两个尺度下切 patch：

$$\mathcal{P} = \{P_1, P_2\}$$

- $P_1$（细粒度）：捕获短时突变和点异常，如 patch size = 8
- $P_2$（粗粒度）：捕获长子序列异常和形态漂移，如 patch size = 32

> **设计说明**：经过前几轮讨论，确认两个尺度（一细一粗）即可覆盖绝大多数异常类型。第三个中间尺度的边际收益有限，但计算和存储开销会增加 50%。如果实验发现两尺度不够，可在 ablation 中加入第三尺度验证。

对每个尺度 $\ell$，切分得到：

$$R^{(w)} \rightarrow \{r_{i,\ell}\}_{i=1}^{N_\ell}, \quad X^{(w)} \rightarrow \{x_{i,\ell}\}_{i=1}^{N_\ell}$$

**残差主通道编码**：

$$e_{i,\ell}^{r} = E_\ell^r(r_{i,\ell}), \quad e_{i,\ell}^{r} \in \mathbb{R}^{d}$$

$E_\ell^r$ 采用共享 trunk + 尺度特定 head 的结构：底层 1D-Conv 层跨尺度共享参数（因为不同尺度的 patch 在底层共享"局部斜率、振幅"等基础特征），顶层 MLP head 按尺度分开。

**原始旁路编码**（信息瓶颈式兜底）：

$$e_{i,\ell}^{x} = E_\ell^x(x_{i,\ell}), \quad e_{i,\ell}^{x} \in \mathbb{R}^{d/4}$$

原始旁路维度严格限制为主通道的 $1/4$，确保残差仍然是主信息源，原始信号只提供补充。

**融合**：

$$u_{i,\ell} = e_{i,\ell}^{r} + \sigma(v_\ell) \odot W_\ell e_{i,\ell}^{x}$$

其中 $\sigma(v_\ell) \in (0,1)^d$ 是 sigmoid 门控向量（$v_\ell$ 可学习），保证旁路权重有界，不会绕过残差主通道。

**Patch 表征**：

$$z_{i,\ell} = H_\ell(u_{i,\ell}), \quad z_{i,\ell} \in \mathbb{R}^{d_z}$$

$H_\ell$ 为轻量 MLP 投影头。

### 5.2.3 状态条件化通道调制

根据窗口状态 $g_w$ 生成通道门控，对残差 patch 输入做条件化缩放：

$$a_w = \sigma(W_g g_w), \quad a_w \in \mathbb{R}^{C}$$

$$\tilde{r}_{i,\ell}^{(c)} = a_w^{(c)} \cdot r_{i,\ell}^{(c)}$$

这一步在 patch 进入 encoder 之前执行。它表达的物理含义是：不同工况下，不同通道的重要性和正常波动幅度不同。

> **实现说明**：通道调制不作为独立创新点，而是 patch encoder 内部的一个条件化机制。在论文中作为 5.2 节的一个子段落描述即可。

### 5.2.4 局部上下文键构造

对每个 patch，利用其时间邻域内的 patch 表征构造上下文键：

$$c_{i,\ell} = \frac{1}{2k+1} \sum_{j=i-k}^{i+k} u_{j,\ell}$$

其中 $c_{i,\ell} \in \mathbb{R}^{d_c}$，$k$ 为上下文半径（默认 $k=2$）。

> **设计说明**：上下文键采用简单的邻域平均池化而非复杂的 Transformer 或 GRU。原因：上下文键的目的只是"描述当前 patch 周围的局部环境"用于检索匹配，不需要太强的非线性建模。简单平均池化足以捕获"当前 patch 周围是平稳的还是有趋势的"这一关键信息，同时避免引入新的可训练参数和调参负担。

> **边界处理**：窗口边缘的 patch 用零填充或截断邻域。

---

## 5.3 模块 3：自监督预训练任务

### 5.3.1 Masked Residual Completion（主任务）

**训练阶段**：对每个尺度的 patch 序列随机 mask 一部分索引：

$$\Omega_\ell \subset \{1, \dots, N_\ell\}, \quad |\Omega_\ell| = \lfloor \rho \cdot N_\ell \rfloor$$

mask ratio $\rho$ 默认取 0.25。模型根据未被 mask 的上下文 patch 预测被 mask 的 patch：

$$\hat{r}_{i,\ell} = H_\ell^{mask}(u_{1:N_\ell,\ell}^{masked}), \quad i \in \Omega_\ell$$

$H_\ell^{mask}$ 采用轻量 Transformer（2 层，4 头）在 patch 序列上做上下文推理。

损失：

$$\mathcal{L}_{mask} = \sum_\ell \frac{1}{|\Omega_\ell|} \sum_{i \in \Omega_\ell} |\hat{r}_{i,\ell} - r_{i,\ell}|_1$$

**测试阶段**：采用确定性分组 mask 策略——

将每个尺度的 $N_\ell$ 个 patch 按 $\text{index} \mod K$ 分为 $K$ 组（$K=4$），依次 mask 每组并用剩余 patch 补全被 mask 的 patch。每个 patch 恰好被预测一次，无随机性。

测试时第 $i$ 个 patch 的 completion score：

$$s_{i,\ell}^{comp} = |\hat{r}_{i,\ell} - r_{i,\ell}|_1$$

> **设计说明**：$K=4$ 意味着每次 mask 25% 的 patch，推理需要 4 次前向传播。这与 mask ratio $\rho=0.25$ 一致，保证训练和测试的 mask 比例相同。

### 5.3.2 Next-Patch Prediction（辅助任务，仅用于训练）

在每个尺度上预测下一个 patch：

$$\bar{r}_{i+1,\ell} = H_\ell^{pred}(u_{i,\ell})$$

损失：

$$\mathcal{L}_{pred} = \sum_\ell \frac{1}{N_\ell - 1} \sum_{i=1}^{N_\ell - 1} |\bar{r}_{i+1,\ell} - r_{i+1,\ell}|_1$$

**Next-patch prediction 只参与训练，不进入测试阶段的异常分数。** 原因：prediction 误差在测试时存在归因方向模糊问题——当前 patch 异常会导致预测下一个 patch 出错，误差无法明确归因于哪个 patch。但 prediction 对训练时 encoder 学习正常时序演化规律仍然有价值，因此保留为训练辅助损失。

---

## 5.4 模块 4：状态-上下文双条件检索与多尺度匹配

这是 CoReM-AD 最核心的创新模块。

### 5.4.1 训练集索引库构建（阶段 B 离线完成）

用阶段 A 训练好的 encoder 对全部正常训练数据提取：

- **窗口状态向量库**：$\mathcal{G}_{train} = \{g_j^{tr}\}$
- **多尺度 patch 表征库**：$\mathcal{Z}_\ell^{train} = \{z_{j,n,\ell}^{tr}\}$
- **多尺度上下文键库**：$\mathcal{C}_\ell^{train} = \{c_{j,n,\ell}^{tr}\}$

**Memory 质量过滤**：利用阶段 A 训练好的 completion 模型计算每个训练 patch 的 completion score，去掉 score 最高的 top-$\epsilon$%（默认 $\epsilon=2\%$），只用剩余的高质量正常 patch 构建 memory。这是一个 self-cleaning 步骤，无需外部标签，可以过滤训练集中可能存在的标注噪声。

**Coreset 压缩**：对 memory 库做 k-center-greedy 或 diversity-aware coreset selection，将每个状态邻域内的 patch 数量压缩到可控范围，保持多样性的同时降低存储和检索开销。

**ANN 索引构建**：
- 一级索引：对 $\mathcal{G}_{train}$ 建 ANN（Faiss IVF 或 HNSW）
- 二级索引：对 $\mathcal{C}_\ell^{train}$ 建 ANN

### 5.4.2 双条件检索流程

**第一步：状态粗筛**

用测试窗口的状态向量 $g_w$ 在 $\mathcal{G}_{train}$ 中检索 top-$M$ 个状态最近邻的窗口：

$$\mathcal{W}(g_w) = \text{TopMNN}(g_w, \mathcal{G}_{train})$$

$M$ 默认取 50，含义是"从训练集中找到 50 个工况最相似的窗口"。

**第二步：上下文精排**

在粗筛得到的窗口集合内部，对当前 patch 的上下文键 $c_{i,\ell}$ 做精排检索，得到最终的局部正常邻域：

$$\mathcal{N}_{i,\ell} = \text{TopKNN}\big(c_{i,\ell},\ \{c_{j,n,\ell}^{tr} \mid j \in \mathcal{W}(g_w)\}\big)$$

$K$ 默认取 20，含义是"在工况相似的窗口中，找到 20 个局部上下文最接近的 patch"。

**两级检索的意义**：

- 第一级解决"同一工况下才可比"的问题
- 第二级解决"同一工况内局部环境也可能差异很大"的问题（如启停过渡 vs 稳态运行）
- 两级合在一起，检索到的是"状态相似且局部上下文相似的正常 patch"，这比任何单一条件的检索都更精准

### 5.4.3 多尺度条件化 Memory 距离

对当前 patch 表征 $z_{i,\ell}$，在其条件化局部邻域 $\mathcal{M}_{i,\ell} = \{z_{j,n,\ell}^{tr} \mid (j,n) \in \mathcal{N}_{i,\ell}\}$ 中计算 $k$-NN 距离（$k$ 默认取 5）：

$$s_{i,\ell}^{mem} = \frac{1}{k} \sum_{m \in \text{NN}_k(z_{i,\ell},\ \mathcal{M}_{i,\ell})} \|z_{i,\ell} - m\|_2^2$$

### 5.4.4 多尺度融合

不同尺度的 memory 距离用固定权重融合：

$$s_i^{mem} = \sum_\ell w_\ell \cdot s_{i,\ell}^{mem}$$

其中 $w_\ell = 1 / P_\ell$（patch size 的倒数），归一化后使用。含义是：细粒度尺度天然对短时异常更敏感，给予更高权重。

> **设计说明**：这里采用固定权重而非可学习的动态尺度门控。原因是：在前一版方案中，动态尺度门控 $\pi_{i,\ell} = \text{softmax}(W_s[u_{i,1}, u_{i,2}])$ 在阶段 A 中缺乏训练信号（memory score 在阶段 A 中不计算，gate 无梯度来源），导致 gate 参数要么不更新、要么被随机梯度带着走。固定权重虽然简单，但避免了这个训练信号缺失问题。在 ablation 中可以对比"固定权重 vs 可学习权重"来验证这一决策。

同样，completion score 也做多尺度融合：

$$s_i^{comp} = \sum_\ell w_\ell \cdot s_{i,\ell}^{comp}$$

---

## 5.5 模块 5：可信度感知融合

### 5.5.1 核心思想

不是把所有证据简单加权求和，而是根据当前 memory 证据的可靠性来动态调整。

**关键重新定义**：状态新颖度 $a_w$ 和检索覆盖度 $\rho_i$ 不是"异常证据"，而是"memory 证据是否可信"的先验指示器。

### 5.5.2 可靠性信号

**状态新颖度**：

$$a_w = \frac{1}{k_s} \sum_{g \in \text{NN}_{k_s}(g_w, \mathcal{G}_{train})} \|g_w - g\|_2^2$$

$a_w$ 高意味着当前工况在训练集中少见，memory 可能覆盖不足，memory 距离不可靠。

$a_w$ 高**不等于异常**——可能只是一个"新但正常的工况"。

**检索覆盖度**：

$$\rho_i = \frac{1}{|\mathcal{P}|} \sum_\ell \frac{|\mathcal{N}_{i,\ell}|_{\text{eff}}}{K}$$

其中 $|\mathcal{N}_{i,\ell}|_{\text{eff}}$ 是候选集中距离低于某阈值的有效邻居数。$\rho_i$ 低意味着检索到的"近邻"其实也不太近，memory 距离的区分力不足。

### 5.5.3 融合逻辑

构造融合输入向量：

$$f_i = [s_i^{comp},\ s_i^{mem},\ a_w,\ \rho_i]$$

送入一个极轻量的融合器 $\phi$（单调逻辑回归或 2 维 logistic regression），输出最终 patch 异常分数：

$$q_i = \phi(f_i) \in [0, 1]$$

### 5.5.4 融合器的内在逻辑

- 当 $a_w$ 低（工况常见）且 $\rho_i$ 高（检索覆盖充分）时：$\phi$ 主要依赖 $s_i^{mem}$
- 当 $a_w$ 高（工况少见）或 $\rho_i$ 低（检索覆盖不足）时：$\phi$ 降低 memory 权重，更依赖 $s_i^{comp}$
- 当两个证据都高时：高置信度异常
- 当 memory 说正常但 completion 说异常时：可能是 memory 覆盖不足导致的漏检，$\phi$ 会参考可靠性信号做判断

这就避免了"新但正常工况"被 memory 距离直接误杀的问题。

### 5.5.5 关于融合器训练的说明

**推荐方案**：使用 patch-level 伪异常辅助训练。

在阶段 C 中，encoder 和 memory 均冻结。对正常训练数据，随机选取少量 patch 注入伪异常，生成正负样本对，训练 $\phi$。

**伪异常类型**（保留两种）：

1. **频域局部扰动**：对 patch 的 FFT 频谱添加局部扰动（随机频段增幅或减幅），再 IFFT 回时域。这模拟的是"某个频率成分异常增大或消失"。

2. **Latent boundary extrapolation**：在 embedding 空间中，将正常 patch 的表征沿远离最近原型的方向外推一段距离。这模拟的是"正常边界之外的 patch"。

> **设计说明**：不使用"近邻工况 patch swap"。原因是 swap 的效果依赖于状态检索的质量，而状态检索本身正是可能被质疑的模块。依赖一个可能不稳定的模块来生成训练数据，会让整个框架更脆弱。

**Ablation 对照**：论文实验中应对比以下三种融合方式——
1. 伪异常辅助训练的 $\phi$（推荐）
2. 仅用正常数据做分位数阈值（one-class baseline）
3. 不用融合器，直接用 $s_i^{mem}$（最简 baseline）

如果方案 3 性能只略低于方案 1，那融合器的存在价值就需要重新评估——此时应简化为方案 3，减少一个训练阶段。

---

# 六、训练流程

采用三阶段训练，职责严格分离。

## 阶段 A：自监督表征学习

**训练模块**：分解器 $\mathcal{D}_\theta$、状态编码器 $E_s$、多尺度 patch encoder $E_\ell^r / E_\ell^x$、通道调制、completion head $H_\ell^{mask}$、prediction head $H_\ell^{pred}$

**损失**：

$$\mathcal{L}_A = \mathcal{L}_{mask} + \lambda_{pred} \mathcal{L}_{pred} + \lambda_{smooth} \mathcal{L}_{smooth}$$

建议初始值：$\lambda_{pred} = 0.5$，$\lambda_{smooth} = 0.01$

**目标**：学到好的状态表征和残差 patch 表征，使得正常模式的 embedding 空间结构清晰。

## 阶段 B：离线建索引与压缩记忆

**操作**：

1. 用阶段 A 的 encoder 对全部正常训练数据提取 $g_w$、$z_{i,\ell}$、$c_{i,\ell}$
2. 用 completion score 做 self-cleaning，去掉 top-2% 高分 patch
3. 做 coreset 压缩
4. 建两级 ANN 索引

**不训练任何参数。**

## 阶段 C：训练轻量融合器（可选阶段）

**冻结**：分解器、状态编码器、patch encoder、memory、ANN 索引（全部冻结）

**训练**：仅训练融合器 $\phi$

**数据**：正常 patch（标签 0）+ 伪异常 patch（标签 1）

**损失**：二元交叉熵

**说明**：如果 ablation 发现"直接用 $s_i^{mem}$"性能足够好，则省略阶段 C，整个训练简化为 A + B 两阶段。

---

# 七、测试流程

对一个测试窗口 $X^{(w)}$：

| 步骤 | 操作 | 输出 |
|------|------|------|
| 1 | 分解 | $S, R$ |
| 2 | 状态编码 | $g_w$ |
| 3 | 多尺度 patch 切分 + 通道调制 + 编码 | $z_{i,\ell},\ c_{i,\ell}$ |
| 4 | 一级 ANN：状态粗筛 top-$M$ 窗口 | $\mathcal{W}(g

---

# gemini response

Let me re-read the original proposal carefully to make sure the rewrite is comprehensive and faithful to the best parts.





Now I have the complete picture. Let me also re-read the evaluation reply to make sure I incorporate all the valid points.

Now I have complete understanding of all three documents. Let me write the comprehensive refined proposal.

---

# CoReM-AD v2.0 完整框架方案

## Condition-aware Residual Memory for Anomaly Detection

---

# 一、核心思想

整篇论文只围绕一个命题展开：

> **异常不应相对全局正常模式来判断，而应相对"状态相似且局部上下文一致"的正常局部流形来判断。**

对应到工业场景：

- 同样的局部波动，在不同工况下意义不同
- 正常模式不是全局共享的几个中心，而是一个**随状态变化的局部流形族**
- 正常边界是**条件化的、局部的、可检索的**

因此，CoReM-AD 的任务不是学习"唯一正常模板"，也不是"把正常序列重构出来"，而是：

1. 判断当前窗口处于什么运行状态
2. 找到与该状态相近、且局部上下文也相似的正常片段
3. 在这些局部正常邻域里判断当前 patch 是否偏离正常流形

---

# 二、问题定义

给定多变量时间序列：

$$X = \{x_t\}_{t=1}^{T}, \quad x_t \in \mathbb{R}^{C}$$

其中 $T$ 为时间长度，$C$ 为变量数。训练阶段仅使用正常数据。测试阶段输出逐点异常分数 $s_t \in \mathbb{R}$。

---

# 三、贡献点

**贡献 1**：提出一种**状态-上下文双条件局部正常边界框架**，将异常判别从相对全局正常模式改写为相对"状态相似且上下文一致"的局部正常流形，从根本上解决全局 prototype 在多工况场景下边界模糊的问题。

**贡献 2**：提出一种**多尺度条件化 patch memory 机制**，在多个时间粒度上刻画局部正常模式，并通过动态尺度融合同时适应短时突变与长子序列异常。

**贡献 3**：提出一种**可信度感知的轻量证据融合策略**，基于状态新颖度与检索覆盖度自适应调整 memory 与 completion 证据的权重，从而减少"新但正常工况"带来的误判。

---

# 四、总体框架

整体数据流：

$$X \rightarrow (S, R) \rightarrow g_w,\ \{z_{i,\ell}\},\ \{c_{i,\ell}\} \rightarrow \text{dual-key retrieval} \rightarrow \text{multi-scale memory matching} + \text{masked completion} \rightarrow \text{reliability-aware fusion} \rightarrow s_t$$

其中：
- $S$：慢变状态分量
- $R = X - S$：快变残差分量
- $g_w$：窗口级状态向量
- $z_{i,\ell}$：第 $i$ 个 patch 在尺度 $\ell$ 下的表征
- $c_{i,\ell}$：第 $i$ 个 patch 在尺度 $\ell$ 下的局部上下文键

**整个方法由五个核心模块构成：**

| 模块 | 职责 |
|------|------|
| **模块 1：状态-残差分解** | 将原始信号拆分为慢变工况背景与快变局部残差 |
| **模块 2：多尺度条件化 patch 表征** | 在多个时间粒度上编码残差 patch，并利用状态信息做通道调制，同时构造局部上下文键 |
| **模块 3：自监督预训练任务** | Masked residual completion（主任务）+ Next-patch prediction（辅助任务），驱动表征学习 |
| **模块 4：状态-上下文双条件检索与多尺度匹配** | 先按状态粗筛、再按上下文精排，构造条件化局部正常邻域，计算多尺度 memory 距离 |
| **模块 5：可信度感知融合** | 基于状态新颖度和检索覆盖度，自适应加权 memory 证据和 completion 证据，输出最终异常分数 |

---

# 五、模块设计

---

## 5.1 模块 1：状态-残差分解

### 目标

将输入窗口拆分为慢变状态分量 $S$（反映工况背景）和快变残差分量 $R$（反映局部动态扰动）。

### 定义

$$X^{(w)} = S^{(w)} + R^{(w)}$$

### 实现：频域低通初始化 + 轻量时域修正

**步骤 1**：对输入做 FFT，用可学习的软低通掩码 $M(f)$ 提取慢变初值：

$$\hat{S}_0(f) = M(f) \cdot \hat{X}(f), \quad S_0 = \mathcal{F}^{-1}(\hat{S}_0(f))$$

**步骤 2**：用小型时域修正网络（1-2 层 1D-Conv + ReLU）精修：

$$S = S_0 + \Delta_\theta(X, S_0)$$

**步骤 3**：

$$R = X - S$$

### 设计原则

这一步**不追求完美分解**，而是为后续的状态编码和残差建模提供一个比原始信号更有用的视图。即使 $S$ 和 $R$ 的分离不完美，后续模块通过原始旁路机制（见 5.2 节）也能兜底。

### 正则

只保留一个轻量平滑约束：

$$\mathcal{L}_{smooth} = |\nabla_t^2 S|_1$$

不堆叠正交约束、频域泄漏约束或其他复杂正则。理由：STSD 已经被降格为"软增强模块"而非"硬前置过滤器"，过多约束反而会限制其自适应能力。

---

## 5.2 模块 2：多尺度条件化 Patch 表征

这个模块整合了三个子功能：多尺度 patch 编码、状态条件化通道调制、局部上下文键构造。

### 5.2.1 状态编码

慢变分量 $S$ 进入状态编码器：

$$g_w = E_s(S^{(w)}), \quad g_w \in \mathbb{R}^{d_s}$$

$E_s$ 采用轻量 1D-Conv + GlobalAvgPool + MLP 结构。

**关键设计**：状态编码器不做离散工况聚类，不引入工况数 $Q$，不引入 state anchor pull loss。它只负责一件事：

> **为当前窗口生成一个连续的状态表征，用于后续条件化检索。**

### 5.2.2 多尺度残差 Patch 切分与编码

对残差序列 $R^{(w)}$ 在两个尺度下切 patch：

$$\mathcal{P} = \{P_1, P_2\}$$

- $P_1$（细粒度）：捕获短时突变和点异常，如 patch size = 8
- $P_2$（粗粒度）：捕获长子序列异常和形态漂移，如 patch size = 32

> **设计说明**：经过前几轮讨论，确认两个尺度（一细一粗）即可覆盖绝大多数异常类型。第三个中间尺度的边际收益有限，但计算和存储开销会增加 50%。如果实验发现两尺度不够，可在 ablation 中加入第三尺度验证。

对每个尺度 $\ell$，切分得到：

$$R^{(w)} \rightarrow \{r_{i,\ell}\}_{i=1}^{N_\ell}, \quad X^{(w)} \rightarrow \{x_{i,\ell}\}_{i=1}^{N_\ell}$$

**残差主通道编码**：

$$e_{i,\ell}^{r} = E_\ell^r(r_{i,\ell}), \quad e_{i,\ell}^{r} \in \mathbb{R}^{d}$$

$E_\ell^r$ 采用共享 trunk + 尺度特定 head 的结构：底层 1D-Conv 层跨尺度共享参数（因为不同尺度的 patch 在底层共享"局部斜率、振幅"等基础特征），顶层 MLP head 按尺度分开。

**原始旁路编码**（信息瓶颈式兜底）：

$$e_{i,\ell}^{x} = E_\ell^x(x_{i,\ell}), \quad e_{i,\ell}^{x} \in \mathbb{R}^{d/4}$$

原始旁路维度严格限制为主通道的 $1/4$，确保残差仍然是主信息源，原始信号只提供补充。

**融合**：

$$u_{i,\ell} = e_{i,\ell}^{r} + \sigma(v_\ell) \odot W_\ell e_{i,\ell}^{x}$$

其中 $\sigma(v_\ell) \in (0,1)^d$ 是 sigmoid 门控向量（$v_\ell$ 可学习），保证旁路权重有界，不会绕过残差主通道。

**Patch 表征**：

$$z_{i,\ell} = H_\ell(u_{i,\ell}), \quad z_{i,\ell} \in \mathbb{R}^{d_z}$$

$H_\ell$ 为轻量 MLP 投影头。

### 5.2.3 状态条件化通道调制

根据窗口状态 $g_w$ 生成通道门控，对残差 patch 输入做条件化缩放：

$$a_w = \sigma(W_g g_w), \quad a_w \in \mathbb{R}^{C}$$

$$\tilde{r}_{i,\ell}^{(c)} = a_w^{(c)} \cdot r_{i,\ell}^{(c)}$$

这一步在 patch 进入 encoder 之前执行。它表达的物理含义是：不同工况下，不同通道的重要性和正常波动幅度不同。

> **实现说明**：通道调制不作为独立创新点，而是 patch encoder 内部的一个条件化机制。在论文中作为 5.2 节的一个子段落描述即可。

### 5.2.4 局部上下文键构造

对每个 patch，利用其时间邻域内的 patch 表征构造上下文键：

$$c_{i,\ell} = \frac{1}{2k+1} \sum_{j=i-k}^{i+k} u_{j,\ell}$$

其中 $c_{i,\ell} \in \mathbb{R}^{d_c}$，$k$ 为上下文半径（默认 $k=2$）。

> **设计说明**：上下文键采用简单的邻域平均池化而非复杂的 Transformer 或 GRU。原因：上下文键的目的只是"描述当前 patch 周围的局部环境"用于检索匹配，不需要太强的非线性建模。简单平均池化足以捕获"当前 patch 周围是平稳的还是有趋势的"这一关键信息，同时避免引入新的可训练参数和调参负担。

> **边界处理**：窗口边缘的 patch 用零填充或截断邻域。

---

## 5.3 模块 3：自监督预训练任务

### 5.3.1 Masked Residual Completion（主任务）

**训练阶段**：对每个尺度的 patch 序列随机 mask 一部分索引：

$$\Omega_\ell \subset \{1, \dots, N_\ell\}, \quad |\Omega_\ell| = \lfloor \rho \cdot N_\ell \rfloor$$

mask ratio $\rho$ 默认取 0.25。模型根据未被 mask 的上下文 patch 预测被 mask 的 patch：

$$\hat{r}_{i,\ell} = H_\ell^{mask}(u_{1:N_\ell,\ell}^{masked}), \quad i \in \Omega_\ell$$

$H_\ell^{mask}$ 采用轻量 Transformer（2 层，4 头）在 patch 序列上做上下文推理。

损失：

$$\mathcal{L}_{mask} = \sum_\ell \frac{1}{|\Omega_\ell|} \sum_{i \in \Omega_\ell} |\hat{r}_{i,\ell} - r_{i,\ell}|_1$$

**测试阶段**：采用确定性分组 mask 策略——

将每个尺度的 $N_\ell$ 个 patch 按 $\text{index} \mod K$ 分为 $K$ 组（$K=4$），依次 mask 每组并用剩余 patch 补全被 mask 的 patch。每个 patch 恰好被预测一次，无随机性。

测试时第 $i$ 个 patch 的 completion score：

$$s_{i,\ell}^{comp} = |\hat{r}_{i,\ell} - r_{i,\ell}|_1$$

> **设计说明**：$K=4$ 意味着每次 mask 25% 的 patch，推理需要 4 次前向传播。这与 mask ratio $\rho=0.25$ 一致，保证训练和测试的 mask 比例相同。

### 5.3.2 Next-Patch Prediction（辅助任务，仅用于训练）

在每个尺度上预测下一个 patch：

$$\bar{r}_{i+1,\ell} = H_\ell^{pred}(u_{i,\ell})$$

损失：

$$\mathcal{L}_{pred} = \sum_\ell \frac{1}{N_\ell - 1} \sum_{i=1}^{N_\ell - 1} |\bar{r}_{i+1,\ell} - r_{i+1,\ell}|_1$$

**Next-patch prediction 只参与训练，不进入测试阶段的异常分数。** 原因：prediction 误差在测试时存在归因方向模糊问题——当前 patch 异常会导致预测下一个 patch 出错，误差无法明确归因于哪个 patch。但 prediction 对训练时 encoder 学习正常时序演化规律仍然有价值，因此保留为训练辅助损失。

---

## 5.4 模块 4：状态-上下文双条件检索与多尺度匹配

这是 CoReM-AD 最核心的创新模块。

### 5.4.1 训练集索引库构建（阶段 B 离线完成）

用阶段 A 训练好的 encoder 对全部正常训练数据提取：

- **窗口状态向量库**：$\mathcal{G}_{train} = \{g_j^{tr}\}$
- **多尺度 patch 表征库**：$\mathcal{Z}_\ell^{train} = \{z_{j,n,\ell}^{tr}\}$
- **多尺度上下文键库**：$\mathcal{C}_\ell^{train} = \{c_{j,n,\ell}^{tr}\}$

**Memory 质量过滤**：利用阶段 A 训练好的 completion 模型计算每个训练 patch 的 completion score，去掉 score 最高的 top-$\epsilon$%（默认 $\epsilon=2\%$），只用剩余的高质量正常 patch 构建 memory。这是一个 self-cleaning 步骤，无需外部标签，可以过滤训练集中可能存在的标注噪声。

**Coreset 压缩**：对 memory 库做 k-center-greedy 或 diversity-aware coreset selection，将每个状态邻域内的 patch 数量压缩到可控范围，保持多样性的同时降低存储和检索开销。

**ANN 索引构建**：
- 一级索引：对 $\mathcal{G}_{train}$ 建 ANN（Faiss IVF 或 HNSW）
- 二级索引：对 $\mathcal{C}_\ell^{train}$ 建 ANN

### 5.4.2 双条件检索流程

**第一步：状态粗筛**

用测试窗口的状态向量 $g_w$ 在 $\mathcal{G}_{train}$ 中检索 top-$M$ 个状态最近邻的窗口：

$$\mathcal{W}(g_w) = \text{TopMNN}(g_w, \mathcal{G}_{train})$$

$M$ 默认取 50，含义是"从训练集中找到 50 个工况最相似的窗口"。

**第二步：上下文精排**

在粗筛得到的窗口集合内部，对当前 patch 的上下文键 $c_{i,\ell}$ 做精排检索，得到最终的局部正常邻域：

$$\mathcal{N}_{i,\ell} = \text{TopKNN}\big(c_{i,\ell},\ \{c_{j,n,\ell}^{tr} \mid j \in \mathcal{W}(g_w)\}\big)$$

$K$ 默认取 20，含义是"在工况相似的窗口中，找到 20 个局部上下文最接近的 patch"。

**两级检索的意义**：

- 第一级解决"同一工况下才可比"的问题
- 第二级解决"同一工况内局部环境也可能差异很大"的问题（如启停过渡 vs 稳态运行）
- 两级合在一起，检索到的是"状态相似且局部上下文相似的正常 patch"，这比任何单一条件的检索都更精准

### 5.4.3 多尺度条件化 Memory 距离

对当前 patch 表征 $z_{i,\ell}$，在其条件化局部邻域 $\mathcal{M}_{i,\ell} = \{z_{j,n,\ell}^{tr} \mid (j,n) \in \mathcal{N}_{i,\ell}\}$ 中计算 $k$-NN 距离（$k$ 默认取 5）：

$$s_{i,\ell}^{mem} = \frac{1}{k} \sum_{m \in \text{NN}_k(z_{i,\ell},\ \mathcal{M}_{i,\ell})} \|z_{i,\ell} - m\|_2^2$$

### 5.4.4 多尺度融合

不同尺度的 memory 距离用固定权重融合：

$$s_i^{mem} = \sum_\ell w_\ell \cdot s_{i,\ell}^{mem}$$

其中 $w_\ell = 1 / P_\ell$（patch size 的倒数），归一化后使用。含义是：细粒度尺度天然对短时异常更敏感，给予更高权重。

> **设计说明**：这里采用固定权重而非可学习的动态尺度门控。原因是：在前一版方案中，动态尺度门控 $\pi_{i,\ell} = \text{softmax}(W_s[u_{i,1}, u_{i,2}])$ 在阶段 A 中缺乏训练信号（memory score 在阶段 A 中不计算，gate 无梯度来源），导致 gate 参数要么不更新、要么被随机梯度带着走。固定权重虽然简单，但避免了这个训练信号缺失问题。在 ablation 中可以对比"固定权重 vs 可学习权重"来验证这一决策。

同样，completion score 也做多尺度融合：

$$s_i^{comp} = \sum_\ell w_\ell \cdot s_{i,\ell}^{comp}$$

---

## 5.5 模块 5：可信度感知融合

### 5.5.1 核心思想

不是把所有证据简单加权求和，而是根据当前 memory 证据的可靠性来动态调整。

**关键重新定义**：状态新颖度 $a_w$ 和检索覆盖度 $\rho_i$ 不是"异常证据"，而是"memory 证据是否可信"的先验指示器。

### 5.5.2 可靠性信号

**状态新颖度**：

$$a_w = \frac{1}{k_s} \sum_{g \in \text{NN}_{k_s}(g_w, \mathcal{G}_{train})} \|g_w - g\|_2^2$$

$a_w$ 高意味着当前工况在训练集中少见，memory 可能覆盖不足，memory 距离不可靠。

$a_w$ 高**不等于异常**——可能只是一个"新但正常的工况"。

**检索覆盖度**：

$$\rho_i = \frac{1}{|\mathcal{P}|} \sum_\ell \frac{|\mathcal{N}_{i,\ell}|_{\text{eff}}}{K}$$

其中 $|\mathcal{N}_{i,\ell}|_{\text{eff}}$ 是候选集中距离低于某阈值的有效邻居数。$\rho_i$ 低意味着检索到的"近邻"其实也不太近，memory 距离的区分力不足。

### 5.5.3 融合逻辑

构造融合输入向量：

$$f_i = [s_i^{comp},\ s_i^{mem},\ a_w,\ \rho_i]$$

送入一个极轻量的融合器 $\phi$（单调逻辑回归或 2 维 logistic regression），输出最终 patch 异常分数：

$$q_i = \phi(f_i) \in [0, 1]$$

### 5.5.4 融合器的内在逻辑

- 当 $a_w$ 低（工况常见）且 $\rho_i$ 高（检索覆盖充分）时：$\phi$ 主要依赖 $s_i^{mem}$
- 当 $a_w$ 高（工况少见）或 $\rho_i$ 低（检索覆盖不足）时：$\phi$ 降低 memory 权重，更依赖 $s_i^{comp}$
- 当两个证据都高时：高置信度异常
- 当 memory 说正常但 completion 说异常时：可能是 memory 覆盖不足导致的漏检，$\phi$ 会参考可靠性信号做判断

这就避免了"新但正常工况"被 memory 距离直接误杀的问题。

### 5.5.5 关于融合器训练的说明

**推荐方案**：使用 patch-level 伪异常辅助训练。

在阶段 C 中，encoder 和 memory 均冻结。对正常训练数据，随机选取少量 patch 注入伪异常，生成正负样本对，训练 $\phi$。

**伪异常类型**（保留两种）：

1. **频域局部扰动**：对 patch 的 FFT 频谱添加局部扰动（随机频段增幅或减幅），再 IFFT 回时域。这模拟的是"某个频率成分异常增大或消失"。

2. **Latent boundary extrapolation**：在 embedding 空间中，将正常 patch 的表征沿远离最近原型的方向外推一段距离。这模拟的是"正常边界之外的 patch"。

> **设计说明**：不使用"近邻工况 patch swap"。原因是 swap 的效果依赖于状态检索的质量，而状态检索本身正是可能被质疑的模块。依赖一个可能不稳定的模块来生成训练数据，会让整个框架更脆弱。

**Ablation 对照**：论文实验中应对比以下三种融合方式——
1. 伪异常辅助训练的 $\phi$（推荐）
2. 仅用正常数据做分位数阈值（one-class baseline）
3. 不用融合器，直接用 $s_i^{mem}$（最简 baseline）

如果方案 3 性能只略低于方案 1，那融合器的存在价值就需要重新评估——此时应简化为方案 3，减少一个训练阶段。

---

# 六、训练流程

采用三阶段训练，职责严格分离。

## 阶段 A：自监督表征学习

**训练模块**：分解器 $\mathcal{D}_\theta$、状态编码器 $E_s$、多尺度 patch encoder $E_\ell^r / E_\ell^x$、通道调制、completion head $H_\ell^{mask}$、prediction head $H_\ell^{pred}$

**损失**：

$$\mathcal{L}_A = \mathcal{L}_{mask} + \lambda_{pred} \mathcal{L}_{pred} + \lambda_{smooth} \mathcal{L}_{smooth}$$

建议初始值：$\lambda_{pred} = 0.5$，$\lambda_{smooth} = 0.01$

**目标**：学到好的状态表征和残差 patch 表征，使得正常模式的 embedding 空间结构清晰。

## 阶段 B：离线建索引与压缩记忆

**操作**：

1. 用阶段 A 的 encoder 对全部正常训练数据提取 $g_w$、$z_{i,\ell}$、$c_{i,\ell}$
2. 用 completion score 做 self-cleaning，去掉 top-2% 高分 patch
3. 做 coreset 压缩
4. 建两级 ANN 索引

**不训练任何参数。**

## 阶段 C：训练轻量融合器（可选阶段）

**冻结**：分解器、状态编码器、patch encoder、memory、ANN 索引（全部冻结）

**训练**：仅训练融合器 $\phi$

**数据**：正常 patch（标签 0）+ 伪异常 patch（标签 1）

**损失**：二元交叉熵

**说明**：如果 ablation 发现"直接用 $s_i^{mem}$"性能足够好，则省略阶段 C，整个训练简化为 A + B 两阶段。

---

# 七、测试流程

对一个测试窗口 $X^{(w)}$：

| 步骤 | 操作 | 输出 |
|------|------|------|
| 1 | 分解 | $S, R$ |
| 2 | 状态编码 | $g_w$ |
| 3 | 多尺度 patch 切分 + 通道调制 + 编码 | $z_{i,\ell},\ c_{i,\ell}$ |
| 4 | 一级 ANN：状态粗筛 top-$M$ 窗口 | $\mathcal{W}(g$