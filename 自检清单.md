> From: https://gemini.google.com/u/1/app/93d239d3f26c7508?is_sa=1&is_sa=1&android-min-version=301356232&ios-min-version=322.0&campaign_id=bkws&utm_source=sem&utm_medium=paid-media&utm_campaign=bkws&pt=9008&mt=8&ct=p-growth-sem-bkws&gclsrc=aw.ds&gad_source=1&gad_campaignid=20108148196&gbraid=0AAAAApk5Bhmo4ZyZDH8_G8pqBMUeWxcae&gclid=Cj0KCQjw7cLOBhDmARIsAGsuA0mznv4fIOUpLoYmXaBTN2_ZG3n_bD3AzySepEe4GEncAQcz0Tb9G9YaAiRsEALw_wcB&pageId=none

# you asked

请你原封不动的输出：# CoReM-AD v2.0 自检清单

---

## 使用方式

- ✅ = 确认通过
- ⚠️ = 有问题需要修
- ❌ = 未实现
- 每一条后面留空给你打勾

---

# 一、数据处理与预处理

## 1.1 数据加载

| # | 检查项 | 为什么重要 | 你的状态 |
|---|--------|-----------|---------|
| 1.1.1 | train.npy / test.npy 加载后 shape 是 `(T, C)`，不是 `(C, T)` | 转置错误会导致通道混乱，模型训练不报错但结果全废 | |
| 1.1.2 | `test_labels.npy` 的长度 == `test.npy` 的 T 维度 | 标签和数据长度不一致会导致 F1 计算时 silent misalignment | |
| 1.1.3 | 检查 NaN / Inf 处理：`np.nan_to_num` 在归一化**之前**执行 | NaN 进 StandardScaler 会导致 mean/std 变成 NaN，后续全崩 | |
| 1.1.4 | 不同数据集的通道数 C 是否正确传入 config | SMD=38, PSM=25, SMAP=25, MSL=55, SWaT=51 — 搞错会 shape mismatch | |
| 1.1.5 | 如果有多个子数据集（如 SMD 的 28 台机器），每台单独加载、单独归一化 | 混在一起归一化会导致跨机器信息泄漏 | |

## 1.2 归一化

| # | 检查项 | 为什么重要 | 你的状态 |
|---|--------|-----------|---------|
| 1.2.1 | StandardScaler **只 fit 在训练集上**，test 只 transform | fit 了 test 等于信息泄漏 | |
| 1.2.2 | 检查是否有某些通道在训练集中是**全零常量**（std=0） | std=0 时 z-score 会产生 NaN 或 Inf。应在 scaler 中加 `clip(std, min=1e-8)` | |
| 1.2.3 | 归一化后检查：`assert not np.isnan(train_norm).any()` | 作为硬保障 | |
| 1.2.4 | 归一化参数（mean, std）被保存下来，推理时能加载复用 | 部署/复现时需要 | |

## 1.3 滑窗构造

| # | 检查项 | 为什么重要 | 你的状态 |
|---|--------|-----------|---------|
| 1.3.1 | 训练集 stride=1（充分覆盖），测试集 stride=1（逐点分数） | stride > 1 会导致某些时间点没有分数覆盖 | |
| 1.3.2 | 窗口数计算 `n_windows = (T - seq_len) // stride + 1` 是否正确 | off-by-one 错误会导致最后几个时间点丢失 | |
| 1.3.3 | **尾部丢弃**：T 不能被 seq_len 整除时，最后不足一窗的数据如何处理？是丢弃还是 padding？ | 如果丢弃，评估时的标签也要对应截断 | |
| 1.3.4 | 测试集的窗口 label：`window_label = labels[start:start+seq_len]`，是逐点还是取 max？ | 训练用不到标签；测试时用于评估，逐点标签用于计算 F1 | |
| 1.3.5 | `drop_last=True` 只在训练 DataLoader 中使用，测试 DataLoader 用 `drop_last=False` | 测试时丢弃最后一个 batch 会丢失数据 | |
| 1.3.6 | DataLoader 的 `shuffle=True` 只在训练时使用，测试时 `shuffle=False` | 测试时打乱顺序会导致分数无法对齐回原始时间轴 | |

---

# 二、模块维度自检

## 2.1 STSD 分解器

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.1.1 | 输入 `x` | `(B, L, C)` | |
| 2.1.2 | `torch.fft.rfft(x, dim=1)` 的输出 | `(B, L//2+1, C)` 注意是复数 | |
| 2.1.3 | `freq_mask_logits` 的 shape | `(1, C, L//2+1)` — 注意这里 L 必须和实际输入 L 一致 | |
| 2.1.4 | **关键**：如果你用了不同的 seq_len 做测试（比如训练 L=128，测试 L=256），FFT 的频率维度会变，freq_mask_logits 无法匹配 | 必须保证训练和测试的 seq_len 一致，或对 mask 做插值 | |
| 2.1.5 | `irfft` 的 `n=L` 参数必须显式指定 | 不指定时如果 L 是奇数会出错 | |
| 2.1.6 | correction 网络输入 `(B, 2C, L)` → 输出 `(B, C, L)` | Conv1d 的 in_channels=2C, out_channels=C | |
| 2.1.7 | 输出 S 和 R：`S + R == X` 必须严格成立 | 用 `assert torch.allclose(S + R, x, atol=1e-5)` 检查 | |
| 2.1.8 | smoothness_loss 的 `d2S` shape | `(B, L-2, C)` — 不应该出现 L 维度的 off-by-one | |

## 2.2 状态编码器

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.2.1 | 输入 S | `(B, L, C)` → permute → `(B, C, L)` 后送入 Conv1d | |
| 2.2.2 | Conv1d 的 `in_channels` = C（不是 d_hidden） | 第一层 Conv 的 in_channels 容易写错 | |
| 2.2.3 | AdaptiveAvgPool1d(1) 输出 | `(B, d_hidden, 1)` → squeeze(-1) → `(B, d_hidden)` | |
| 2.2.4 | 最终输出 g_w | `(B, d_state)` | |
| 2.2.5 | g_w **没有做 L2 归一化** | 归一化与否会影响后续检索距离的尺度 — 确认你的检索距离计算和归一化策略一致 | |

## 2.3 多尺度 Patch 编码器

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.3.1 | patch 切分：`N_l = L // P_l`，对于 L=128, P=8 → N=16; P=32 → N=4 | 确认两个尺度的 N 值 | |
| 2.3.2 | **关键**：`L % P_l == 0` 必须成立，否则 reshape 会报错 | 建议加 `assert seq_len % ps == 0` | |
| 2.3.3 | 通道调制后：`r_mod` shape 仍然是 `(B, N, P, C)` | 乘法是 broadcast，不应改变 shape | |
| 2.3.4 | reshape 到 `(B*N, P, C)` 送入 SharedConvTrunk | Conv1d 期望 `(batch, channels, length)` = `(B*N, C, P)` | |
| 2.3.5 | **SharedConvTrunk 的 AdaptiveAvgPool1d(1)** | 不管 P 是 8 还是 32，输出都是 `(B*N, d_trunk, 1)` → squeeze → `(B*N, d_trunk)` | |
| 2.3.6 | ScaleHead 输出 | `(B*N, d_z)` | |
| 2.3.7 | BypassEncoder 输出 | `(B*N, d_bypass)` 其中 d_bypass = d_z // 4 = 32 | |
| 2.3.8 | bypass_proj 输出 | `(B*N, d_z)` — 从 d_bypass 投影回 d_z | |
| 2.3.9 | sigmoid gate：`torch.sigmoid(self.bypass_gate_logits[l])` | shape = `(d_z,)` — 广播到 `(B*N, d_z)` | |
| 2.3.10 | 最终 u = e_r + alpha * e_x_proj | `(B*N, d_z)` → reshape → `(B, N, d_z)` | |
| 2.3.11 | **关键：reshape 回 (B, N, d_z) 时 B 和 N 的顺序** | `reshape(B, N, -1)` — 如果写成 `reshape(-1, N, d_z)` 要确认 batch 维度对齐 | |
| 2.3.12 | context key 的 padding：`F.pad(u, (0,0,k,k))` | pad 维度顺序是从最后一维开始的：(d_z_left, d_z_right, N_left, N_right) — 确认 pad 的是 N 维度 | |
| 2.3.13 | context key 的 shape | `(B, N_l, d_z)` — 和 z 同 shape | |
| 2.3.14 | **两个尺度的 N 不同**（N_1=16, N_2=4），后续所有逐尺度操作是否正确处理了不同 N | 不能 stack 不同 N 的 tensor | |

## 2.4 Masked Completion Head

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.4.1 | mask_token shape | `(1, 1, d_z)` → 广播到被 mask 的位置 | |
| 2.4.2 | positional encoding 的 max_len >= 最大 N_l | 如果 N_1=16 且 N_2=4，max_len >= 16 即可 | |
| 2.4.3 | **mask 的 dtype 必须是 bool** | 如果传入 float 或 int，`torch.where` 行为不同 | |
| 2.4.4 | Transformer 的 batch_first=True | 否则输入预期是 `(N, B, d_z)` 而不是 `(B, N, d_z)` | |
| 2.4.5 | out_proj 输出 | `(B, N, P*C)` — 需要 reshape 为 `(B, N, P, C)` 用于计算 loss | |
| 2.4.6 | **每个尺度对应独立的 completion head**（因为 P 不同，output_dim 不同） | 不能共享 completion head | |
| 2.4.7 | loss 只在 masked 位置计算，不在未 mask 位置计算 | 如果对所有位置计算 L1，未 mask 的位置 loss ≈ 0 会稀释梯度 | |

## 2.5 Next-Patch Prediction Head

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.5.1 | 输入 u[:, :-1, :] | `(B, N-1, d_z)` — 用前 N-1 个预测后 N-1 个 | |
| 2.5.2 | target r_patches[:, 1:, :, :] | `(B, N-1, P, C)` | |
| 2.5.3 | output shape | `(B, N-1, P*C)` → reshape → `(B, N-1, P, C)` | |
| 2.5.4 | **这个 head 只参与训练损失，不参与推理分数** | 推理代码中不应该调用这个 head | |

## 2.6 融合器 Scorer

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.6.1 | 输入特征向量 f_i | `[s_comp, s_mem, a_w, rho_i]` — 确认维度 = 4（标量拼接） | |
| 2.6.2 | 如果 a_w 是 window 级标量，需要广播到每个 patch | 每个 patch 共享同一个 a_w | |
| 2.6.3 | 输出 q_i ∈ [0, 1] | 用 sigmoid 而非 softmax | |
| 2.6.4 | **scorer 的参数量应该极小**（几十到几百） | 如果参数量 > 1K，scorer 可能在抢 encoder 的戏 | |

---

# 三、数据流完整性

## 3.1 前向传播完整链路

```
逐步检查以下链路中每一步的输出维度和数值范围：

输入: x (B, L, C)
  │
  ├─ STSD ──→ S (B,L,C), R (B,L,C)     检查: S+R==X ✓?
  │
  ├─ StateEncoder(S) ──→ g_w (B, d_state)   检查: 无 NaN ✓?
  │
  ├─ ChannelMod(R, g_w) ──→ r_mod            检查: shape 不变 ✓?
  │
  ├─ 对每个尺度 l:
  │   ├─ make_patches(R, P_l) ──→ (B, N_l, P_l, C)
  │   ├─ make_patches(X, P_l) ──→ (B, N_l, P_l, C)
  │   ├─ SharedTrunk ──→ (B*N_l, d_trunk)
  │   ├─ ScaleHead ──→ (B*N_l, d_z)           = e_r
  │   ├─ Bypass ──→ (B*N_l, d_bypass) → proj → (B*N_l, d_z) = e_x_proj
  │   ├─ u = e_r + sigmoid(gate) * e_x_proj   (B*N_l, d_z) → (B, N_l, d_z)
  │   ├─ z = u                                 (B, N_l, d_z)
  │   └─ c = context_key(u)                    (B, N_l, d_z)
  │
  ├─ MaskedCompletion(u, mask) ──→ r_hat (B, N_l, P_l*C)
  │   └─ loss: L1(r_hat[masked], r_true[masked])
  │
  └─ NextPatchPred(u) ──→ r_pred (B, N_l-1, P_l*C)
      └─ loss: L1(r_pred, r_true[:, 1:])
```

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 3.1.1 | 用一个 dummy input `torch.randn(2, 128, 25)` 跑完整前向传播，不报错 | |
| 3.1.2 | 打印每一步的输出 shape，和上面的预期逐一核对 | |
| 3.1.3 | 检查梯度：`loss.backward()` 后，所有需要训练的参数的 `.grad` 不为 None | |
| 3.1.4 | 检查梯度不含 NaN：`assert not any(torch.isnan(p.grad).any() for p in model.parameters() if p.grad is not None)` | |

## 3.2 两个尺度之间的独立性

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 3.2.1 | 两个尺度的 completion head 是否独立（不共享参数） | |
| 3.2.2 | 两个尺度的 prediction head 是否独立 | |
| 3.2.3 | 两个尺度的 bypass encoder 是否独立 | |
| 3.2.4 | SharedConvTrunk 是否真的共享（只有一个实例） | |
| 3.2.5 | 两个尺度的 loss 是否分别计算后再求平均，而不是拼接后一起算 | |

---

# 四、训练管道

## 4.1 阶段 A：自监督表征学习

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 4.1.1 | 训练数据**不含任何异常标签** | |
| 4.1.2 | mask 生成：每个 batch 独立随机生成 mask，mask_ratio = 0.25 | |
| 4.1.3 | **mask 是在 patch 维度上的**（mask 掉某些 patch），不是在时间步维度上 | |
| 4.1.4 | mask 的数量：`n_masked = round(N_l * mask_ratio)`，检查两个尺度的 n_masked 是否都 >= 1 | 如果 N_2=4 且 ratio=0.25，n_masked=1，这是合理的 |
| 4.1.5 | 损失计算：`L_A = L_mask + 0.5 * L_pred + 0.01 * L_smooth` | |
| 4.1.6 | L_mask 只在被 mask 的位置计算 L1 | |
| 4.1.7 | L_pred 在所有相邻 patch 对上计算 | |
| 4.1.8 | L_smooth 在 S 上计算（不在 R 上） | |
| 4.1.9 | optimizer 是否包含了**所有需要训练的参数**：STSD + StateEncoder + PatchEncoder + CompletionHeads + PredHeads | |
| 4.1.10 | 学习率调度器（CosineAnnealing / ReduceOnPlateau）是否正确配置 | |
| 4.1.11 | 梯度裁剪：建议 `torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)` | |
| 4.1.12 | **checkpoint 保存**：每 N epoch 保存，保存包括模型参数 + optimizer 状态 + epoch 数 | |
| 4.1.13 | 训练时的 `model.train()` 是否正确调用（影响 BatchNorm 和 Dropout） | |

## 4.2 阶段 B：离线建索引

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 4.2.1 | **model.eval()** + **torch.no_grad()** | 不能有梯度计算 |
| 4.2.2 | 遍历**全部训练集**（不是只一个 epoch 的 batch），提取每个窗口的 g_w, z, c | |
| 4.2.3 | 提取时 DataLoader 的 `shuffle=False` | 保证窗口顺序和 window_id 对应 |
| 4.2.4 | 每个 patch 正确记录它属于哪个 window（window_id mapping） | |
| 4.2.5 | **self-cleaning**：用 completion score 去掉 top-2% — 确认是去掉 score **最高**的 2% | |
| 4.2.6 | self-cleaning 时，completion score 的计算方式和测试时一致（确定性分组 mask） | |
| 4.2.7 | coreset 压缩后，检查每个尺度的 memory 大小是否合理 | |
| 4.2.8 | **Faiss 索引构建**：`state_vectors` 必须是 `float32`，`np.ascontiguousarray` | |
| 4.2.9 | 索引构建后做 sanity check：用训练集自身的 g_w 检索，top-1 应该是自己（距离≈0） | |
| 4.2.10 | memory bank 中的 z, c, window_map 三个数组的长度一致（每个尺度内） | |
| 4.2.11 | memory bank 保存到磁盘（.npz 或 pickle），避免每次重建 | |

## 4.3 阶段 C：融合器训练（可选）

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 4.3.1 | **encoder 和 memory 全部冻结**，只训练 scorer | |
| 4.3.2 | 冻结方式：`for p in encoder.parameters(): p.requires_grad = False` | |
| 4.3.3 | 伪异常生成**只在 patch 级别**（1-2 个 patch），不是整窗 | |
| 4.3.4 | 伪异常标签：被污染的 patch → 1，正常 patch → 0 | |
| 4.3.5 | 伪异常比例：~15% 的 patch 被污染 | |
| 4.3.6 | 伪异常类型 1（频域扰动）：FFT → 随机频段扰动 → IFFT — 确认扰动后的 patch 在数值范围内 | |
| 4.3.7 | 伪异常类型 2（latent boundary extrapolation）：方向是远离最近邻 — 确认方向计算正确 | |
| 4.3.8 | scorer 的 loss 是 **BCE**（二元交叉熵），不是 MSE | |
| 4.3.9 | 阶段 C 的正负样本比例是否平衡 — 如果严重不平衡需要加权 | |
| 4.3.10 | **如果你决定跳过阶段 C**（直接用 s_mem），这里全部跳过，推理时直接输出 s_mem | |

---

# 五、推理管道

## 5.1 确定性分组 Mask

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 5.1.1 | 分组策略：`group_id = index % K`，K=4 | |
| 5.1.2 | **每个 patch 恰好被 mask 一次** — 验证：所有 4 组的 union = 全集，且互不重叠 | |
| 5.1.3 | 当 N_l 不能被 K 整除时（如 N=4, K=4 → 每组 1 个；N=16, K=4 → 每组 4 个），是否处理正确 | |
| 5.1.4 | 推理循环 K=4 次，每次 mask 一组，记录被 mask 位置的补全误差 | |
| 5.1.5 | 最终每个 patch 有且仅有一个 completion score | |
| 5.1.6 | **推理时不能有随机性**：同一输入跑两次，分数必须完全一致 | |
| 5.1.7 | 推理时 `model.eval()` + `torch.no_grad()` | |
| 5.1.8 | BatchNorm 在 eval 模式下使用 running statistics，不是 batch statistics | |

## 5.2 双条件检索

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 5.2.1 | g_w 从 GPU tensor 转为 numpy float32（Faiss 要求） | |
| 5.2.2 | Level-1 检索返回的 indices 范围在 `[0, N_train_windows)` 内 | |
| 5.2.3 | Level-2 候选集：正确从 `patch_window_map` 中筛选出属于 top-M 窗口的 patch | |
| 5.2.4 | 当候选集为空时（极端情况），返回高异常分数而不是 crash | |
| 5.2.5 | kNN 距离计算用的是 `z` 空间的距离（不是 `c` 空间的距离） | |
| 5.2.6 | kNN 的 k=5 和 top_K=20 的关系：先取 20 个上下文近邻，再在这 20 个中取 z 距离最近的 5 个 | |
| 5.2.7 | `state_novelty` 计算：用 top-k_s 个状态距离的均值，不是全部距离的均值 | |
| 5.2.8 | coverage 计算：有效邻居比例 — 确认"有效"的定义（距离 < inf 的邻居） | |

## 5.3 分数计算与聚合

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 5.3.1 | 每个 patch 得到两个子分数：`s_comp` 和 `s_mem`，加上两个可靠性信号 `a_w` 和 `rho_i` | |
| 5.3.2 | 多尺度融合：`s_mem = sum(w_l * s_mem_l)`，权重 `w_l = 1/P_l` 归一化后使用 | |
| 5.3.3 | **确认归一化**：`w = [1/8, 1/32]` → 归一化后 `w = [0.8, 0.2]`（细尺度权重更大） | |
| 5.3.4 | 同样对 `s_comp` 做多尺度融合 | |
| 5.3.5 | 如果用 scorer：输入 `f_i = [s_comp, s_mem, a_w, rho_i]`，输出 `q_i` | |
| 5.3.6 | 如果不用 scorer：最终分数 = `s_mem`（或 `s_comp + s_mem` 经归一化后加权） | |

## 5.4 Patch 分数 → 点级分数

**这是最容易出错的地方之一。**

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 5.4.1 | 每个 patch 对应原始序列中的 `[start, start+P_l)` 这段时间 | |
| 5.4.2 | patch 分数需要**广播到该 patch 覆盖的每个时间点** | |
| 5.4.3 | 不同尺度的 patch 覆盖的时间范围不同：细粒度 8 步 vs 粗粒度 32 步 | |
| 5.4.4 | 如果已经在 patch 级做了多尺度融合（5.3.2），只需把融合后的分数广播到时间点 | |
| 5.4.5 | 如果在时间点级做多尺度融合，同一时间点可能被不同尺度的不同 patch 覆盖，需要取 **max 或 mean** | |
| 5.4.6 | **关键：窗口重叠聚合** — 测试时

---

# gemini response

# CoReM-AD v2.0 自检清单

---

## 使用方式

- ✅ = 确认通过
- ⚠️ = 有问题需要修
- ❌ = 未实现
- 每一条后面留空给你打勾

---

# 一、数据处理与预处理

## 1.1 数据加载

| # | 检查项 | 为什么重要 | 你的状态 |
|---|--------|-----------|---------|
| 1.1.1 | train.npy / test.npy 加载后 shape 是 `(T, C)`，不是 `(C, T)` | 转置错误会导致通道混乱，模型训练不报错但结果全废 | |
| 1.1.2 | `test_labels.npy` 的长度 == `test.npy` 的 T 维度 | 标签和数据长度不一致会导致 F1 计算时 silent misalignment | |
| 1.1.3 | 检查 NaN / Inf 处理：`np.nan_to_num` 在归一化**之前**执行 | NaN 进 StandardScaler 会导致 mean/std 变成 NaN，后续全崩 | |
| 1.1.4 | 不同数据集的通道数 C 是否正确传入 config | SMD=38, PSM=25, SMAP=25, MSL=55, SWaT=51 — 搞错会 shape mismatch | |
| 1.1.5 | 如果有多个子数据集（如 SMD 的 28 台机器），每台单独加载、单独归一化 | 混在一起归一化会导致跨机器信息泄漏 | |

## 1.2 归一化

| # | 检查项 | 为什么重要 | 你的状态 |
|---|--------|-----------|---------|
| 1.2.1 | StandardScaler **只 fit 在训练集上**，test 只 transform | fit 了 test 等于信息泄漏 | |
| 1.2.2 | 检查是否有某些通道在训练集中是**全零常量**（std=0） | std=0 时 z-score 会产生 NaN 或 Inf。应在 scaler 中加 `clip(std, min=1e-8)` | |
| 1.2.3 | 归一化后检查：`assert not np.isnan(train_norm).any()` | 作为硬保障 | |
| 1.2.4 | 归一化参数（mean, std）被保存下来，推理时能加载复用 | 部署/复现时需要 | |

## 1.3 滑窗构造

| # | 检查项 | 为什么重要 | 你的状态 |
|---|--------|-----------|---------|
| 1.3.1 | 训练集 stride=1（充分覆盖），测试集 stride=1（逐点分数） | stride > 1 会导致某些时间点没有分数覆盖 | |
| 1.3.2 | 窗口数计算 `n_windows = (T - seq_len) // stride + 1` 是否正确 | off-by-one 错误会导致最后几个时间点丢失 | |
| 1.3.3 | **尾部丢弃**：T 不能被 seq_len 整除时，最后不足一窗的数据如何处理？是丢弃还是 padding？ | 如果丢弃，评估时的标签也要对应截断 | |
| 1.3.4 | 测试集的窗口 label：`window_label = labels[start:start+seq_len]`，是逐点还是取 max？ | 训练用不到标签；测试时用于评估，逐点标签用于计算 F1 | |
| 1.3.5 | `drop_last=True` 只在训练 DataLoader 中使用，测试 DataLoader 用 `drop_last=False` | 测试时丢弃最后一个 batch 会丢失数据 | |
| 1.3.6 | DataLoader 的 `shuffle=True` 只在训练时使用，测试时 `shuffle=False` | 测试时打乱顺序会导致分数无法对齐回原始时间轴 | |

---

# 二、模块维度自检

## 2.1 STSD 分解器

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.1.1 | 输入 `x` | `(B, L, C)` | |
| 2.1.2 | `torch.fft.rfft(x, dim=1)` 的输出 | `(B, L//2+1, C)` 注意是复数 | |
| 2.1.3 | `freq_mask_logits` 的 shape | `(1, C, L//2+1)` — 注意这里 L 必须和实际输入 L 一致 | |
| 2.1.4 | **关键**：如果你用了不同的 seq_len 做测试（比如训练 L=128，测试 L=256），FFT 的频率维度会变，freq_mask_logits 无法匹配 | 必须保证训练和测试的 seq_len 一致，或对 mask 做插值 | |
| 2.1.5 | `irfft` 的 `n=L` 参数必须显式指定 | 不指定时如果 L 是奇数会出错 | |
| 2.1.6 | correction 网络输入 `(B, 2C, L)` → 输出 `(B, C, L)` | Conv1d 的 in_channels=2C, out_channels=C | |
| 2.1.7 | 输出 S 和 R：`S + R == X` 必须严格成立 | 用 `assert torch.allclose(S + R, x, atol=1e-5)` 检查 | |
| 2.1.8 | smoothness_loss 的 `d2S` shape | `(B, L-2, C)` — 不应该出现 L 维度的 off-by-one | |

## 2.2 状态编码器

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.2.1 | 输入 S | `(B, L, C)` → permute → `(B, C, L)` 后送入 Conv1d | |
| 2.2.2 | Conv1d 的 `in_channels` = C（不是 d_hidden） | 第一层 Conv 的 in_channels 容易写错 | |
| 2.2.3 | AdaptiveAvgPool1d(1) 输出 | `(B, d_hidden, 1)` → squeeze(-1) → `(B, d_hidden)` | |
| 2.2.4 | 最终输出 g_w | `(B, d_state)` | |
| 2.2.5 | g_w **没有做 L2 归一化** | 归一化与否会影响后续检索距离的尺度 — 确认你的检索距离计算和归一化策略一致 | |

## 2.3 多尺度 Patch 编码器

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.3.1 | patch 切分：`N_l = L // P_l`，对于 L=128, P=8 → N=16; P=32 → N=4 | 确认两个尺度的 N 值 | |
| 2.3.2 | **关键**：`L % P_l == 0` 必须成立，否则 reshape 会报错 | 建议加 `assert seq_len % ps == 0` | |
| 2.3.3 | 通道调制后：`r_mod` shape 仍然是 `(B, N, P, C)` | 乘法是 broadcast，不应改变 shape | |
| 2.3.4 | reshape 到 `(B*N, P, C)` 送入 SharedConvTrunk | Conv1d 期望 `(batch, channels, length)` = `(B*N, C, P)` | |
| 2.3.5 | **SharedConvTrunk 的 AdaptiveAvgPool1d(1)** | 不管 P 是 8 还是 32，输出都是 `(B*N, d_trunk, 1)` → squeeze → `(B*N, d_trunk)` | |
| 2.3.6 | ScaleHead 输出 | `(B*N, d_z)` | |
| 2.3.7 | BypassEncoder 输出 | `(B*N, d_bypass)` 其中 d_bypass = d_z // 4 = 32 | |
| 2.3.8 | bypass_proj 输出 | `(B*N, d_z)` — 从 d_bypass 投影回 d_z | |
| 2.3.9 | sigmoid gate：`torch.sigmoid(self.bypass_gate_logits[l])` | shape = `(d_z,)` — 广播到 `(B*N, d_z)` | |
| 2.3.10 | 最终 u = e_r + alpha * e_x_proj | `(B*N, d_z)` → reshape → `(B, N, d_z)` | |
| 2.3.11 | **关键：reshape 回 (B, N, d_z) 时 B 和 N 的顺序** | `reshape(B, N, -1)` — 如果写成 `reshape(-1, N, d_z)` 要确认 batch 维度对齐 | |
| 2.3.12 | context key 的 padding：`F.pad(u, (0,0,k,k))` | pad 维度顺序是从最后一维开始的：(d_z_left, d_z_right, N_left, N_right) — 确认 pad 的是 N 维度 | |
| 2.3.13 | context key 的 shape | `(B, N_l, d_z)` — 和 z 同 shape | |
| 2.3.14 | **两个尺度的 N 不同**（N_1=16, N_2=4），后续所有逐尺度操作是否正确处理了不同 N | 不能 stack 不同 N 的 tensor | |

## 2.4 Masked Completion Head

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.4.1 | mask_token shape | `(1, 1, d_z)` → 广播到被 mask 的位置 | |
| 2.4.2 | positional encoding 的 max_len >= 最大 N_l | 如果 N_1=16 且 N_2=4，max_len >= 16 即可 | |
| 2.4.3 | **mask 的 dtype 必须是 bool** | 如果传入 float 或 int，`torch.where` 行为不同 | |
| 2.4.4 | Transformer 的 batch_first=True | 否则输入预期是 `(N, B, d_z)` 而不是 `(B, N, d_z)` | |
| 2.4.5 | out_proj 输出 | `(B, N, P*C)` — 需要 reshape 为 `(B, N, P, C)` 用于计算 loss | |
| 2.4.6 | **每个尺度对应独立的 completion head**（因为 P 不同，output_dim 不同） | 不能共享 completion head | |
| 2.4.7 | loss 只在 masked 位置计算，不在未 mask 位置计算 | 如果对所有位置计算 L1，未 mask 的位置 loss ≈ 0 会稀释梯度 | |

## 2.5 Next-Patch Prediction Head

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.5.1 | 输入 u[:, :-1, :] | `(B, N-1, d_z)` — 用前 N-1 个预测后 N-1 个 | |
| 2.5.2 | target r_patches[:, 1:, :, :] | `(B, N-1, P, C)` | |
| 2.5.3 | output shape | `(B, N-1, P*C)` → reshape → `(B, N-1, P, C)` | |
| 2.5.4 | **这个 head 只参与训练损失，不参与推理分数** | 推理代码中不应该调用这个 head | |

## 2.6 融合器 Scorer

| # | 检查项 | 预期维度 | 你的状态 |
|---|--------|---------|---------|
| 2.6.1 | 输入特征向量 f_i | `[s_comp, s_mem, a_w, rho_i]` — 确认维度 = 4（标量拼接） | |
| 2.6.2 | 如果 a_w 是 window 级标量，需要广播到每个 patch | 每个 patch 共享同一个 a_w | |
| 2.6.3 | 输出 q_i ∈ [0, 1] | 用 sigmoid 而非 softmax | |
| 2.6.4 | **scorer 的参数量应该极小**（几十到几百） | 如果参数量 > 1K，scorer 可能在抢 encoder 的戏 | |

---

# 三、数据流完整性

## 3.1 前向传播完整链路

```
逐步检查以下链路中每一步的输出维度和数值范围：

输入: x (B, L, C)
  │
  ├─ STSD ──→ S (B,L,C), R (B,L,C)      检查: S+R==X ✓?
  │
  ├─ StateEncoder(S) ──→ g_w (B, d_state)   检查: 无 NaN ✓?
  │
  ├─ ChannelMod(R, g_w) ──→ r_mod            检查: shape 不变 ✓?
  │
  ├─ 对每个尺度 l:
  │    ├─ make_patches(R, P_l) ──→ (B, N_l, P_l, C)
  │    ├─ make_patches(X, P_l) ──→ (B, N_l, P_l, C)
  │    ├─ SharedTrunk ──→ (B*N_l, d_trunk)
  │    ├─ ScaleHead ──→ (B*N_l, d_z)            = e_r
  │    ├─ Bypass ──→ (B*N_l, d_bypass) → proj → (B*N_l, d_z) = e_x_proj
  │    ├─ u = e_r + sigmoid(gate) * e_x_proj   (B*N_l, d_z) → (B, N_l, d_z)
  │    ├─ z = u                                  (B, N_l, d_z)
  │    └─ c = context_key(u)                    (B, N_l, d_z)
  │
  ├─ MaskedCompletion(u, mask) ──→ r_hat (B, N_l, P_l*C)
  │    └─ loss: L1(r_hat[masked], r_true[masked])
  │
  └─ NextPatchPred(u) ──→ r_pred (B, N_l-1, P_l*C)
       └─ loss: L1(r_pred, r_true[:, 1:])
```

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 3.1.1 | 用一个 dummy input `torch.randn(2, 128, 25)` 跑完整前向传播，不报错 | |
| 3.1.2 | 打印每一步的输出 shape，和上面的预期逐一核对 | |
| 3.1.3 | 检查梯度：`loss.backward()` 后，所有需要训练的参数的 `.grad` 不为 None | |
| 3.1.4 | 检查梯度不含 NaN：`assert not any(torch.isnan(p.grad).any() for p in model.parameters() if p.grad is not None)` | |

## 3.2 两个尺度之间的独立性

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 3.2.1 | 两个尺度的 completion head 是否独立（不共享参数） | |
| 3.2.2 | 两个尺度的 prediction head 是否独立 | |
| 3.2.3 | 两个尺度的 bypass encoder 是否独立 | |
| 3.2.4 | SharedConvTrunk 是否真的共享（只有一个实例） | |
| 3.2.5 | 两个尺度的 loss 是否分别计算后再求平均，而不是拼接后一起算 | |

---

# 四、训练管道

## 4.1 阶段 A：自监督表征学习

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 4.1.1 | 训练数据**不含任何异常标签** | |
| 4.1.2 | mask 生成：每个 batch 独立随机生成 mask，mask_ratio = 0.25 | |
| 4.1.3 | **mask 是在 patch 维度上的**（mask 掉某些 patch），不是在时间步维度上 | |
| 4.1.4 | mask 的数量：`n_masked = round(N_l * mask_ratio)`，检查两个尺度的 n_masked 是否都 >= 1 | 如果 N_2=4 且 ratio=0.25，n_masked=1，这是合理的 |
| 4.1.5 | 损失计算：`L_A = L_mask + 0.5 * L_pred + 0.01 * L_smooth` | |
| 4.1.6 | L_mask 只在被 mask 的位置计算 L1 | |
| 4.1.7 | L_pred 在所有相邻 patch 对上计算 | |
| 4.1.8 | L_smooth 在 S 上计算（不在 R 上） | |
| 4.1.9 | optimizer 是否包含了**所有需要训练的参数**：STSD + StateEncoder + PatchEncoder + CompletionHeads + PredHeads | |
| 4.1.10 | 学习率调度器（CosineAnnealing / ReduceOnPlateau）是否正确配置 | |
| 4.1.11 | 梯度裁剪：建议 `torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)` | |
| 4.1.12 | **checkpoint 保存**：每 N epoch 保存，保存包括模型参数 + optimizer 状态 + epoch 数 | |
| 4.1.13 | 训练时的 `model.train()` 是否正确调用（影响 BatchNorm 和 Dropout） | |

## 4.2 阶段 B：离线建索引

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 4.2.1 | **model.eval()** + **torch.no_grad()** | 不能有梯度计算 |
| 4.2.2 | 遍历**全部训练集**（不是只一个 epoch 的 batch），提取每个窗口的 g_w, z, c | |
| 4.2.3 | 提取时 DataLoader 的 `shuffle=False` | 保证窗口顺序和 window_id 对应 |
| 4.2.4 | 每个 patch 正确记录它属于哪个 window（window_id mapping） | |
| 4.2.5 | **self-cleaning**：用 completion score 去掉 top-2% — 确认是去掉 score **最高**的 2% | |
| 4.2.6 | self-cleaning 时，completion score 的计算方式和测试时一致（确定性分组 mask） | |
| 4.2.7 | coreset 压缩后，检查每个尺度的 memory 大小是否合理 | |
| 4.2.8 | **Faiss 索引构建**：`state_vectors` 必须是 `float32`，`np.ascontiguousarray` | |
| 4.2.9 | 索引构建后做 sanity check：用训练集自身的 g_w 检索，top-1 应该是自己（距离≈0） | |
| 4.2.10 | memory bank 中的 z, c, window_map 三个数组的长度一致（每个尺度内） | |
| 4.2.11 | memory bank 保存到磁盘（.npz 或 pickle），避免每次重建 | |

## 4.3 阶段 C：融合器训练（可选）

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 4.3.1 | **encoder 和 memory 全部冻结**，只训练 scorer | |
| 4.3.2 | 冻结方式：`for p in encoder.parameters(): p.requires_grad = False` | |
| 4.3.3 | 伪异常生成**只在 patch 级别**（1-2 个 patch），不是整窗 | |
| 4.3.4 | 伪异常标签：被污染的 patch → 1，正常 patch → 0 | |
| 4.3.5 | 伪异常比例：~15% 的 patch 被污染 | |
| 4.3.6 | 伪异常类型 1（频域扰动）：FFT → 随机频段扰动 → IFFT — 确认扰动后的 patch 在数值范围内 | |
| 4.3.7 | 伪异常类型 2（latent boundary extrapolation）：方向是远离最近邻 — 确认方向计算正确 | |
| 4.3.8 | scorer 的 loss 是 **BCE**（二元交叉熵），不是 MSE | |
| 4.3.9 | 阶段 C 的正负样本比例是否平衡 — 如果严重不平衡需要加权 | |
| 4.3.10 | **如果你决定跳过阶段 C**（直接用 s_mem），这里全部跳过，推理时直接输出 s_mem | |

---

# 五、推理管道

## 5.1 确定性分组 Mask

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 5.1.1 | 分组策略：`group_id = index % K`，K=4 | |
| 5.1.2 | **每个 patch 恰好被 mask 一次** — 验证：所有 4 组的 union = 全集，且互不重叠 | |
| 5.1.3 | 当 N_l 不能被 K 整除时（如 N=4, K=4 → 每组 1 个；N=16, K=4 → 每组 4 个），是否处理正确 | |
| 5.1.4 | 推理循环 K=4 次，每次 mask 一组，记录被 mask 位置的补全误差 | |
| 5.1.5 | 最终每个 patch 有且仅有一个 completion score | |
| 5.1.6 | **推理时不能有随机性**：同一输入跑两次，分数必须完全一致 | |
| 5.1.7 | 推理时 `model.eval()` + `torch.no_grad()` | |
| 5.1.8 | BatchNorm 在 eval 模式下使用 running statistics，不是 batch statistics | |

## 5.2 双条件检索

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 5.2.1 | g_w 从 GPU tensor 转为 numpy float32（Faiss 要求） | |
| 5.2.2 | Level-1 检索返回的 indices 范围在 `[0, N_train_windows)` 内 | |
| 5.2.3 | Level-2 候选集：正确从 `patch_window_map` 中筛选出属于 top-M 窗口的 patch | |
| 5.2.4 | 当候选集为空时（极端情况），返回高异常分数而不是 crash | |
| 5.2.5 | kNN 距离计算用的是 `z` 空间的距离（不是 `c` 空间的距离） | |
| 5.2.6 | kNN 的 k=5 和 top_K=20 的关系：先取 20 个上下文近邻，再在这 20 个中取 z 距离最近的 5 个 | |
| 5.2.7 | `state_novelty` 计算：用 top-k_s 个状态距离的均值，不是全部距离的均值 | |
| 5.2.8 | coverage 计算：有效邻居比例 — 确认"有效"的定义（距离 < inf 的邻居） | |

## 5.3 分数计算与聚合

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 5.3.1 | 每个 patch 得到两个子分数：`s_comp` 和 `s_mem`，加上两个可靠性信号 `a_w` 和 `rho_i` | |
| 5.3.2 | 多尺度融合：`s_mem = sum(w_l * s_mem_l)`，权重 `w_l = 1/P_l` 归一化后使用 | |
| 5.3.3 | **确认归一化**：`w = [1/8, 1/32]` → 归一化后 `w = [0.8, 0.2]`（细尺度权重更大） | |
| 5.3.4 | 同样对 `s_comp` 做多尺度融合 | |
| 5.3.5 | 如果用 scorer：输入 `f_i = [s_comp, s_mem, a_w, rho_i]`，输出 `q_i` | |
| 5.3.6 | 如果不用 scorer：最终分数 = `s_mem`（或 `s_comp + s_mem` 经归一化后加权） | |

## 5.4 Patch 分数 → 点级分数

**这是最容易出错的地方之一。**

| # | 检查项 | 你的状态 |
|---|--------|---------|
| 5.4.1 | 每个 patch 对应原始序列中的 `[start, start+P_l)` 这段时间 | |
| 5.4.2 | patch 分数需要**广播到该 patch 覆盖的每个时间点** | |
| 5.4.3 | 不同尺度的 patch 覆盖的时间范围不同：细粒度 8 步 vs 粗粒度 32 步 | |
| 5.4.4 | 如果已经在 patch 级做了多尺度融合（5.3.2），只需把融合后的分数广播到时间点 | |
| 5.4.5 | 如果在时间点级做多尺度融合，同一时间点可能被不同尺度的不同 patch 覆盖，需要取 **max 或 mean** | |
| 5.4.6 | **关键：窗口重叠聚合** — 测试时 stride=1，每个时间点会被 `seq_len` 个不同的窗口覆盖。需要对这些窗口产生的分数取 **mean**。确认这一步的代码逻辑。 | |