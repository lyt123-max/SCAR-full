# 消融实验方案

## 本轮落地范围

这次先落实 A3，保持“每个消融单独一个文件，不把所有消融混在一个代码文件里”。

本轮已经对应到以下独立脚本：

- `scripts/ablation/run_a3_pit_fusion.sh`
- `scripts/ablation/run_a3_raw_max.sh`
- `scripts/ablation/run_a3_zscore_mean.sh`
- `scripts/ablation/run_a3_single_scale_short.sh`
- `scripts/ablation/run_a3_single_scale_long.sh`
- `scripts/ablation/run_a3_multi_scale.sh`
- `scripts/ablation/run_a3_matrix.sh`

兼容入口仍保留：

- `scripts/ablation/run_dataset_ablation.sh`

它现在只做分发，不再承担 A3 配置逻辑本身。

---

## A. 核心消融实验总目标

这一组实验不是为了证明“模块越多越好”，而是为了回答 3 个核心问题：

### Q1. 状态 + 局部解耦表征是否必要？

对应 A1。

### Q2. 检索参考集到底该如何定义？

对应 A2。

### Q3. 多源证据校准和多尺度建模是否真的有贡献？

对应 A3。

---

## A0. Full 模型定义

所有消融都相对同一个 Full 版本进行。

### Full 包含

- STSD 分解：`X -> S, R`
- 状态编码器：`S -> g_w`
- 状态条件化通道门控
- 多尺度 patch 编码
- 原始旁路补偿
- 局部上下文键 `c`
- 双条件检索
- Level 1：state-based coarse retrieval
- Level 2：context-based reranking
- completion score
- memory distance
- state novelty
- CDF-PIT calibration
- 默认融合：`cdf_max`
- 默认尺度：双尺度

### Full 作为对照基线时固定的内容

除非某个消融本身就在研究对应因素，否则以下设置尽量不变：

- encoder 宽度
- window 长度
- batch size
- optimizer / lr
- epoch
- memory bank 压缩率
- top-K
- candidate M
- 阈值规则
- 随机种子组

---

## A1. 表征主干消融

A1 只回答一个问题：

> 收益是否来自状态感知的表征主干，而不是单纯来自检索或后处理？

### A1.1 w/o decomposition

- 独立脚本：`scripts/ablation/run_a1_wo_decomposition.sh`
- 文件标题：`A1: w/o decomposition`

### A1.2 w/o state-aware representation

- 独立脚本：`scripts/ablation/run_a1_wo_state_aware_representation.sh`
- 文件标题：`A1: w/o state-aware representation`

---

## A2. 检索条件设计消融

A2 回答的问题是：

> 你的“局部正常参考”到底该怎么构造？

### A2.1 global retrieval

- 独立脚本：`scripts/ablation/run_a2_global_retrieval.sh`
- 文件标题：`A2: global retrieval`

### A2.2 state-only retrieval

- 独立脚本：`scripts/ablation/run_a2_state_only_retrieval.sh`
- 文件标题：`A2: state-only retrieval`

### A2.3 context-only retrieval

- 独立脚本：`scripts/ablation/run_a2_context_only_retrieval.sh`
- 文件标题：`A2: context-only retrieval`

### A2.4 dual-condition retrieval

- 独立脚本：`scripts/ablation/run_a2_dual_condition_retrieval.sh`
- 文件标题：`A2: dual-condition retrieval`

---

## A3. 校准与时间尺度消融

A3 回答两个问题：

1. 统计校准是否必要  
2. 多尺度是否必要

这一组现在已经全部拆成独立脚本。

### A3.1 PIT fusion

独立脚本：`scripts/ablation/run_a3_pit_fusion.sh`  
文件标题：`A3: PIT fusion`

#### 目的

给出带校准的 Full 融合作为 A3 的基线。

#### 实现

- 融合方式采用 `CDF-PIT + cdf_max`
- 作为当前默认的主报告分数
- 这组实验的 `selected` 分数等于 `cdf_max`

#### 含义

- 对照 raw-max 和 zscore-mean
- 用来证明校准不仅是后处理，而是带来更稳定的融合

---

### A3.2 raw-max

独立脚本：`scripts/ablation/run_a3_raw_max.sh`  
文件标题：`A3: raw-max fusion`

#### 目的

验证去掉 PIT 校准后，直接对原始子分数做最大值融合是否足够。

#### 实现

- 新增 `raw_max` 融合路径
- 直接对原始子分数取 `max`
- 这组实验的 `selected` 分数等于 `raw_max`

#### 含义

- 不做经验 CDF / PIT
- 不做标准化
- 是最直接的“无校准版本”

---

### A3.3 zscore-mean

独立脚本：`scripts/ablation/run_a3_zscore_mean.sh`  
文件标题：`A3: zscore-mean fusion`

#### 目的

验证一个比 raw-max 更强、但仍不使用 PIT 的简单统计融合是否已经够用。

#### 实现

- 新增 `zscore_mean` 融合路径
- 在训练正常数据上拟合每个子分数的 `mean/std`
- 测试时先做 z-score，再对各子分数求平均
- 这组实验的 `selected` 分数等于 `zscore_mean`

#### 含义

- 它是“有标准化、无 PIT”的中间对照
- 用来区分“收益来自普通标准化”还是“收益来自 PIT 校准”

---

### A3.4 single-scale (short)

独立脚本：`scripts/ablation/run_a3_single_scale_short.sh`  
文件标题：`A3: single-scale (short)`

#### 目的

验证只用短时间尺度时，模型是否更偏向尖峰型和短时异常。

#### 实现

- `patch_sizes="8"`
- 这组实验的 `selected` 分数默认仍使用 `cdf_max`

#### 含义

- 作为 multi-scale 的单尺度短窗对照

---

### A3.5 single-scale (long)

独立脚本：`scripts/ablation/run_a3_single_scale_long.sh`  
文件标题：`A3: single-scale (long)`

#### 目的

验证只用长时间尺度时，模型是否更偏向慢漂移或长模式异常。

#### 实现

- `patch_sizes="32"`
- 这组实验的 `selected` 分数默认仍使用 `cdf_max`

#### 含义

- 作为 multi-scale 的单尺度长窗对照

---

### A3.6 multi-scale

独立脚本：`scripts/ablation/run_a3_multi_scale.sh`  
文件标题：`A3: multi-scale`

#### 目的

给出双尺度方案作为时间尺度比较的最终方法行。

#### 实现

- `patch_sizes="8 32"`
- 这组实验的 `selected` 分数默认使用 `cdf_max`

#### 含义

- 与 `single-scale(short)`、`single-scale(long)` 直接比较
- 用来验证多尺度不是简单加参数，而是真的覆盖不同时间尺度异常

---

## A3 汇报建议

### 校准对比表

| Variant | SWaT | WADI | SMD | MSL | SMAP | Avg |
|---|---:|---:|---:|---:|---:|---:|
| PIT fusion |  |  |  |  |  |  |
| raw-max |  |  |  |  |  |  |
| zscore-mean |  |  |  |  |  |  |

建议比较指标：

- F1
- AUPRC
- range-F1
- seed 波动
- 阈值敏感性

### 尺度对比表

| Variant | SWaT | WADI | SMD | MSL | SMAP | Avg |
|---|---:|---:|---:|---:|---:|---:|
| single-scale(short) |  |  |  |  |  |  |
| single-scale(long) |  |  |  |  |  |  |
| multi-scale |  |  |  |  |  |  |

建议比较指标：

- F1
- AUPRC
- range-F1
- 按异常长度分桶的 recall / event recall

---

## A3 当前实现说明

为了让不同 A3 变体能放进同一张表里，当前代码新增了统一的 `selected` 指标：

- `pit_fusion` 的 `selected = cdf_max`
- `raw_max` 的 `selected = raw_max`
- `zscore_mean` 的 `selected = zscore_mean`
- `single_scale_short / single_scale_long / multi_scale` 的 `selected = cdf_max`

这样做的好处是：

- A3 汇总表可以统一读 `selected`
- 同时又保留所有底层分数文件和原始指标

当前已经额外保存：

- `raw_max`
- `zscore_mean`
- `cdf_max`
- `cdf_mean`
- `cdf_softmax`
- `selected`

---

## A 组统一实验设置建议

### 1. 数据集

A 部分建议至少跑：

- SWaT
- WADI
- SMD

如果资源允许再扩到：

- MSL
- SMAP

### 2. 随机种子

正式实验中每个变体统一使用固定 seed `42` 运行一次，汇报固定 seed 结果。CLI 覆盖
仅用于调试，不得进入论文正式表格。

### 3. 阈值协议

必须全变体一致。

- 如果有验证集：验证集选阈值
- 如果没有：训练正常分数采用同一 quantile rule

### 4. 参数一致性

除非是实验变量本身，以下参数全部固定：

- window size
- optimizer / lr
- batch size
- epoch
- memory top-K
- candidate M
- coreset ratio

---

## 运行方式

直接跑某一个 A3 消融：

```bash
bash scripts/ablation/run_a3_pit_fusion.sh PSM
bash scripts/ablation/run_a3_raw_max.sh PSM
bash scripts/ablation/run_a3_zscore_mean.sh PSM
bash scripts/ablation/run_a3_single_scale_short.sh PSM
bash scripts/ablation/run_a3_single_scale_long.sh PSM
bash scripts/ablation/run_a3_multi_scale.sh PSM
```

跑整组 A3：

```bash
bash scripts/ablation/run_a3_matrix.sh
```

如果仍想沿用统一入口，也可以：

```bash
bash scripts/ablation/run_psm.sh pit_fusion
bash scripts/ablation/run_psm.sh raw_max
bash scripts/ablation/run_psm.sh zscore_mean
bash scripts/ablation/run_psm.sh single_scale_short
bash scripts/ablation/run_psm.sh single_scale_long
bash scripts/ablation/run_psm.sh multi_scale
```

---

## 待拍板事项

这次我先按当前代码和你方案的主线做了两个默认假设，如果你想改，我再一起调整：

1. `single-scale(short)` 先默认对应 `patch_size=8`，`single-scale(long)` 默认对应 `patch_size=32`。
2. `multi-scale` 先默认对应当前双尺度配置 `patch_sizes="8 32"`。

另外还有一个现存范围问题我没有擅自扩展：

3. 文档里仍推荐 `WADI`，但当前脚本仍只支持 `MSL / SMAP / PSM / SWAT / SMD`，还没有 `WADI` 数据入口。
