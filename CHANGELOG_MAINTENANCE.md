# 项目修改记录与维护台账

> 维护约定：本文件用于记录项目后续每一次改动。以后不论是代码、参数、脚本、数据协议、文档还是实验口径发生变化，都需要在下表新增记录，写清楚改动内容、结果、验证方式和后续事项。
>
> 历史记录保留其发生时的原始协议。若历史条目与最新条目冲突，以表格最上方的最新协议及 `PROJECT_PLAN.md` 为准；2026-07-24 以前条目中的三 seed 正式运行计划已被固定 seed `42` 协议废止。

## 长期维护规则

- `PROJECT_PLAN.md` 是项目方案说明文档，负责保持“当前项目是什么、怎么运行、有哪些模块和参数”的最新状态。
- `CHANGELOG_MAINTENANCE.md` 是修改记录文档，负责记录“什么时候改了什么、结果如何、如何验证、是否还有风险”。
- 每次修改后都要判断是否需要同步更新 `PROJECT_PLAN.md`；只要影响项目使用方式、模型理解、脚本入口、关键参数、数据集、产物或评估口径，就必须同步更新。
- 修改记录按时间倒序追加，最新记录放在表格最上方。
- 验证状态必须写清楚：已运行的命令、未运行的原因，或仅文档变更无需运行测试。
- 每完成一批代码或文档修改都必须创建一次本地 Git 提交；提交信息应简明注明改动主题，
  不得把不同目的的后续修改长期堆积在未提交工作区。

## 修改记录

| 日期 | 类型 | 改动范围 | 改动内容 | 改动结果 | 验证情况 | 后续事项 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-24 | Git/维护规则 | `.gitignore`、`CHANGELOG_MAINTENANCE.md`、当前实现与文档 | 按作者要求提交当前项目状态，并新增“每完成一批修改即创建本地提交、提交信息注明主题”的长期规则；将本地 `CATCH-master/` 上游快照、NeurIPS 模板副本和论文 PDF 明确排除在主仓库之外。 | 当前 SCAR 实现、实验流水线、测试、环境规格和维护文档纳入同一次可审计提交；外部材料继续保留在工作区但不污染版本历史。 | 提交前重新执行 `git diff --check`、检查 staged 文件清单并确认外部材料均被忽略。 | 后续每次修改完成并验证后立即创建独立本地提交，同时更新本表。 |
| 2026-07-24 | 代码/脚本/协议/测试/文档 | `coremad/{config,trainer}.py`、`run.py`、`scripts/experiments/`、`scripts/rebuttal/muqn/`、`scripts/main/train_tep*.sh`、`tests/`、三份方案文档 | 补齐实验闭环最后一轮：E10 每个数据集新增唯一冻结三折协议，非零污染与零污染重评估共用事件折和嵌套候选前缀；唯一异常窗口不足时用固定 seed 的重复随机排列补足并披露；产物检查升级为 checkpoint 头、JSON/NPY/NPZ 可读性、SCAR/TEP/baseline 分数长度检查；resume 增加配置/源码/数据哈希一致性并向模型与 TEP shell 传递续跑标志；源码指纹与环境冻结纳入未跟踪源码文件哈希；CLI 增加 `--data-root-map` 并重映射依赖 run-id；失败依赖写入审计记录；TEP 增加仅供 smoke 的序列数上限并改用序列级正式产物验收；新增严格 P0 CSV/文字汇总器。 | 中央 manifest 更新为 1230 项：P0 903、P1 327；实际 P0 训练口径不变，仍为 659 full 加 180 Stage-B/Test。E10 包含 5 个冻结协议、135 个非零 Stage-B/Test 和 30 个零污染重评估；正式输出不含图。远程 smoke 会冻结六个环境并将 TEP 限为 2 条序列，正式 TEP 仍为 168 条。 | `coremad/`、`scripts/`、`tests/` 与 `run.py` 共 121 个 Python 文件通过 AST/compileall；66 个项目 shell 脚本 `bash -n` 通过；静态 preflight 16/16 通过；完整 manifest 无重复 run-id。`unittest discover` 发现 92 项，其中 87 项通过，5 个模块仅因本地 Python 缺少 PyTorch 无法导入；额外手工执行 7 个纯 NumPy contamination pytest 函数并通过，本地未安装 pytest。尚未执行 CUDA 模型级 smoke。 | 在 RTX 4090 服务器按 `environments/` 建环境，运行 `preflight.py --strict-runtime` 与 `remote_smoke.sh`；重点核验五个 baseline 真实上游兼容性、PUAD 项目侧单序列适配、TEP 分片以及全部 PyTorch/pytest 测试。G8 通过后再启动 P0。 |
| 2026-07-24 | 代码/脚本/参数/环境/实验协议/测试/文档 | `scripts/experiments/`、`scripts/rebuttal/{baselines,muqn}/`、`scripts/efficiency/`、`scripts/main/`、`scripts/sensitivity/`、`scripts/tsb_ad/`、`environments/`、`third_party/baselines/README.md`、`tests/`、三份方案文档 | 实现 SCAR rebuttal 中央配置和 `plan/run/resume/status/validate/collect` CLI；加入稳定 run-id、配置/数据/源码哈希、环境冻结、依赖调度、严格复用检查和正式模式保护。补齐 full memory audit、E9 0.005、E10 嵌套污染与零污染复用、三策略重校准评分、E11/E12、E38/E39、CATCH 30 文件审计、SCAR 1+3 计时、TEP 固定目录/无图入口、TSB 官方 598 项保护及 E29/E30/E31 runner。固定并登记 MEMTO/CATCH 上游，建立 PaAno/MEMTO/PUAD/PGRF-Net/CATCH 五数据集项目侧 adapter、统一数据/逐点分数/资源协议和独立环境规格。 | 正式 manifest 为 1224 项：P0 897 项（659 full、180 Stage-B/Test、58 analysis），P1 327 项（88 full、20 Stage-B/Test、219 analysis）；run-id 无重复。P0 重计算总量保持 839，TSB 为 M20+180、U48+350。正式命令不含绘图任务或 U/eval_full；stronger purification 统一为 `clean_ratio=0.10`。 | 114 个项目 Python 文件通过 AST/py_compile，65 个 shell 脚本通过 `bash -n`；83 个单测中 78 个通过，剩余 5 个仅因桌面 Python 缺少 PyTorch而无法导入。CATCH 12 ASD+6 real+12 synthetic 及 DETECT_META 边界审计通过；TSB M/U eval 各一条 dry-run 通过；25 个 baseline 中央 dry-run 通过；静态第三方 commit/环境/preflight 通过。未在本机执行 CUDA 模型级 smoke 或正式训练。 | 在远程 RTX 4090 按 `environments/` 创建独立环境并冻结实际 lock；运行 `preflight.py --strict-runtime` 与 `remote_smoke.sh`，特别验证五个 baseline 的真实上游兼容性、MEMTO/PUAD 官方环境失败时的最小 CUDA 兼容升级、TEP 小分片及完整 PyTorch 测试。只有 smoke 和 G0-G8 全通过后启动 P0。 |
| 2026-07-24 | 文档/TSB-AD 官方协议/运行量 | `rebuttal实验运行清单.md`、`rebuttal执行计划.md`、`PROJECT_PLAN.md`、`scripts/tsb_ad/README.md`、`CHANGELOG_MAINTENANCE.md` | 将 rebuttal 的 TSB-AD 正式范围改为官方 runner 默认口径：M-Tuning 20、M-Eva 180、U-Tuning 48、U-Eva 350。确认官方仓库同时发布 U-Eva-Full 822，但将其明确降为论文修订扩展，不进入 P0；PaAno 仍不运行 TSB-AD。补充多配置 tuning 的运行量公式 `20×H_M + 48×H_U + 530`。 | 按 M/U 各验证一个预注册配置核算，TSB-AD 从 1070 次降为 598 次 full；完整 P0 从 1131 次降为 659 次 full model fit，另有 180 次 Stage-B/Test，共 839 次重计算任务。无需实现从 U822 派生 U350 的正式 collector，`eval-full` 代码能力继续保留。 | 核对官方仓库 `Datasets/File_List` 和 `Run_Detector_U/M.py`：U/M runner 默认分别使用 U-Eva 350 与 M-Eva 180；本地 manifests 计数为 M `20/180`、U `48/350/822`，U350 是 U822 子集，U822 与 tuning 48 并集为 U all 870。运行活跃文档关键词扫描和 `git diff --check`；未启动训练。 | 在远程 CUDA 环境先对 M/U 各做一条模型级冒烟；正式 batch manifest 禁止提交 `U/eval_full`，并在启动前冻结 `H_M/H_U` 和配置候选。 |
| 2026-07-24 | 文档/实验范围/运行量 | `rebuttal实验运行清单.md`、`rebuttal执行计划.md`、`PROJECT_PLAN.md`、`CHANGELOG_MAINTENANCE.md` | 根据作者确认，从 TSB-AD 实验中删除 PaAno；PaAno 仅保留在 MSL、PSM、SMAP、SMD、SWaT 五个主数据集的相关基线与效率比较。TSB-AD 只新增 SCAR 的 M tuning/eval 与 U tuning/eval-full，U-Eva 350 继续从 U-Eva-Full 822 派生；公开 TSB-AD 基线只标记为 `reported`。 | 删除最多 1070 次 PaAno 逐序列 full fit；完整 P0 上限由 2201 次降为 1131 次 full model fit，Stage-B/Test 仍为 180 次；TSB-AD full fit 由 SCAR/PaAno 共 2140 次降为 SCAR 1070 次。 | 全仓 Markdown 搜索确认活跃实验清单和总计划不再要求 PaAno TSB-AD；重新核算 `5+25+30+1+1070=1131`；运行 `git diff --check`，未启动训练。 | TSB-AD runner 仅需完成 SCAR 模型级冒烟和 U-Eva350 无训练派生 collector；无需开发 PaAno TSB-AD 批量 runner。 |
| 2026-07-24 | 文档/实验调度/产物复用 | `rebuttal实验运行清单.md`、`rebuttal执行计划.md`、`PROJECT_PLAN.md`、`CHANGELOG_MAINTENANCE.md` | 新增面向远程服务器的实验运行清单，按可复用产物重排 P0：五数据集主运行同时作为机制、E9 默认点、E10 零污染默认点、E38 全量 memory 点和资源锚点；E9 的无净化 score 复用于 E10 零污染 no-purification；TSB-U Eva 350 从 Eva-Full 822 派生；五数据集 baseline 的性能和资源来自同一 checkpoint。登记正式运行前必须补齐的审计参数透传、E9/E10/E38 runner、TSB 子集 collector、baseline adapters 和批量 manifest。 | 去重后五数据集只训练 5 个 SCAR Stage-A；E9 只新增 25 个、E10 只新增 135 个、E38 只新增 20 个 Stage-B/Test；E11/E12/E39 和 U-Eva350 均为零训练派生。完整 P0 上限核算为 2201 次 full model fit 和 180 次 Stage-B/Test，其中 2140 次 full 来自 TSB-AD 的逐序列 SCAR/PaAno。 | 对照当前主训练脚本、敏感性 runner、污染 runner、TSB manifests/collector、TEP full 入口和资源监控说明人工核对；确认当前主脚本尚未透传 `memory_audit_mode`、E9 尚缺 0.005、E38 尚无 keep-ratio sweep、U-Eva350 尚不能从 full collector 派生。运行 Markdown 结构检查和 `git diff --check`；未启动训练。 | 先实现并测试清单 G0-G8；在远程 CUDA 环境完成每类 runner 的单样本模型冒烟，再按清单顺序提交正式任务。 |
| 2026-07-24 | 文档/实验范围/远程执行协议 | `rebuttal执行计划.md`、`PROJECT_PLAN.md`、`CHANGELOG_MAINTENANCE.md` | 根据作者确认重构 rebuttal 执行范围：CATCH 补齐全部兼容真实数据及六类合成异常；TSB-AD 固定 M-Eva 180、U-Eva-Full 822 并从相同产物派生 U-Eva 350；E9/E10 全覆盖五个主数据集；SCAR 与 PaAno/MEMTO/PUAD/PGRF-Net/CATCH 在五数据集同次采集性能和资源；所有训练在远程服务器执行；无 MATLAB 时 DAMP/GDFlex 只做诚实协议审计；rebuttal 禁止图片，统一改为表格和文字分析。 | 原 39 个独立任务被重组为共享 checkpoint 的远程流水线，完整证据范围保留但删除重复训练；Figure 1、机制图和各类曲线移入论文修订 backlog；稀有正常与低误差异常统计改由 E9/E10 audit 产物派生。 | 人工逐节检查任务优先级、TSB manifests、E9/E10 参数、基线集合、资源字段、表格清单、执行顺序和验收条件；运行 Markdown 关键词扫描和 `git diff --check`，未启动训练。 | 获取远程服务器连接入口和环境信息；补齐五数据集 baseline adapters；让 U-Eva collector 可从 U-Eva-Full 产物无重复训练地汇总；完成模型级 smoke test 后按 P0 队列启动。 |
| 2026-07-24 | 代码/实验协议/测试/文档 | `scripts/tsb_ad/{common,run_benchmark,collect_results}.py`、`scripts/rebuttal/muqn/{run_contamination_sweep.py,README.md}`、`scripts/rebuttal/muqn/baselines/run_paano.py`、`tests/test_{single_seed_protocol,tsb_ad,contamination_protocol}.py`、`rebuttal执行计划.md`、`Reviewer_ef7G_解决方案.md`、`PROJECT_PLAN.md`、`消融实验方案.md`、效率设计与 TSB-AD README | 将正式 rebuttal/论文模型随机种子统一固定为 `42`；保留 CLI 覆盖供调试，但非 42 结果不得进入正式表格。PaAno 默认 seed 从 `2000` 改为 `42`，污染 folds 不再派生额外 seed；TSB collector 默认只汇总 `seed_42`，`--all-seeds` 仅用于历史审计。同步删除活跃文档中的三 seed 和模型 `mean±std` 承诺，同时保留 event folds、block bootstrap、参数扫描和推理重复计时。 | 正式训练矩阵缩减为每个方法/数据集一次 seed-42 模型运行；历史 `seed_*` 目录仍可读取，但不会混入默认正式表格。性能结果标为 fixed-seed result，计时均值/标准差只表示系统测量噪声。 | 全仓 `compileall` 通过；单 seed 协议 3 项、TSB-AD 6 项、污染协议 5 项和资源监控 25 项测试通过，其中覆盖历史 seed 43 不进入默认 collector；TSB 与 PaAno dry-run 均只生成一次 `--seed 42` 模型命令。完整 `unittest discover` 的 42 个发现项中 37 项通过，另 5 个模块因当前运行时缺少 PyTorch 而在导入阶段阻塞；当前环境亦未安装 `pytest`。 | 在完整 CUDA/PyTorch 环境分别进行一个 SCAR、TSB-AD 和 PaAno seed-42 模型级冒烟，并补跑完整测试套件；正式表格不得调用 `--all-seeds`。 |
| 2026-07-24 | 代码/脚本/参数/测试/文档 | `coremad/resource_monitor.py`、`coremad/{config,trainer}.py`、`run.py`、`scripts/efficiency/`、`tests/test_resource_monitor.py`、`PROJECT_PLAN.md` | 新增默认开启的 SCAR 三阶段资源监控、纯 inference/端到端 scoring 等嵌套区间、CPU/子进程 RSS 与按 PID CPU 时间、PyTorch/NVML GPU 峰值、实际窗口/覆盖点吞吐、模型和产物大小、跨进程安全原子追加 JSON；新增不修改第三方源码的外部命令包装器，支持严格依赖预检、追加日志、启动失败、退出码、OOM、超时和进程树终止。 | Stage A/B/Test 保持原公共入口并共享 invocation；resume 的 Stage B 也经过包装并记录 skipped；`CUDA_VISIBLE_DEVICES`/UUID 正确映射 NVML，unavailable 哨兵不伪造峰值；损坏历史 JSON 不会被覆盖；外部进程不会误报包装器自身的 PyTorch allocator；失败/OOM/超时/中断和依赖不可用均可审计。 | 统一监控的 25 项 `unittest` 通过，覆盖成功/失败/OOM/超时/启动失败、真实子进程 RSS、动态子进程 CPU 累计、跨进程并发写、严格依赖与可见 GPU 映射、NVML unavailable、损坏 JSON/采样线程故障、重复日志/span、吞吐、跳过和存储故障隔离；相关文件通过 `py_compile`，`git diff --check` 通过。当前桌面 Python 无 PyTorch/CUDA，尚未执行真实 SCAR 三阶段和真实 GPU 峰值冒烟。 | 在正式 CUDA 环境安装 `scripts/efficiency/requirements.txt`，先执行 SCAR CPU/GPU 小样本全流程并核对 NVML 与 PyTorch 峰值，再接入五数据集 baseline adapters、批量 runner 和 SHA-256 manifest。 |
| 2026-07-24 | 代码/脚本/参数/实验协议/测试/文档 | `coremad/{config,memory,trainer,evaluation}.py`、`run.py`、`scripts/sensitivity/run_dataset_sensitivity.py`、`scripts/rebuttal/muqn/`、`tests/test_{memory_provenance,muqn_proxy_metrics,contamination_protocol,evaluation}.py`、`PROJECT_PLAN.md` | 针对 Reviewer muQn 增加记忆 patch 来源追踪、独立 `memory_seed`、逐 patch/状态稀有度审计、检索三策略日志和条件兼容性/稳定性统计；将 `clean_ratio` 纳入敏感性框架，新增事件级 K 折污染且整段排除注入事件；增加 PaAno、DAMP 项目侧只读适配、GDFlex 适用范围审计及统一逐点评估。 | 新建 memory 会保存精确原始起点，旧产物以 `-1` 显式表示未知；`memory_meta.json` 会记录净化/coreset 数量和 cap 是否生效；可生成粗/细条件距离、邻居 Jaccard、块 bootstrap 区间、稀有正常误删率和污染 held-out 指标；第三方源码未修改。 | 全部相关 Python 文件通过 `compileall`；纯 NumPy 代理指标、污染事件、统一 AUROC/AP 合成检查通过；检索 evidence 用三策略合成 NPZ 完成端到端冒烟；PaAno/DAMP dry-run 和 GDFlex 审计通过。当前桌面运行时无 PyTorch/pytest/CUDA，且未检测到 MATLAB，因此未运行 MemoryBank PyTorch 单测、真实 Stage-B 重建、正式污染训练和 DAMP 数值实验。 | 在原训练环境重建五个主数据集 Stage B（旧 memory 无精确 provenance），先各做一个小规模导出/污染冒烟，再按 seeds `42/43/44` 跑正式实验；核对 `coreset_cap_active`，论文不得把实际 200k cap 的产物写成未压缩；安装 PaAno 独立环境并在 MATLAB 中验证 DAMP 输出长度与训练边界。 |
| 2026-07-24 | 文档/实验协议审计 | `PROJECT_PLAN.md`、`CHANGELOG_MAINTENANCE.md`、`CATCH-master/`、`dataset/anomaly_detect/`、`scripts/main/train_{msl,psm,smap,smd,swat}.sh` | 对照 CATCH 官方论文、官方代码、五数据集脚本和 SCAR 主实验实现，新增 CATCH 主实验对齐审计；区分必须统一的共享协议与应保留的模型专属超参数。 | 确认五个主数据集、训练边界及 AUROC/AP 实现已经对齐；确认 seed、重复次数、验证比例/方式尚未完全对齐，官方 CATCH 结果当前只能标为 reported，不能标为统一协议三 seed 重跑。 | 对 `DETECT_META.csv`、MSL、PSM、SMAP、SMD、SWaT 两份本地副本执行 SHA-256 核验并一致；人工核对 CATCH `unfixed_detect_score_multi_config.json`、五个官方 CATCH 脚本、`CATCH.py`、指标实现、SCAR 数据加载器/训练脚本及论文实验设置。未启动训练。 | 建立 CATCH 统一协议运行适配，使用 seeds `42/43/44` 输出 AUROC/AP mean/std；在完成前修改论文中“all methods are evaluated under the same protocol”的过强表述，清楚区分 reported 与 reproduced。 |
| 2026-07-24 | 文档/评审分析/论文方案 | `Reviewer_ef7G_解决方案.md`、`rebuttal执行计划.md`、`PROJECT_PLAN.md`、`CHANGELOG_MAINTENANCE.md` | 纳入 ef7G Official Review 原文及 Quality/Clarity/Significance/Originality/Rating 信息，逐句复核原方案；明确 reviewer 把 TEP 误认成 custom dataset，修正 Figure 1 方案为先重组 Section 3.2-3.4、再按同名三阶段绘图，并将核心方法信息从 Appendix 移回正文；同步修正总执行清单的优先级和 TEP full 主方案。 | 专项方案现在直接对应原始评审；Related Work 改为保留其获认可的现有结构并补映射和选择规则，Figure 与正文形成 3.2 Representation Learning、3.3 Memory Construction、3.4 Retrieval and Scoring 的一一对应；TEP full 与最近 retrieval 基线均固定为 P0。 | 人工核对用户提供的完整原始评审与专项文档五组问题，确认 Summary、Weaknesses 和 Questions 均已覆盖；本次仅修改 Markdown 文档，未运行训练。 | 在正式 rebuttal 文本中明确 MMFDD-TEP 公共来源；完成新增实验后，把真实数值填入回复，避免只报告计划或预期结论。 |
| 2026-07-24 | 文档/实验方案/论文审计 | `Reviewer_ef7G_解决方案.md`、`PROJECT_PLAN.md`、`CHANGELOG_MAINTENANCE.md` | 对照 ef7G 执行清单、当前论文、CATCH 全量数据、MMFDD-TEP full 协议、训练脚本和外部基线代码，形成五组问题的逐条解决方案；固定 CATCH 12 真实数据集族/12 合成文件、ASD macro average、三随机种子、TEP 六模式全故障、PaAno 优先基线和 Figure 1 三阶段口径，并登记缺失环境。 | 项目新增 ef7G 专项执行入口；明确 TEP 应以 full 结果为主、基线对齐提升为 P0、Figure 1 只保留总体链路；同时识别论文旧 TEP 表把 `dXX` 误作同号 IDV 的事实错误。 | 已人工核对 `main.tex` 方法/数据/基线段落并渲染检查当前 Figure 1；核对本地 CATCH 35 个 CSV、TSB-AD 数据、TEP 174 个核心文件、主训练脚本和 PaAno/DAMP/GDFlex 官方代码。尝试运行 TEP 审计时确认当前可调用 Python 缺少 SciPy；本次为文档变更，未启动训练。 | 获取 ef7G 原始评审全文；进入完整 CUDA 环境后按 `42/43/44` 启动 CATCH 扩展和 TEP full；优先实现 PaAno 统一协议适配，并在 MATLAB 环境审计 DAMP/GDFlex。 |
| 2026-07-24 | 代码/脚本/参数/数据协议/测试/文档 | `coremad/config.py`、`coremad/data.py`、`coremad/trainer.py`、`run.py`、`scripts/main/train_tep*.sh`、`scripts/tep/`、`tests/test_tep_*.py`、`PROJECT_PLAN.md` | 完整接入 MMFDD-TEP `full` 协议，同时保留 `selected` 默认值；新增六模式正常联合训练、168 条全故障测试、严格协议 checkpoint 兼容检查、数据审计、测试与机制日志逐序列原子分片/断点续跑、schema v2 只读适配、序列等权指标、动态六模式图表及 T2/T9 rebuttal 表格。 | `full` 可自动解析 `M1-M6` 嵌套目录，使用官方 `d01-d28 → IDV28-IDV1` 映射；Stage A/B 会拒绝跨协议产物，分片签名绑定全部 `.mat` 文件及模型/校准产物状态；确认正常训练/审计窗口为 35,196/5,718，故障窗口为 1,132,009；168 条故障全部保留，默认不生成约 4 GB 窗口 CSV；T9 自动发现 selected 基线并以只读方式补算旧 schema 缺失指标。 | 本地完整仓库审计通过：6 个模式、174 个核心文件、81 列、158 条 7,201 点故障和 10 条短故障且均不少于 128 点；14 个协议/映射/目录解析/边界/schema 兼容测试和 2 个本地完整数据测试通过；Python `compileall`、Shell `bash -n`、合成机制指标测试和 `git diff --check` 通过。当前环境缺少完整 PyTorch/SciPy/Matplotlib/CUDA，未启动正式全量训练和绘图运行级冒烟。 | 在 CUDA 训练环境执行 `DATA_ROOT=./Multi-mode-Fault-Diagnosis-Datasets-with-TE-process bash scripts/main/train_tep_full.sh full tep_full_mechanism`；如中断，使用相同实验名并设置 `RESUME=1`。 |
| 2026-07-24 | 基线/代码包/文档 | `third_party/baselines/DAMP/`、`third_party/baselines/README.md`、`PROJECT_PLAN.md` | 从 DAMP 作者官方文档页下载标准 2.0、topK、fullMP、X-Lag-Amnesic、Golden 和 Multidim 六个 MATLAB 源文件，并保存官方使用说明与 BourkeStreetMall 样例；登记 Drive 文件 ID 和 SHA-256。 | 官方 DAMP 非 Git 代码包已纳入本地只读基线目录；五个多变量基准的正式适配目标固定为 `DAMP_Multidim.m`。 | 六个 `.m` 文件均验证为可读 MATLAB 源码；PPTX 文件头为有效 ZIP 签名 `50-4B-03-04`；样例为数值序列；八个文件均完成大小和 SHA-256 校验。尚未在 MATLAB 中运行。 | 审计 `DAMP_Multidim.m` 的输出、训练/测试边界、子序列长度和逐点分数映射，再建立 SCAR 五数据集包装与统一 AUROC/AP 评价入口。 |
| 2026-07-24 | 基线/仓库/文档 | `third_party/baselines/`、`.gitignore`、`PROJECT_PLAN.md` | 拉取 PaAno、PUAD、PGRF-Net、GDFlex 四个作者/官方 GitHub 仓库，保留为独立 Git 工作树；新增来源与 commit 清单，并让父项目忽略仓库本体。 | 四个基线已位于统一目录，远端均命名为 `upstream`；固定提交依次为 `d4c6711`、`41e8b43`、`5dc6f75`、`4d93205`。DAMP 因官方代码不在 GitHub、DMemAD 因暂无公开官方仓库，未在本轮拉取。 | 核验了四个仓库的远端 URL、`main` 分支、完整 commit SHA 和工作树状态；四个工作树均干净。尚未安装各基线依赖或运行模型。 | 下一步逐仓库开展协议审计，优先为 PaAno 和 DAMP 建立 SCAR 五数据集适配与统一 AUROC/AP 评价入口。 |
| 2026-07-24 | 代码/脚本/数据/实验/文档 | `coremad/config.py`、`coremad/data.py`、`coremad/trainer.py`、`run.py`、`scripts/tsb_ad/`、`tests/test_tsb_ad.py`、`PROJECT_PLAN.md` | 新增 TSB-AD-U/M 单 CSV 独立训练协议、官方 ACF VUS window、四种固定融合输出、官方 split 批量运行与断点续跑、失败记录、长表和四张论文宽表；固定 `TSB-AD==1.5` 指标依赖并登记官方 manifests。 | 已可识别当前双层 M/U 数据目录，按 `_tr_N_` 使用正常前缀并对完整序列评分；`cdf_mean` 固定为主结果，同时正式输出 `raw_max`、`zscore_mean`、`cdf_max`，产物完整性检查会核对所有逐点数组长度。 | 官方清单及本地 1070 个 CSV 全量头检查通过：M `200/20/180`，U `870/48/350/822`；真实 M/U 样例的训练长度、完整长度、标签首异常位置和通道数核对通过；6 个单元测试通过；M/U `--limit 1 --dry-run` 通过；全部 Python 文件通过 `py_compile`。模型级 `--limit 1` 尚未运行，因为当前可调用 Python 缺少 PyTorch 和 TSB-AD 包。 | 在正式 SCAR 训练环境安装 `scripts/tsb_ad/requirements.txt` 后，分别执行 M/U `--limit 1` 模型冒烟，再启动 seeds 42/43/44；完整 1070 条训练本次未启动。 |
| 2026-07-24 | 代码/数据协议/文档 | `coremad/data.py`、`scripts/tep/`、`scripts/main/download_tep_selected.py`、`scripts/ablation/generate_paper_tables.py`、`tests/test_tep_fault_mapping.py`、`PROJECT_PLAN.md` | 按 MMFDD-TEP 上游官方设定统一故障编号：`d01-d28` 反向映射到 `IDV28-IDV1`；新增双向映射函数，在机制元数据中同时保留原始 `file_fault_id` 和官方 `fault_id`，并修正下载提示、论文表格及机制图标签。 | 当前受控子集的八个文件编号正式对应 `IDV28/25/23/22/19/16/15/2`；训练数据和异常标签不变，重新导出的机制日志与图表将使用官方 IDV。 | 对全部 28 个故障执行双向往返检查并通过；当前八文件映射检查通过；所有涉及的 Python 文件通过 `compileall`；`git diff --check` 通过。桌面 Python 缺少项目依赖，未运行需要完整 `torch/scipy` 环境的端到端机制导出。 | 旧机制日志、统计表和图片仍保留旧编号语义，正式 rebuttal 实验前须重新导出；全量六模式实验沿用本映射。 |
| 2026-07-24 | 数据/文档 | `Multi-mode-Fault-Diagnosis-Datasets-with-TE-process/`、`.gitignore`、`PROJECT_PLAN.md` | 拉取 THUFDD 完整 MMFDD-TEP 上游仓库，登记六模式全故障数据源，并将大体积嵌套仓库加入父项目 Git 忽略规则。 | 已获得 `M1` 至 `M6`、每模式 `d00` 至 `d28` 的 174 个核心 `.mat` 文件；上游仓库检出于提交 `4d7b8a7`，工作树与 `origin/main` 一致。 | 核对六个模式目录及文件名覆盖完整；仓库共 256 个文件、约 0.641 GB。默认 Python 会话当前无法启动，尚未读取 `.mat` 内部扰动通道。 | 全量实验前核验上游 README 所述 `d01-d28` 与 `IDV28-IDV1` 的反向映射，并扩展加载器与运行脚本以支持六模式全故障协议。 |
| 2026-07-22 | 文档 | `PROJECT_PLAN.md`、`CHANGELOG_MAINTENANCE.md` | 新增项目方案说明文档和长期维护台账，覆盖项目介绍、总体模型框架、方法说明、模块设计、关键参数、脚本功能、数据集介绍和训练命令。 | 项目已有统一方案入口和变更记录入口；后续改动需要同步维护这两份表。 | 文档变更，未运行训练/测试。 | 后续任何代码、脚本、参数或实验口径变化，都要新增本表记录并按需更新 `PROJECT_PLAN.md`。 |

## 记录模板

新增记录时复制以下模板到“修改记录”表格顶部：

```markdown
| YYYY-MM-DD | 代码/脚本/参数/数据/实验/文档 | `涉及文件或模块` | 简述改动内容 | 改动后的实际结果 | 运行了哪些验证，或说明未验证原因 | 待办、风险或无 |
```

## 当前维护状态

| 文档 | 当前状态 | 最近维护日期 | 说明 |
| --- | --- | --- | --- |
| `PROJECT_PLAN.md` | 持续维护 | 2026-07-24 | 已登记 MMFDD-TEP、TSB-AD 和 CATCH 主实验对齐审计；后续作为项目方案和使用说明的主入口。 |
| `CHANGELOG_MAINTENANCE.md` | 持续维护 | 2026-07-24 | 已记录数据/基线接入、协议审计和验证结果；后续作为所有改动的维护台账。 |
