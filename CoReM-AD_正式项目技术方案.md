# CoReM-AD 正式项目技术方案

基于当前仓库代码实现整理，核心实现位于 `run.py` 与 `coremad/` 目录，面向多变量时间序列异常检测任务。

## 1. 项目背景与目标

随着工业控制、网络安全、能源系统与复杂传感网络的持续发展，多变量时间序列中普遍存在工况切换频繁、局部扰动复杂、异常形态多样的问题。传统基于全局重构误差或单一原型匹配的异常检测方法，容易在多工况场景下出现正常模式混叠、异常边界模糊和误报率偏高等问题。

本项目提出 CoReM-AD 异常检测框架，目标是在仅使用正常训练数据的前提下，构建一个兼顾工况感知、局部模式检索和多证据融合的检测系统。项目核心目标包括：

- 学习能够区分慢变系统状态与快变局部扰动的时序表征。
- 在多尺度时间粒度下建立可检索的正常记忆库，而非依赖单一全局正常模板。
- 通过“状态粗筛 + 上下文精排”的两级检索机制，构造与当前输入更匹配的局部正常参考。
- 将检索距离、状态新颖度和补全误差等多源证据进行校准融合，输出稳定、可解释的异常分数。
- 支持从训练、记忆库构建到测试评估与可视化导出的完整工程化流程。

## 2. 核心功能概述

项目围绕异常检测全流程实现了以下核心能力：

- 自监督表征学习：对正常训练数据进行窗口级预训练，学习状态表示与多尺度残差表示。
- 时序分解建模：将输入序列拆分为慢变成分与残差成分，分别承担状态建模与异常敏感建模。
- 多尺度 Patch 编码：在不同时间尺度上提取局部模式表示，兼顾短时突变与长时结构异常。
- 条件记忆检索：基于状态向量和上下文键值，从正常记忆库中检索相似局部片段。
- 记忆库自清洗与压缩：利用补全误差过滤低质量记忆，并通过 coreset 机制控制规模。
- 分数校准融合：对不同子分数进行 CDF-PIT 或 Z-score 校准，形成更稳健的最终异常得分。
- 多协议评估：支持点级评估，也支持序列级聚合评估，适配 TEP 等按序列判别的数据场景。
- 工程化产物输出：自动保存模型、记忆库、融合器、分数文件、指标文件与可视化图像。

## 3. 系统架构 / 模块划分

系统采用分阶段、模块化设计，整体可划分为五个层次。

### 3.1 入口与配置层

- `run.py`：统一命令行入口，负责 `stage_a`、`stage_b`、`test`、`full` 四种执行模式调度。
- `coremad/config.py`：集中管理数据、模型、训练、检索、融合与评估相关超参数，并负责实验目录与工件路径管理。

### 3.2 数据处理层

- `coremad/data.py`：负责多数据集加载、格式校验、缓存、训练/验证切分、标准化和滑动窗口构建。
- 支持两类数据源：
- CATCH 风格 CSV 数据集，通过 `DETECT_META.csv` 指定训练段长度。
- TEP `.mat` 数据集，按正常工况与故障工况组织训练、验证与测试序列。

### 3.3 表征学习层

- `coremad/model.py`：定义核心神经网络，包括 STSD 分解器、状态编码器、多尺度 Patch 编码器、Masked Completion 头与 Next-Patch Prediction 头。
- 该层负责从窗口输入中生成：
- 慢变状态表征 `state_vec`
- 多尺度局部特征 `z`
- 局部上下文键 `c`
- 多尺度补全误差

### 3.4 记忆检索与评分层

- `coremad/memory.py`：构建正常记忆库，保存窗口级状态向量与多尺度 Patch 特征，并实现两级检索与 kNN 异常评分。
- `coremad/faiss_index.py`：提供 Level-1 状态检索索引，优先使用 Faiss，失败时回退到精确 Torch 检索。
- `coremad/scorer.py`：实现 CDF-PIT 融合器、Z-score 融合器与原始最大值融合策略。

### 3.5 训练调度与结果输出层

- `coremad/trainer.py`：负责 Stage A 训练、Stage B 记忆构建与融合器拟合、测试评估、断点恢复和可视化导出。
- `coremad/visualization.py`：负责训练曲线、分数分布图和时间轴检测图导出。

## 4. 关键技术与实现思路

### 4.1 STSD 慢变-残差分解

模型首先对窗口输入执行可学习频域低通分解，再通过轻量卷积修正慢变分量，得到慢变状态成分与残差成分。该设计的目的不是做严格信号分离，而是将“工况背景”与“局部异常敏感信息”拆开建模，使后续状态编码与局部匹配更加稳定。

### 4.2 基于慢变分量的状态表示学习

状态编码器采用卷积 + 全局池化 + MLP 结构，对窗口级慢变成分进行编码，得到状态向量。状态向量承担两个任务：一是刻画当前窗口所处工况，二是在检索阶段用于快速筛选状态相近的正常窗口，避免直接在全局正常样本中搜索导致的工况错配。

### 4.3 多尺度残差 Patch 表征

残差序列在多个 Patch 尺度上切分编码。短尺度更适合捕获尖峰、瞬态和短时扰动，长尺度更适合捕获持续偏移、结构变形和阶段性异常。编码器采用“共享卷积主干 + 尺度专属投影头”的结构，在控制参数量的同时兼顾跨尺度表征一致性。

### 4.4 状态感知的通道调制

项目引入基于状态向量生成的通道门控，对残差 Patch 做状态条件调制。其设计意图是让模型认识到不同工况下各通道的重要性和正常波动范围并不一致，从而提升表征的工况自适应能力。

### 4.5 上下文键构造与双条件检索

对于每个 Patch，系统不仅保留其表示向量 `z`，还通过局部邻域平均构造上下文键 `c`。测试时先利用状态向量进行 Level-1 粗筛，再在候选窗口对应的 Patch 集合中基于上下文键进行 Level-2 精排，最终以最近邻 Patch 的嵌入距离形成记忆匹配分数。该机制将“窗口状态一致性”和“局部上下文一致性”同时纳入判断依据。

### 4.6 自监督补全与辅助预测

Stage A 训练阶段采用两类自监督任务：

- Masked Completion：随机遮挡部分 Patch，通过上下文补全原始片段，驱动模型学习局部时序结构。
- Next-Patch Prediction：预测后续 Patch，增强时序演化规律建模能力。

其中补全误差不仅服务于训练，还在 Stage B 用于记忆库自清洗，并在测试阶段作为独立异常证据参与融合。

### 4.7 记忆库自清洗与 Coreset 压缩

为降低噪声窗口或边缘模式对记忆库质量的影响，系统先按照补全误差剔除高误差 Patch，再通过近似最远点采样或分层随机采样进行 coreset 压缩。该策略兼顾记忆代表性、检索效率与工程可部署性。

### 4.8 分数校准与融合

项目并不直接将多个子分数简单相加，而是在 Stage B 使用正常训练数据拟合两类融合器：

- CDF-PIT 融合：将各子分数映射到统一分位空间，再执行 `max`、`mean` 或 `softmax` 融合。
- Z-score 融合：将各子分数标准化后求均值。

该设计可以降低不同分数量纲不一致带来的偏置，提高跨数据集与跨工况下的输出稳定性。

## 5. 代码逻辑与流程说明

### 5.1 执行阶段划分

项目整体流程由 `run.py` 调度，分为三个执行阶段：

- Stage A：自监督表征学习，训练编码器与补全/预测头，输出模型检查点和归一化器。
- Stage B：使用冻结编码器构建正常记忆库，并拟合分数融合校准器。
- Test：对测试集逐窗口打分，再汇聚为点级或序列级异常结果。

### 5.2 Stage A 训练流程

1. 加载训练数据并完成验证集切分。
2. 使用训练段拟合标准化器，并将训练、验证、测试数据统一映射到标准空间。
3. 通过滑动窗口构造训练样本。
4. 对每个窗口执行 STSD 分解、状态编码和多尺度 Patch 编码。
5. 计算补全损失、预测损失和平滑正则项，形成总损失。
6. 使用 AdamW 与余弦退火学习率调度进行优化，并支持梯度裁剪、早停和断点续训。
7. 保存最优检查点 `stage_a.pt` 与最近检查点 `stage_a_last.pt`。

### 5.3 Stage B 构建流程

1. 加载 Stage A 最优模型与标准化器，并冻结参数。
2. 对全部正常训练窗口进行编码，得到状态向量、多尺度 Patch 表示和上下文键。
3. 可选地计算补全误差，对高误差 Patch 执行自清洗。
4. 使用 coreset 对各尺度记忆进行压缩。
5. 建立窗口级状态索引，优先采用 Faiss。
6. 基于训练数据重新计算点级诊断分数，拟合 CDF-PIT 与 Z-score 融合器。
7. 输出 `memory.pt`、Faiss 索引、融合器参数和元信息文件。

### 5.4 测试与评估流程

1. 加载 Stage A 检查点、Stage B 记忆库与融合器。
2. 对测试序列进行标准化和滑动窗口切分。
3. 对每个窗口提取多尺度补全误差、状态新颖度和记忆匹配分数。
4. 将窗口级 Patch 分数扩展回时间点，并对重叠窗口覆盖的时间点进行平均聚合。
5. 生成原始分数、校准分数和最终融合分数。
6. 计算点级指标；若数据包含序列范围信息，则进一步计算序列级指标。
7. 保存 NPY、NPZ、JSON、CSV 与图像等结果文件。

## 6. 输入输出与数据流程

### 6.1 输入

系统输入包括三类信息：

- 数据输入：正常训练序列、测试序列及测试标签。
- 配置输入：窗口长度、Patch 尺度、检索参数、训练参数、融合方式等。
- 运行输入：阶段选择、实验名称、数据根目录、工件目录等命令行参数。

### 6.2 中间数据流

数据在系统中的主要流转链路如下：

原始序列 -> 数据校验与缓存 -> 训练/验证切分 -> 标准化 -> 滑动窗口 -> STSD 分解 -> 状态编码/多尺度 Patch 编码 -> Stage A 训练或 Stage B 记忆构建 -> 两级检索 -> 子分数生成 -> 校准融合 -> 指标评估与可视化输出。

其中，Stage A 只在训练子集上拟合标准化参数；Stage B 和 Test 均复用该标准化器，保证训练-推理统计口径一致。

### 6.3 输出

系统输出包括以下几类工件：

- 模型工件：`stage_a.pt`、`stage_a_last.pt`
- 记忆工件：`memory.pt`、Faiss 索引、`memory_meta.json`
- 融合工件：`cdf_fusion.*`、`zscore_fusion.json`
- 评分工件：各类 `test_scores_*.npy`、`test_diagnostic_scores.npz`
- 评估工件：`test_metrics.json`、可选 `test_sequence_scores.*`
- 可视化工件：训练曲线、分数分布图、异常时间轴图

## 7. 创新点与亮点

### 7.1 从“全局正常模板”转向“条件化局部正常参考”

项目不采用单一重构器或全局原型库，而是通过状态检索与上下文匹配构建局部正常邻域，使异常判断建立在更贴近当前工况的参考基础上，提升了多工况环境下的判别精度。

### 7.2 慢变状态与快变残差分而治之

通过 STSD 分解将工况背景信息与异常敏感信息解耦，避免单一编码器同时承担状态识别和局部异常建模两类冲突任务，提高了表征稳定性与可解释性。

### 7.3 多尺度记忆匹配机制

同一序列同时在短尺度和长尺度上建立局部记忆，能够兼顾尖峰类异常、漂移类异常和阶段性结构异常，增强了对复杂时间模式的适应能力。

### 7.4 补全驱动的记忆库自清洗

补全误差不仅用于训练，还直接参与记忆库构建阶段的质量控制，使正常样本库具备自筛选能力，能够在无额外监督的情况下提升记忆纯度。

### 7.5 校准式多证据融合

通过对检索分数和补全分数进行统计校准，再执行融合，而非直接人工加权，减少了分数尺度差异造成的不稳定性，增强了跨数据集的可迁移性。

### 7.6 完整工程闭环

项目不仅包含算法实现，还包含恢复训练、工件版本化、可视化导出、消融脚本和多协议评估逻辑，具备较强的实验复现性和项目交付能力。

## 8. 可改进方向与未来规划

### 8.1 引入自适应融合策略

当前最终融合方式主要在 `raw_max`、`zscore_mean`、`cdf_max`、`cdf_mean`、`cdf_softmax` 中选择，后续可进一步引入基于状态新颖度、邻域覆盖率或不确定性的动态加权机制，实现更细粒度的自适应决策。

### 8.2 优化长序列检索效率

当训练规模继续扩大时，当前两级检索与 Patch 级记忆库仍存在显著存储和查询开销。后续可引入更高效的向量压缩、分层索引和增量更新机制，提升在线部署能力。

### 8.3 增强跨域泛化能力

现有系统主要面向单数据集单模型训练场景，未来可探索跨数据集预训练、领域自适应归一化和工况迁移学习，以提高模型在新场景中的冷启动性能。

### 8.4 补充在线与流式检测能力

当前流程以离线构建和批量测试为主，后续可将记忆更新、滑窗缓存和实时融合策略改造为流式推理框架，适配在线工业监测与实时安全告警场景。

### 8.5 强化可解释分析

现有输出已经具备状态新颖度、kNN 距离、补全误差和时间轴图等解释信号，未来可继续增加邻居回溯、通道贡献度分析和异常片段对照展示，提升结果可解释性和业务可用性。

