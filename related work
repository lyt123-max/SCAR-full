\section{相关工作}

\subsection{基于重建的异常检测}

基于重建的方法是无监督多变量时间序列异常检测（TSAD）中最广泛采用的范式。其核心假设是：仅在正常数据上训练的模型在面对异常输入时会产生更高的重建误差。早期工作将普通自编码器~\cite{sakurada2014anomaly}及其鲁棒变体~\cite{zhou2017anomaly}应用于学习正常模式的压缩表示。为了更好地捕捉时序依赖，循环架构被引入：基于LSTM的编码器-解码器~\cite{malhotra2015long,malhotra2016lstm}建模序列动态，而随机循环网络如OmniAnomaly~\cite{su2019robust}则结合变分推断来处理多变量序列固有的随机性。变分自编码器（VAE）及其扩展进一步为隐空间赋予概率结构，实现了不确定性感知的异常评分~\cite{park2018multimodal,kieu2022anomaly,kieu2022robust}。近年来，基于Transformer的检测器日益突出：Anomaly Transformer~\cite{xu2021anomaly}引入了基于自注意力机制的关联差异准则，TranAD~\cite{10.14778/3514061.3514067}结合对抗训练与元学习实现数据高效的检测，DCdetector~\cite{yang2023dcdetector}利用双重注意力和对比学习来锐化正常-异常边界。DADA~\cite{DADA}通过自适应瓶颈和双对抗解码器进一步追求通用性。

尽管这些方法效果显著，但基于重建的方法存在一个根本性局限：重建目标是全局定义的，未以运行状态或局部时序上下文为条件。因此，单一解码器必须适应所有运行工况，当系统呈现多模态正常行为时，决策边界容易模糊。我们的方法通过将全局重建参考替换为经两级检索构建的条件感知局部正常邻域来解决这一局限。

\subsection{时序分解与多尺度建模}

工业时间序列通常表现出交织的慢变化趋势和快变化瞬态，这推动了基于分解的方法的发展。TFAD~\cite{zhang2022tfad}应用时频分析对输入进行分解，并分别在分解后的分量上执行异常检测。D$^3$R~\cite{wang2023drift}在动态分解的分量上使用基于扩散的重建来处理非平稳漂移。CATCH~\cite{wu2024catch}引入频率分片以在多种粒度下捕捉通道感知的频谱模式，而Nam等人~\cite{nam2024breaking}则显式地解决了时域和频域表示之间的粒度不一致问题。

与分解互补的是，多尺度时序建模已被证明能有效捕捉在不同时间尺度上表现出的异常。TimesNet~\cite{wu2022timesnet}将一维时间序列转换为按学习到的周期索引的二维张量，通过二维卷积实现多尺度特征提取。PatchTST~\cite{nie2022time}将时间序列分割为固定长度的片段，提供了一种自然的多分辨率视角，后续的异常检测器如PatchTrAD~\cite{vilhes2025patchtrad}也采用了这一思路。CrossAD~\cite{CrossAD}进一步利用跨尺度关联与跨窗口建模来同时捕捉局部和全局异常模式。

我们的慢-快时间序列分解（STSD）模块与分离异构时序动态的动机一致，但目的不同：STSD并非将分解后的分量送入独立的检测头，而是将慢分量路由至状态检索分支、将快残差路由至片段级匹配分支，从而提供直接服务于下游两级检索的面向任务的归纳偏置。

\subsection{记忆增强与原型学习方法}

记忆增强网络通过维护正常模式的显式存储库，提供了纯重建之外的替代方案。Gong等人~\cite{gong2019memorizing}首先在视觉领域引入了记忆增强自编码器用于异常检测，其中解码器被约束为仅使用存储的正常原型来重建输入。这一思想被MEMTO~\cite{song2023memto}扩展到时间序列领域，该方法为Transformer编码器配备了可学习的记忆槽以总结代表性的正常动态。MIXAD结合记忆组件以提高检测鲁棒性和可解释性。Zhou等人~\cite{zhou2025unsupervised}提出了双域记忆模型，在时域和频域同时维护正常参考。

基于原型的方法提供了互补的视角，通过学习紧凑的代表性锚点来表征正常行为。PUAD以原型导向的方式构建无监督异常检测，而H-PAD~\cite{shen2025learn}学习混合原型以同时捕捉局部和全局正常模式。最近，PGRF-Net~\cite{jeong2026pgrfnet}引入了原型引导的关系融合网络，利用原型建模变量间的诊断关系。

我们的方法与上述两类工作相关但占据不同的位置。现有的记忆和原型方法将存储的表示视为可训练的槽位或紧凑摘要，以单阶段方式与查询输入进行比较。相比之下，我们的框架使用状态级描述符作为组织性锚点来构建两级检索过程：状态向量首先将候选池缩小到相关的运行工况，然后上下文感知的片段匹配从该工况内选择细粒度的正常参考。检索到的条目不是抽象原型，而是显式的历史案例，使得该框架在概念上更接近基于案例的推理（CBR），即通过检索和复用结构相似的历史实例来辅助决策。从这一视角看，我们设计中的原型扮演检索的组织角色，而检索到的片段提供案例级的异常评估证据。

\subsection{基于图的方法与变量间关系建模}

多变量时间序列天然涉及变量间的复杂依赖关系，这推动了基于图的方法的发展。GDN~\cite{deng2021graph}在传感器变量上学习图结构，并使用图注意力传播信息，将偏离学习到的关系模式视为异常。InterFusion~\cite{li2021multivariate}建模层次化的指标间和时序嵌入，以捕捉变量内和变量间的依赖。Zhang等人~\cite{zhang2024graph}通过监测工业过程中学习到的图结构变化来检测异常，而CausalFormer~\cite{kong2024causalformer}引入了可解释的Transformer用于时序因果发现，可作为因果感知异常检测的骨干网络。

这些方法聚焦于建模变量间的关系结构，与我们关注的以运行状态和局部时序上下文为条件的正常性评估是正交的。我们的状态条件变量门控机制（公式9）通过基于全局运行状态动态调整变量相关性，提供了一种轻量级的变量间建模形式，但我们方法的主要创新在于基于检索的范式，而非关系图建模。

\subsection{本文方法的定位}

我们框架所采用的粗到细检索策略借鉴了其他领域中研究的层次化检索范式。在视觉定位中，HF-Net执行全局检索以获取候选位置，然后在候选范围内细化匹配，展示了层次化搜索在效率和鲁棒性方面的有效性。在工业视觉检测中，PatchCore构建经核心集压缩的正常片段特征记忆库，并通过最近邻检索检测异常。我们的方法将这些思想适配到时间序列场景：状态级检索（Level 1）在功能上类似于全局候选选择，而上下文感知的片段检索（Level 2）在所选工况内执行细粒度匹配。

总而言之，尽管我们的方法在表面上与基于重建、记忆增强和原型学习的TSAD模型共享某些组件，但其核心贡献更准确地描述为一个\emph{状态引导、上下文感知的粗到细检索框架}，用于多变量时间序列异常检测。创新之处不在于引入原型或记忆本身，而在于利用慢-快分解产生面向任务的表示，并通过两级层次结构组织显式的案例检索，使正常参考能够动态适应每个查询的运行状态和时序上下文。
